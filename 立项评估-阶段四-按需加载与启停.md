# skill-mcp-studio 阶段四立项评估 — 按需加载与启停

## 版本

| 版本 | 日期 | 说明 |
|------|------|------|
| v0.1 | 2026-09-01 | 立项评估草案：现有资产盘点 + 维度3/4/维度2工程可行性 定稿；维度1/竞品对照/结论 待调研 |
| v1.0 | 2026-09-01 | 定稿：竞品调研填充维度1（需求真实）+ 维度2（差异化方向=启停一致性审计/禁用生效验证/回滚幂等）；四维度均通过，**立项通过** |

> 本文档是**立项评估**，回答「阶段四是否立项、若立项做什么、怎么对齐安全与边界」。不是详细设计——立项通过后另起《详细设计文档-阶段四》。评估维度取自《产品形态与开发计划》§11：需求确认 / 形态选择 / 安全对齐 / 边界复核。

---

## 1. 评估背景与立项门槛

阶段四（按需加载与启停）在产品规划中明确定位为**增值功能，不纳入主线承诺**：

- 《产品形态与开发计划》§4 形态阶梯：`L3 按需加载与启停 = L1/L2 之上的增值开关层（阶段四，视需求立项）`；
- 《产品定位与路线图规划》§4 路线图：`阶段四 | 技能启用/禁用、按会话加载（增值功能） | 视需求 | 立项时另定`；
- 《产品定位与路线图规划》§6 非目标：阶段四「进入 skills-hub 类工具的竞争领域」，仅作增值。

**立项门槛**：§11 的四个维度**均须通过**才立项；任一维度不达标则维持 M3（阶段三只读看板）形态，不强行推进。其中维度1（需求确认）是**阈值门槛**——「统计阶段三上线后『技能冲突/加载过重』类反馈的真实占比，达到阈值再立项」。

> 评估时点事实：阶段三（只读看板 + Tauri `.app`）于本次评估当日 push 上线，**尚无任何用户反馈数据**。这直接影响维度1的评估方法（见 §2）。

## 2. 评估方法

| 维度 | 数据来源 | 评估方法 |
|------|----------|----------|
| 1 需求确认 | 阶段三上线反馈（**缺失**） + 竞品启停需求调研 | 因本工具零反馈，用竞品在「启停」上的需求强度与覆盖度**替代**评估需求是否真实存在且无成熟替代 |
| 2 形态选择 | 现有资产工程可行性（代码盘点） + 竞品启停范式 | 工程可行性基于盘点定稿；竞品范式对照差异化窗口 |
| 3 安全对齐 | 代码盘点（`mcp_fixer.py` / `fixer.py` 工作流） | 直接判定：启停写操作能否复用现有备份-原子-校验-回滚范式 |
| 4 边界复核 | 规划 §6 非目标 + 启停操作对象 | 直接判定：启停是否滑入技能商店/同步分发 |

---

## 3. 现有资产与启停的关系（盘点小结）

> 详见并行 subagent 盘点报告（已交叉核实 `skills_analyser.py` / `checker.py` 描述准确）。此处只列立项评估所需结论。

### 3.1 核心事实：操作粒度不同

| 维度 | 现有 skills 管理 | 阶段四「启停」所需 |
|------|------------------|---------------------|
| 操作对象 | **客户端级链接**：`fixer.py:fix_path` 把客户端 skills 路径（如 `~/.claude/skills`）修复为**一条**指向 `unified_skills_dir`（`~/.skills-manager/skills`）的 symlink | **仓库内技能级**：在 `unified_skills_dir` 仓库内对**单个技能**做启用/禁用，因所有客户端链接都指向该仓库，一改全影响 |
| 粒度 | 一客户端一条根链接 | 一技能 × 多客户端的逐条目链接 |
| 状态 | 路径 6 态（`correct/real_dir/missing/wrong_link/broken_link/skipped`，`checker.py:340-352`） | 需技能级 enabled/disabled 状态 |

### 3.2 可直接复用的资产

- **安全工作流黄金范式**：`core/mcp_fixer.py:389-478` —— 备份（`shutil.copy2` 到 `.bak-<ts>`）→ 原子写（`tempfile.mkstemp`+`fsync`+`os.replace`）→ 写后校验（`_validate_written_config` 重读校验）→ 失败回滚（`_restore_backup`）。**这是启停写操作可直接套用的安全骨架**。
- **复检闭环**：`scan.py:523-536`（`--setup-all` 复检）+ `core/combined_checker.py:160-189` `result_ok` 退出码判定 —— 启停完成后可复用产出退出码。
- **技能清单扫描**：`core/skills_analyser.py:16-98` `scan_skills_distribution` —— 可作为开关状态初始化的技能发现来源。
- **DSH 式逐条目软链检测范式**：`core/checker.py:52-76` `_all_entries_point_to_unified` —— 证明了「目录内逐条目 symlink」是**已认可的合规形态**，启停的「单技能 symlink」与其同构（只读检测已有，缺对应创建器）。

### 3.3 需新建 / 加固的空白点

| # | 空白 | 现状参照 | 启停所需 |
|---|------|----------|----------|
| 1 | 无技能级身份/状态模型 | `config.yaml:154-189` `tools` 是客户端→仓库根 | 技能一等实体（name/enabled）与状态存储 |
| 2 | 无 enable/disable 状态文件 | `change_tracker` state.yaml 只存路径 symlink 差异 | 技能级开关状态文件 |
| 3 | 无单技能 symlink 启停函数 | `fixer.py:fix_path` 粒度是客户端根 | `enable_skill/disable_skill` 逐条目操作 |
| 4 | symlink 写入非原子、写后不校验 | `fixer.py:88-108` | 套用 `mcp_fixer.py` 原子+校验+回滚三段式 |
| 5 | 无运行时会话注入路径 | `hooks_checker` 仅检存在 | 远期子项，当前零基础 |
| 6 | 无 CLI 开关 flag | `scan.py:230-262` 仅有 `--skills`/`--fix-skills` | `--enable-skill`/`--disable-skill`/`--list-skill-state` |
| 7 | 无冲突/依赖规则引擎 | `skills_analyser:101-150` 仅关键词重叠提示 | 「启用 A 禁用 B」互斥规则 |
| 8 | config schema 无技能级条目 | 仅有 `tools[].skills_paths`/`mcp_tools[]` | 扩展 `skills[]` 声明 |

---

## 4. 维度1：需求确认

阶段三当日 push 上线，本工具零用户反馈，无法用规划 §11 要求的「反馈占比」直接判定。改用**竞品在「技能启停/按会话加载」上的需求强度与覆盖度**替代评估：

### 4.1 竞品启停需求强度

| 竞品/生态 | 启停支持 | 机制 | 区分「已装但禁用」vs「未装」 |
|-----------|----------|------|------------------------------|
| **skills-hub**（qufei1993/skills-hub） | ✅ 内置 | 中心库 `enabled` 标志位 + symlink 同步到各工具目录 | ✅ 中心库已装是事实，enabled 是另一维度 |
| **skillhub**（RsLuna7/skillhub） | ✅ | Rust CLI + MCP server，本地 SQLite trust allow/block/reset | ✅ blocked 技能从 MCP 消失但保留索引 |
| **claude-code-tool-manager** | ✅ | 跨客户端改写 `.claude.json`/`config.toml`/`mcp.json` + **Profiles**（命名快照一键切换） | ✅ 全局启用 vs per-project 启用 toggle |
| **Claude Code 原生** | ✅ 最完整 | `/mcp` 面板 toggle，`disabledMcpServers`/`disabledMcpjsonServers` 两条 opt-out 列表 | ✅ "disable without removing" |
| **Codex CLI 原生** | ⚠️ 有 profile 概念 | `~/.codex/config.toml` + `codex --profile`；**已知 bug：disables 被忽略**（issue #17588） | 部分 |
| **MCP-Server-Selector**（henkisdabro） | ✅ | TUI **三态** RED(off)→GREEN(on)→ORANGE(available but disabled) | ✅ ORANGE 中间态明确建模 |
| agentregistry | ❌ | 注册中心审批/采纳门禁，无运行时/per-机器启停 | — |
| skill-mcp-manager | ⚠️ 推断 | LobeHub 有条目，repo 不可达，机制未验证 | 未知 |

> **2026-09-04 核实补记（决策记录 D14）**：repo 现已可达——[alone-tree/dsh-skill-mcp-manager](https://github.com/alone-tree/dsh-skill-mcp-manager)（MIT），DSH 宿主级插件「能力库」：设置页可视化管理 SKILL/MCP + MCP 按需加载（目录只暴露 name+描述、会话内热加载、按会话实例）+ 技能隐藏（`disable-model-invocation`）+ 单工具黑名单，仅作用于所装 profile。与 skill-mcp-studio 不同层（运行时层 vs 配置/文件系统层）、不同域（单 DSH 宿主 vs 跨客户端治理），互补不重叠；本机未安装。上表「未知」收口为「✅ 已验证，无重叠风险」。

来源：`qufei1993/skills-hub`（README，高可信）、`RsLuna7/skillhub`（README，高可信）、`tylergraydev/claude-code-tool-manager`（README，高可信）、Claude Code 官方文档 `code.claude.com/docs/en/mcp` + 社区镜像 `ericbuess/claude-code-docs`（高可信）、`henkisdabro/Claude-Code-MCP-Server-Selector`（README，高可信）、Codex issues #17588/#13075（高可信）。skill-mcp-manager 因 repo 不可达，仅 LobeHub 发布页（低可信），立项前需人工核实。

### 4.2 需求结论

竞品**普遍实现且用户买账**（skills-hub 的 enabled state、Claude Code 原生 disabledMcpServers、MCP-Server-Selector 的三态建模均为活跃功能而非提案），说明「技能启停」需求**真实存在**，非伪需求。按 §2 评估方法，这**降低**了本工具的反馈门槛——需求由竞品态势确认，不必等待本工具自有反馈积累。

**但**：竞品已密集覆盖「开关同步/配置改写」本身，本工具若仅复刻 symlink 开关则与 skills-hub/claude-code-tool-manager 正面重叠，落入规划 §6「刻意不做」的竞争领域。维度1**通过（需求真实）**，但**立项形态须避开开关同步器，转向差异化空白**（见 §5.2）。

## 5. 维度2：形态选择

### 5.1 工程可行性（基于盘点，已定稿）

规划 §11 给出形态方向：「先做只读开关（生成/移除符号链接或快捷方式）再考虑运行时会话注入」。盘点结果**支持此方向**：

- **只读开关（近期）**：在客户端 skills 目录内对单个技能建/删 symlink —— 与 `checker.py:52-76` 已认可的 DSH 式逐条目软链形态同构（只读检测已有，缺创建器，空白 #3）。安全可对齐（见 §6）。
- **运行时会话注入（远期）**：`hooks_checker` 仅检测 hooks 配置存在性，不驱动装载；无 session 上下文读取 —— **零基础**（空白 #5），属阶段四远期子项，首版不做。

**形态可行性结论**：只读 symlink 开关工程可行，复用现有 `_all_entries_point_to_unified` 检测范式 + 新建 `enable_skill/disable_skill`；运行时注入暂不纳入。

### 5.2 竞品范式对照（已填充）

**主流启停范式**（竞品调研总结）：**配置文件标志位 + 符号链接/配置改写**——

- 中心库 `enabled` 标志 + symlink 同步到各工具目录（skills-hub）；
- 直接改写各客户端 `.claude.json`/`config.toml`/`mcp.json` 的 mcpServers 或 disabled 列表（claude-code-tool-manager、Claude Code 原生、Codex profile）；
- 以 Profiles/快照提供可逆性（claude-code-tool-manager）；
- 多数已区分「已装但禁用」与「未装」（Claude Code "disable without removing"、MCP-Server-Selector ORANGE 中间态、skills-hub enabled state）。

**运行时按会话注入现状**：**生态中无任何工具做到「对正在运行的会话运行时注入技能而不改配置/不重启」**。最先进者（MCP-Server-Selector）也停在「退出→改配置→重启→该会话只载入选定集合」；Claude Code 的 `--mcp-config` 会话注入对「禁用」还有泄漏 bug（issue #36868）。→ 印证 §5.1「运行时注入是远期子项、首版不做」的判断正确。

### 5.3 三处生态空白 → 本工具差异化方向

竞品调研识别出三处空白，**均落在 skill-mcp-studio「验证与修复」定位内**（竞品负责安装与同步/开关，无人负责校验启停状态是否一致、可逆、真正生效）：

| # | 空白 | 竞品现状 | 本工具立项形态 |
|---|------|----------|----------------|
| 1 | **真正的运行时按会话注入** | 几乎无人做到（含 bug） | **不做**（远期，§5.1 已排期） |
| 2 | **跨客户端启停状态一致性审计** | 各工具语义碎片化（disabledMcpServers vs profile vs symlink 存在与否），无工具校验「某技能在 A 客户端禁用但在 B 仍启用」的漂移 | ✅ **立项核心**：`--audit-skill-states` 检测跨客户端启停漂移，复用四层验证模型 |
| 3 | **启停可逆性/回滚审计薄弱** | Profile 快照存在，但无工具验证「禁用是否真的从运行时消失、是否可幂等回滚」 | ✅ **立项核心**：启停操作的禁用生效验证 + 回滚幂等性 gate |

**形态对照结论**：本工具阶段四**不应造一个 symlink 开关同步器**（与 skills-hub/claude-code-tool-manager 正面重叠，落入 §6 刻意不做领域），而应聚焦**「启停状态一致性审计 + 禁用生效验证 + 回滚幂等性」**——把现有四层验证模型（安装证据/路径合规/配置正确/端点活体）**延伸到启停维度**：第 5 层 = 启停状态是否跨客户端一致、禁用是否真正生效、回滚是否幂等。这正契合产品定位「竞品负责安装与同步，skill-mcp-studio 负责验证与修复」。

## 6. 维度3：安全对齐（已定稿）

规划 §11 第 3 条硬性要求：「任何启停操作复用现有『备份-原子-回滚』工作流，不引入新的写路径」。盘点确认**可对齐**：

| 安全环节 | 复用资产（文件:行） | 启停落地 |
|----------|---------------------|----------|
| 备份 | `mcp_fixer.py:389-391,455-460` `_backup_path` | 启停操作前备份受影响的 skills 目录/配置 |
| 原子写 | `mcp_fixer.py:394-408` `_atomic_write` | 文本状态文件（技能开关状态 yaml）直接套用；symlink 原子替换需平台适配（见下） |
| 写后校验 | `mcp_fixer.py:355-386` `_validate_written_config` | 启停后重读校验：链接是否指向预期、目录是否可解析 |
| 失败回滚 | `mcp_fixer.py:411-414,471-478` `_restore_backup` | 校验失败 → 原子恢复备份 |
| 先决门 | `mcp_fixer.py:427-429` `fix_supported` 拒绝 | 启停对 `fix_supported: false` 客户端拒绝 |
| 复检闭环 | `scan.py:523-536` + `combined_checker.py:160-189` `result_ok` | 启停后复检产出退出码 |

**需加固项**（空白 #4）：`fixer.py:88-108` 的 symlink 创建**非原子**（`unlink`/`move` 与 `symlink` 之间有窗口、路径短暂缺失）且**写后不校验**（依赖外部复扫）。启停的单技能 symlink 操作**必须**升级到 `mcp_fixer.py` 的原子+校验+回滚三段式，而非照搬现有 `fixer.py`。

> symlink 原子替换的平台兼容：`os.replace` 对 symlink 在 POSIX 上原子替换路径项，但「先建临时 symlink 名再 replace」需注意 macOS/Linux 对 symlink 目标的 `rename` 语义；Windows 上 symlink 创建需开发者模式/管理员。首版 macOS 首发（与阶段三一致），Windows 兼容沿用阶段二 §14.5 的延期口径。

**安全对齐结论**：启停写操作**可完全复用** `mcp_fixer.py` 的备份-原子-校验-回滚范式，不引入新写路径；仅需把 `fixer.py` 的 symlink 路径从「非原子+无校验」升级到三段式。安全维度**通过**。

## 7. 维度4：边界复核（已定稿）

规划 §6 + §11 第 4 条要求：「确认不滑入技能商店/同步分发（那是刻意不做的领域）」。启停的边界**清晰且不越界**：

| 刻意不做（§6） | 启停是否触及 | 说明 |
|----------------|--------------|------|
| 技能市场/商店（agentregistry、gh skill 领域） | ❌ 不触及 | 启停只开关**本地已存在**技能，不做技能的分发/上架/检索 |
| 多用户权限体系与云端同步 | ❌ 不触及 | 启停的「仓库」是本地 `unified_skills_dir`，状态存本地文件，无云端、无多用户 |
| 写入型 GUI 按钮（阶段三前） | ✅ 阶段三已过 | 启停仍是 CLI 显式触发（`--enable-skill`/`--disable-skill`），GUI 维持只读，与产品 §8.1 硬边界一致 |

**边界复核结论**：启停 = 本地技能 symlink 开关 + 本地状态文件，不涉及分发/市场/云端/多用户。边界**清晰、不越界**。维度4**通过**。

---

## 8. 立项结论（已定稿）

四维度评估结果：

| 维度 | 结论 | 依据 |
|------|------|------|
| 1 需求确认 | ✅ 通过 | §4：竞品普遍实现且用户买账（skills-hub enabled state、Claude Code disabledMcpServers、MCP-Server-Selector 三态均为活跃功能），需求真实存在；竞品态势确认降低本工具反馈门槛 |
| 2 形态选择 | ✅ 通过 | §5：工程可行（symlink 开关复用 `_all_entries_point_to_unified` 范式）；差异化方向明确（§5.3 三处空白，不造开关同步器） |
| 3 安全对齐 | ✅ 通过 | §6：启停写操作可完全复用 `mcp_fixer.py` 备份-原子-校验-回滚范式，不引入新写路径 |
| 4 边界复核 | ✅ 通过 | §7：启停=本地技能 symlink 开关 + 本地状态文件，不涉及分发/市场/云端/多用户，不越界 |

**立项结论：通过立项。** 四维度均达标，阶段四正式立项。

**立项形态（聚焦差异化，避开开关同步器竞争）**：不复刻 skills-hub / claude-code-tool-manager 的 symlink 开关同步（落入 §6 刻意不做领域），而聚焦竞品空白 §5.3#2/#3——把现有四层验证模型**延伸到启停维度**：

- **第 5 层验证 = 启停状态一致性**：`--audit-skill-states` 检测「某技能在 A 客户端禁用但在 B 客户端仍启用」的漂移，跨客户端语义统一校验（竞品无人做）；
- **禁用生效验证**：校验禁用是否真的从运行时消失（针对 Claude Code issue #36868 的禁用泄漏类缺陷）；
- **回滚幂等性 gate**：启停操作的回滚是否幂等可重复（针对 Profile 快照无合规验证的空白）。

**首版范围**：只读 symlink 开关（§5.1 近期）+ 启停一致性审计 / 禁用生效验证 / 回滚幂等 gate（§5.3 差异化）；运行时按会话注入（§5.1 远期）暂不纳入（生态无人做到，首版不做）。

**遗留行动项**：
- skill-mcp-manager（§4 表，repo 不可达）立项前需人工核实其启停/回滚语义，确认无重叠；
- 立项后另起《详细设计文档-阶段四-按需加载与启停》，落实空白点 #1-#8（§3.3）的具体 schema / 模块 / CLI 设计。

---

## 附录 · 立项维度与规划 §11 对照

| §11 维度 | 本文 | 状态 |
|----------|------|------|
| ① 需求确认 | §4 | ✅ 通过 |
| ② 形态选择 | §5 | ✅ 通过 |
| ③ 安全对齐 | §6 | ✅ 通过 |
| ④ 边界复核 | §7 | ✅ 通过 |
