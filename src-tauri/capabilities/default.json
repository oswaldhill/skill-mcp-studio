{
  "identifier": "default",
  "description": "Default capability for the Skill MCP Studio app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open"
  ]
}
