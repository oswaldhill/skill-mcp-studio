# skill-mcp-studio 详细设计文档

**——阶段五：通用 Skill/MCP 管理台重塑**

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-05 | 产品转向定案：只读审计看板 → 通用管理型桌面 App（三栏管理台 + 端点库 + 多端点挂载 + GUI 写操作） |

---

## 1. 引言：这是一次产品转向

### 1.1 现状回顾

阶段一至四把 skill-mcp-studio 做成了「**多客户端 Skill/MCP 合规审计工具**」：

- **数据模型**：`profiles`（endpoint 声明集合）+ `active_profile`（单激活端点）+ `mcp_tools`/`tools`（客户端注册表）+ `unified_skills_dir`（统一技能目录）。
- **检查模型**：对每个已安装客户端 × **单一** active endpoint，逐层验证「安装 → 路径合规 → 配置正确 → 端点活体 + 能力组」。
- **GUI**：Tauri 壳渲染 `gui/dashboard.html`，一张**只读**红黄绿矩阵，写操作刻意不暴露（文档 §6「阶段三之前不做任何写入型 GUI 按钮」）。

### 1.2 转向目标

用户明确反馈「整个 APP 的样式包括内容都不是我要的」，提出三组功能需求，把产品从「验证器」转向「**管理器**」：

| 需求组 | 内容 | 关键语义变化 |
|--------|------|--------------|
| ① IDE/Agent 检查 | 检查本机所有 IDE/Agent 安装；支持手动配置或自动检查；检查配置是否正确 | 从「固定注册表」→「声明 + 自动发现 + 手动」三来源 |
| ② Skill 检查与管理 | 检查所有 Skill 安装；目录可指定或默认；判定统一/单独目录；**一键合并** | 新增「逐技能清单」视图与「一键合并」写操作 |
| ③ MCP 配置与管理 | 手动指定或自动识别 MCP；检查各 IDE/Agent 是否配置；**支持多个 MCP** | 单一端点 → **端点库 + 每客户端多端点挂载** |

### 1.3 已定案的决策（2026-09-05 用户确认）

1. **多 MCP 语义**：两者都要 —— 全局**端点库**（可增删改）+ **每个客户端可挂载多个端点**。
2. **实现策略**：**复用核心引擎**（安装检测 / Skill 判定 / symlink 合并 / MCP 探活 / 备份-原子写-回滚，263 测试保护），在其上扩展数据模型，**重建 GUI**。
3. **产品形态**：**管理型桌面 App**（Tauri），三栏管理台。
4. **写操作**：**GUI 直接写**（一键合并 / 增删端点 / 挂载调整），Tauri 后端新增写命令，沿用现有备份-原子写-回滚安全链。
5. **MCP 呈现**：**统一端点库管理**（全局表 + 每客户端多选挂载，逐条目探活/能力）。
6. **旧矩阵**：**拆入三栏**，不再有单独红黄绿矩阵首页。
7. **注册来源**：**声明 + 自动发现 + 手动**三来源并存。

---

## 2. 目标形态：三栏管理台

```
┌────────────────────────────────────────────────────────────────┐
│  skill-mcp-studio  ·  通用 Skill/MCP 管理台                       │
├──────────┬─────────────────────────────────────────────────────┤
│          │  [ IDE / Agent ]  [ Skills ]  [ MCP ]                │
│  侧栏    ├─────────────────────────────────────────────────────┤
│  概览    │                                                     │
│  计数    │  各 Tab 内容见 §3                                     │
│          │                                                     │
│          │                                                     │
└──────────┴─────────────────────────────────────────────────────┘
```

三栏即三组需求的一一对应，每栏同时承担「检查结果展示」与「管理动作」：

| Tab | 读（检查） | 写（管理） |
|-----|-----------|-----------|
| **IDE / Agent** | 本机所有 IDE/Agent 安装状态 + 配置正确性 | 手动添加/编辑客户端、重新扫描 |
| **Skills** | 所有 Skill 安装清单；每客户端统一/单独目录判定 | 指定统一目录、**一键合并** |
| **MCP** | 端点库表；每客户端挂载 + 逐端点探活/能力 | 端点库增删改、客户端挂载多选调整 |

---

## 3. 数据模型 v2（schema_version: 2）

### 3.1 核心转变：「单 active 端点」→「端点库 + 每客户端多挂载」

现状 `profiles` 已经是一个**端点声明集合**（每个 profile = 一个 MCP endpoint 的完整声明：name/url/transport/url_policy/required_capabilities/legacy_names）。阶段五不推翻它，而是改**检查模型**：

- **端点库**：保留 `profiles` 结构（语义重述为「endpoint 库」，字段不变，保证向后兼容）。
- **每客户端挂载**：客户端条目新增可选 `mcp_attach: [endpoint_key, ...]`，声明该客户端应接入哪些端点；缺省时**等价于挂载全部端点**——即保持阶段二 `--all-profiles`「每 profile × 全客户端」全矩阵与 `--full` active 路径逐行不变的旧行为。
- **检查遍历**：对每个已安装客户端 × 其挂载的每个端点，逐条产出「配置正确 / 连通 / 能力组」结果。

### 3.2 目标配置形态（示意，不含个人拓扑）

```yaml
schema_version: 2

active_profile: hermes-home          # 语义弱化为「默认端点」（新客户端/未声明挂载时的缺省）

profile_sources:
- ~/.skills-manager/profiles.local.yaml   # 真实端点仍走仓库外覆盖

profiles:                            # 端点库（= 阶段一 profiles，语义重述）
  hermes-home:
    name: hermes
    url: https://hermes.example.com/mcp
    transport: streamable-http
    url_policy: strict
    required_capabilities: { hermes_memory: [...], ai_memory: [...], tdai: [...], hermes: [...] }
    legacy_names: [hermes-nas, hermes-memory, hermes-gateway, ai-memory]
  # 第二个端点示例（多 MCP 能力本质来源）
  # another-mcp:
  #   name: another
  #   url: https://other.example.com/mcp
  #   required_capabilities: { group: [tool...] }

mcp_tools:                           # 客户端注册表：新增可选 mcp_attach
  - name: Cursor
    ...
    mcp_attach: [hermes-home]        # 新增：挂载端点列表；缺省 = [active_profile]
    ...

unified_skills_dir: ~/.skills-manager/skills
```

### 3.3 三来源客户端发现（需求 ①b、③a）

| 来源 | 实现 | 现状 |
|------|------|------|
| **声明** | `mcp_tools` + `tools` 静态注册表 | ✅ 已有 |
| **自动发现** | `scanner.scan_for_new_tools` / `auto_discover.scan_paths` + `/Applications` app bundle 扫描 | ⚠️ 已有 skills 路径扫描，需补 app bundle 扫描 |
| **手动** | GUI「添加客户端」→ 写入持久化 discovered 文件 | ⚠️ 已有 `save_discovered_tools`，需暴露到 GUI |

### 3.4 自动识别本机已有 MCP（需求 ③a）

新增能力：**扫描每个客户端配置文件，列出其当前已配置的全部 MCP 条目**（不只 hermes 正典名），区分：

- 命中端点库 → 标「已挂载该端点」（匹配 name/url）；
- 未命中但形似 MCP 条目 → 标「未纳管 MCP 条目」，可一键纳入端点库；
- legacy 正典名 → 沿用现有 legacy 判定。

---

## 4. 模块边界：复用 vs 新建

### 4.1 完全复用（不动）

| 模块 | 职责 |
|------|------|
| `core/tool_registry.py` | `detect_installation`（app/cli/config 三证据）|
| `core/mcp_probe.py` | 端点 initialize+tools/list 探活（HTTP/stdio 双传输）|
| `core/mcp_fixer.py` | 备份→原子写→重解析验证→回滚安全链 |
| `core/skill_link_migrator.py` | root→per_skill 迁移（含备份/回滚）|
| `core/skill_state.py` / `skill_toggle.py` / `skill_state_auditor.py` | Skill 启停与状态审计 |
| `core/capability_templates.py` | 能力组模板（extends 复用）|
| `core/checker.py` / `scanner.py` | skills 路径扫描与 symlink 判定 |
| `core/profile_loader.py` | profiles 加载 + profile_sources 合并（补 `mcp_attach` 读取）|

### 4.2 扩展改造

| 模块 | 改造内容 |
|------|----------|
| `core/combined_checker.py` | `check_agents` 支持「每客户端多端点」遍历：新增 `mcp_attach` 解析，结果按 `(client, endpoint)` 产出 |
| `core/multi_profile_reporter.py` | 输出结构加入「每客户端 → 每端点」的二维结果，供三栏 GUI 与 CSV/MD 消费 |
| `core/profile_loader.py` | 仅新增读取 `mcp_attach` 字段（无需改 profiles 结构）|

### 4.3 新建

| 模块 | 职责 |
|------|------|
| `core/endpoint_library.py` | 端点库 CRUD（增删改端点，写 profile_sources 覆盖文件）+ 多端点挂载关系维护 |
| `core/client_discovery.py` | 声明 + 自动发现（app bundle + config paths）+ 手动，三来源归一为客户端清单 |
| `core/mcp_inventory.py` | 逐客户端配置解析，列出其当前全部 MCP 条目（纳入端点库 / 未纳管 / legacy 分类）|
| `src-tauri` 写命令 | `add_endpoint` / `remove_endpoint` / `update_endpoint` / `set_client_attach` / `merge_skills` / `set_unified_dir` / `add_client` / `rescan_clients` |

---

## 5. Tauri 命令面（阶段五新增写命令）

沿用现有「CLI 子进程」架构（CLI 是唯一 source of truth，Tauri 只做进程编排与参数传递），写命令走 `scan.py` 新增参数，Tauri 侧加 `spawn_command` 包装，出口码约定不变（0/1=结论，2=工具错误）。

| Tauri command | 转发为 CLI | 说明 |
|---------------|-----------|------|
| `run_audit` | `--all-profiles --format json` | 已有（保留）|
| `add_endpoint` | `--add-endpoint <yaml>` | 端点库增 |
| `remove_endpoint` | `--remove-endpoint <key>` | 端点库删（需确认）|
| `update_endpoint` | `--update-endpoint <key> --set-url ...` | 端点库改 |
| `set_client_attach` | `--client <name> --attach-endpoints key1,key2` | 挂载多选调整 |
| `merge_skills` | `--migrate-skill-links --client <name>`（或 `--fix`） | 一键合并（走现有安全链）|
| `set_unified_dir` | `--set-unified-dir <path>` | 指定统一 skill 目录 |
| `add_client` | `--add-client <yaml>` | 手动加客户端（写 discovered 持久化）|
| `rescan_clients` | `--discover` | 自动发现 |

**写操作安全承诺不变**：所有写命令复用 `mcp_fixer`/`skill_link_migrator` 的备份-原子写-回滚链，GUI 按钮触发前必须二次确认，失败必须可回滚、可解释。

---

## 6. 向后兼容与迁移

1. **schema_version 1 → 2**：`profile_loader` 检测到 v1 时自动补 `mcp_attach` 缺省语义（= [active_profile]），不报错、不强制迁移。
2. **现有 config.yaml 结构不动**：`profiles`/`mcp_tools`/`tools`/`unified_skills_dir` 全部保留；仅新增可选字段。
3. **263 测试全绿门槛**：schema v2 加载 + 检查模型改造后，阶段一~四全部测试必须仍通过（缺省路径 = 旧行为）。
4. **默认行为零变化**：未声明 `mcp_attach` 时，`--full` 输出与改造前逐行一致。

---

## 7. 分阶段实施计划

| 阶段 | 内容 | 交付物 | 验收 |
|------|------|--------|------|
| **P1 数据模型** | `mcp_attach` 解析 + `endpoint_library` + `mcp_inventory`（只读）| core 新模块 + 单测 | 263+ 新增测试全绿；`--full` 逐行不变 |
| **P2 检查模型改造** | `check_agents` 多端点遍历 + `multi_profile_reporter` 二维结果 | core 改造 + 单测 | 单端点缺省=旧结论；双端点出新二维结果 |
| **P3 CLI 管理命令** | `--add/remove/update-endpoint`、`--attach-endpoints`、`--set-unified-dir`、`--add-client`、`--discover` | scan.py 参数 + 单测 | 每命令可独立执行、dry-run、可回滚 |
| **P4 Tauri 写命令** | lib.rs 新增 8 命令 + spawn_command | Rust + 编译通过 | `cargo check` 绿 |
| **P5 GUI 重建** | 三栏管理台替换 dashboard.html | gui/ 重建 | 三栏渲染真实数据、写按钮接线、gate 绿 |
| **P6 文档收口** | 更新总纲/定位/README + BUILD | 文档 | 定位不再自相矛盾 |

每阶段一个 commit + push，详细中文 conventional-commit 消息。

---

## 8. 验收标准（DoD）

1. **三栏管理台可用**：IDE/Agent、Skills、MCP 三栏各自独立展示检查结果，且都能触发对应写操作。
2. **多 MCP 真实生效**：端点库含 ≥2 端点时，客户端可挂载任意子集，逐端点探活/能力独立呈现。
3. **三种发现来源并存**：声明 + 自动发现（app bundle）+ 手动添加，归一后去重、不重复报 UNMANAGED。
4. **写操作安全**：一键合并 / 端点增删 / 挂载调整均走备份-原子写-回滚，dry-run 可预览。
5. **回归零破坏**：阶段一~四全部 263 测试通过，单端点缺省路径输出不变。
6. **GUI/CLI 一致**：`verify_gui_consistency.sh` gate 绿，GUI 结论与 CLI 完全一致。

---

## 附录 A · 与既有文档的关系

本文档是阶段五的「怎么做」。产品定位层面的更新（《产品定位与路线图规划》《产品形态与开发计划》中「只读看板」「刻意不做写入型 GUI」等表述）在 P6 一并收口，避免阶段内文档自相矛盾。

## 附录 B · 术语

| 术语 | 定义 |
|------|------|
| 端点库（endpoint library） | 一组 MCP endpoint 的声明集合（`profiles` 的语义重述）|
| 挂载（attach） | 客户端与端点的多对多关系：`mcp_attach` 声明一个客户端接入了哪些端点 |
| 三栏管理台 | IDE/Agent、Skills、MCP 三个管理视图 |
| 未纳管 MCP 条目 | 客户端配置中形似 MCP 但未命中端点库的条目 |