# Runtime state (local scan snapshots, discovered tools)
data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Packaging
build/
dist/
*.egg-info/

# macOS
.DS_Store

# Rust / Tauri build artifacts & local toolchain cache (CARGO_HOME redirect)
# 注：曾长期保留 .cargo-home/ 与 .rustup-home/，会让人以为「把 RUSTUP_HOME/
# CARGO_HOME 指向仓库内」是预期布局。二者已于 v0.24.0 清理（P2-1），故移除条目；
# 若你在本地又重新生成，请直接把 RUSTUP_HOME/CARGO_HOME 指回 $HOME。
.npm-cache/
src-tauri/target/
src-tauri/gen/

# Tool-generated backups & reports
*.bak-*
scan-report.md

# Personal topology will be moved out in phase-1 step 4; keep untracked until then
profiles-hermes-home/

# Local-only migration notes (contains personal email / SSH paths; never publish)
新设备Git迁移指南.md

# 调试截图（仅本地留存，不入库）
.debug-shots/
