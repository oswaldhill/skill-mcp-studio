# skill-mcp-studio 产品形态与开发计划（总纲）

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-01 | 基于《产品定位与路线图规划》v1.1 与《阶段一详细设计》v1.0 综合，产出产品形态与全阶段可执行开发计划 |
| v1.1 | 2026-09-05 | 阶段五转向定案：形态阶梯 L2 自「只读看板」升级为「三栏管理台（含写操作）」；L3 起追加「阶段五 通用管理台重塑」；详见《详细设计文档-阶段五-通用管理台重塑》 |

> **文档分工**：本文是总纲，回答「产品是什么形态」「怎么分阶段做出来」。《产品定位与路线图规划.md》回答「做什么、为什么与差异化」；《详细设计文档-阶段一-endpoint-profile-抽象.md》回答阶段一「怎么改」。《详细设计文档-阶段五-通用管理台重塑.md》回答阶段五「产品转向后怎么改」。文档层级见附录 A。

---

## 第一部分 · 产品形态

> **阶段五转向注（2026-09-05）**：本部分 §1–§6 为阶段一~四的历史定位（审计/修复工具、只读看板）。阶段五按用户需求将产品重塑为「通用 Skill/MCP 管理台」——三栏管理台（IDE/Agent · Skills · MCP）、GUI 直接写操作、端点库 + 每客户端多端点挂载。下文「刻意不做写入型 GUI」等表述自阶段五起失效，以《详细设计文档-阶段五-通用管理台重塑》为准。

### 1. 一句话定位

> 本地多客户端的 Skill/MCP **通用管理**工具——竞品负责「安装与同步」，skill-mcp-studio 负责「检查、管理与修复」。

这是一个**开发者工具的「管理台」**：它检查本机所有 IDE/Agent 的安装与配置，管理共享 Skills 仓库（统一目录 vs 单独目录、一键合并），管理多个 MCP endpoint（端点库 + 每客户端挂载 + 逐端点探活），并在需要时以**可回滚的安全方式**修复。

### 2. 目标用户与核心场景

| 维度 | 描述 |
|------|------|
| 目标用户 | 同时使用多个 AI 编码助手（Claude Code、Cursor、WorkBuddy、Codex、Reasonix、OpenCode、DSH）的开发者与团队 |
| 典型画像 | 把技能与 MCP 服务集中托管在统一仓库、要求各客户端一致接入的「多客户端重度用户」与维护该托管基础设施的团队 |
| 核心场景 | ① 变更后体检：升级/换机器/改配置后，快速确认哪些客户端掉线、哪些能力缺失；② 一致性治理：多台机器、多客户端接入同一套技能/MCP 时的对齐核查；③ 安全修复：统一端点变更（如迁移新网关）时批量改写客户端配置并逐项回滚保障 |
| 非目标用户 | 只用单一客户端、无集中托管诉求的普通个人开发者（对他们而言，价值密度过低） |

### 3. 价值主张：四层独立验证模型

skill-mcp-studio 的核心是把「配置对了」和「真的能用」分开报告，四层各自独立判结论、**不做短路合并**：

```
L1  安装证据   客户端是否真实安装（app bundle / PATH 命令 / 配置路径存在性）
       ▲
L2  路径合规   Skills 路径是否指向统一仓库（符号链接解析比对）
       ▲
L3  配置正确   MCP 条目是否按客户端格式配置正确（JSON/TOML/JSONC/Reasonix TOML/cordis_yaml/远程 URL/stdio launcher）
       ▲
L4  端点活体   端点是否 initialize + tools/list 成功，且工具名覆盖所需能力组
```

四条判断各自独立呈报：**配置正确但探测失败、探测通过但能力缺失，都是合法且需要分别看到的状态**。配套的安全修复工作流（备份 → 原子写入 → 重解析验证 → 失败回滚）保证「修」这个动作本身不制造新事故。

### 4. 产品形态演进：四级交付阶梯

「形态」不是固定的一张脸，而是随开发阶段逐步升维的阶梯。每一级都可独立交付、独立使用，且下层引擎是上层形态的底座：

| 阶梯 | 形态 | 交付物 | 用户如何使用 | 对应阶段 |
|------|------|--------|--------------|----------|
| L0 | **审计/修复引擎** | core/ 18 模块 + config 注册表 | 被程序调用（Python 库/内部集成） | 现状（skills-mcp-unifier） |
| L1 | **通用 CLI** | scan.py 单命令 + 独立打包 | 终端一条命令完成审计/修复 | 阶段一 + 二 |
| L2 | **桌面 GUI（只读看板）** | Tauri 应用 | 打开即可看到红黄绿矩阵，只读 | 阶段三 |
| L3 | **按需加载与启停** | 增值开关层 | 控制哪些技能启用/按会话加载 | 阶段四（视需求） |

**形态设计的三个硬约束**（贯穿全部阶梯）：
1. **审计与修复分离**：写操作永远需要显式触发（`--fix-*`），GUI 只读。安全性是产品承诺，不随形态升级而放宽；
2. **单一 source of truth**：客户端注册表 + profile 配置是唯一事实源，CLI 与 GUI 共享同一引擎产出，结论必须一致；
3. **形态向上兼容**：每级形态复用下一级的引擎与数据契约，不做推翻式的二次实现（详见第 12 节数据契约）。

### 5. 形态总览图

```
                          ┌─────────────────────────────────────────────┐
                          │  L2  Tauri 桌面 GUI（只读审计看板·红黄绿矩阵）│
                          ├─────────────────────────────────────────────┤
                          │  L1  通用 CLI（scan.py --full / --fix-*）     │
                          ├─────────────────────────────────────────────┤
                          │  L0  审计/修复引擎 core/                      │
                          │   四层验证 ─ 多格式解析 ─ 安全修复 ─ 报告      │
                          ├─────────────────────────────────────────────┤
                          │  配置层 config.yaml + profiles/<name>/        │
                          │  （源事实：客户端注册表 + endpoint profile）   │
                          └─────────────────────────────────────────────┘
        L3 按需加载与启停 = L1/L2 之上的增值开关层（阶段四，视需求立项）
```

### 6. 产品边界

**做（核心）**：多客户端 Skill/MCP 合规审计；四层独立验证；多格式配置解析（7 客户端）；备份-原子写入-回滚的安全修复；endpoint 多 profile 治理；跨 profile 汇总报告。

**刻意不做**（避免定位漂移，详见《产品定位与路线图规划》第 6 节）：
- 不做技能市场/商店（agentregistry、gh skill 的领域）；
- 不做多用户权限体系与云端同步（企业级平台，部署成本不符）；
- 阶段三之前不做任何写入型 GUI 按钮；
- 阶段四是否进入按需加载领域，立项时单独决策——该领域与 skills-hub 类工具正面竞争，仅作增值。

---

## 第二部分 · 开发计划（全阶段）

### 7. 里程碑与时间轴总览

| 里程碑 | 阶段目标 | 预估工期 | 进入条件 | 关键收尾信号 |
|--------|----------|----------|----------|--------------|
| **M0** | 现状基线（skills-mcp-unifier） | 已完成 | — | 18 模块/5731 行引擎、10 文件/1897 行测试、主链路配置驱动 |
| **M1** | 阶段一：endpoint profile 抽象 | 1–2 周 | 总纲评审通过 | 主干零个人拓扑 + 多 profile，默认行为逐行 diff 为空 |
| **M2** | 阶段二：CLI 通用化 | 2–4 周 | M1 DoD 达成 | 首个对外发布 **v1.0**，陌生用户单命令装完即审计 |
| **M3** | 阶段三：Tauri 图形层 | 1–2 个月 | M2 DoD 达成 | GUI 与 CLI 同环境审计结论完全一致（GUI beta） |
| **M4** | 阶段四：按需加载与启停 | 视需求 | M3 上线后按反馈 | 立项时另定（增值功能） |

时间轴（乐观下限–稳健上限）：

```
M0 ── M1(1–2周) ── M2(2–4周) ────── M3(1–2月) ────── M4(TBD)
现状    阶段一        阶段二          阶段三            阶段四
        多profile     可发布CLI v1.0  只读GUI beta      按需加载
```

### 8. 阶段一：endpoint profile 抽象（缩编，详见详细设计文档）

**目标**：单一写死端点 → 可插拔 profile 体系；主干代码库零个人拓扑。

**六步改造**（每步独立验收 + 测试护航，全程保留 `scan.py --full` 前后逐行 diff 作为回归基线）：

| 步 | 内容 | 关键产出 | 验收要点 |
|----|------|----------|----------|
| 1 | config 结构迁移：`unified_mcp` → `profiles` + `active_profile` | profile schema v1 | 旧结构自动包装、迁移提示 |
| 2 | 引擎传参：`--profile` 注入 combined_checker / mcp_fixer | `profile_loader.py` | 默认取 active_profile，向后兼容 |
| 3 | 测试参数化：6 个含个人拓扑的测试文件提取为公共夹具 | `tests/fixtures.py` | 逻辑与断言不变 |
| 4 | 部署层剥离 + profile 专属模块改造：hermes_checker → legacy_checker | `core/legacy_checker.py` | 22 处专属逻辑声明化驱动 |
| 5 | 文档重写：SKILL.md 拆为产品文档 / profile 文档 | 拆分后双文档 | 人工评审 |
| 6 | 全量回归 + 报告逐行 diff | 回归基线报告 | 默认 profile 行为零变化 |

**核心变更点**（其余细节见详细设计文档）：
- 新增模块 `profile_loader.py`（读取/校验/兼容包装）与 `legacy_checker.py`（泛化自 hermes_checker + ai_memory_checker）；
- 状态模型 11 字段 → **7 固定字段 + N 动态能力组字段**（`capability_<组名>`）；
- 探测层参数化：`token_env`（鉴权来源）、`url_policy`（strict/relaxed）——解决 `probe_mcp` 强制 HTTPS+/mcp 结尾的硬约束；
- 新增 CLI：`--profile <name>` / `--list-profiles`；`--hermes`/`--ai-memory` 保留为别名。

**DoD（阶段一验收清单）**：
- [ ] 全部测试通过，无新增跳过项；
- [ ] 默认 profile 下 `scan.py --full` 输出与改造前逐行一致；
- [ ] 通用示例 profile 可独立审计，空 `required_capabilities` 只查 initialize + tools/list；
- [ ] 主干（不含 `profiles/`）全文检索无个人域名/内网网段/设备名命中；
- [ ] 旧结构 config 自动包装为单一 profile 并提示迁移；
- [ ] SKILL.md 完成产品/ profile 文档拆分。

### 9. 阶段二：CLI 通用化（展开）

**目标**：把「我的内部工具」变成「陌生人可用的产品」。三件事——能力组模板库、跨 profile 汇总报告、独立打包分发；并承接阶段一遗留的 stdio 探测与退出码议题。

#### 9.1 能力组模板库

把「能力组 = 一组工具名」从每个 profile 手写，升级为可复用、可组合的模板：

- [ ] 定义模板 schema：`name` + `capability_groups`（组名→工具名列表）+ 可选 `tags`（如 memory/ops/hermes）；
- [ ] profile 的 `required_capabilities` 支持 `extends: <模板名>` 或多个模板合并；模板缺失/循环引用在加载期 fail fast；
- [ ] 内置模板集（首版至少含通用 memory 套件、hermes 专用套件两个起手模板）+ 用户自定义模板目录（`~/.config/skill-mcp-studio/templates/`）；
- [ ] 与阶段一 profile schema v1 兼容：不引模板的 profile 继续手写能力组，行为不变。

**验收**：用一个内置模板即可描述 hermes-home profile 的完整能力需求，且通用模板可被 generic-http 直接复用。

#### 9.2 stdio 传输活体探测（承接 ADR-3）

阶段一探测仅 HTTP。阶段二补齐社团型/本地型 MCP 服务器（stdio）的活体探测：

- [ ] probe 层抽象 `transport` 接口，现有 HTTP 实现为接口之一，新增 stdio 实现（子进程 spawn + JSON-RPC over stdio，含会话生命周期管理）；
- [ ] `profile.transport` 支持 `stdio`，新增 `command` + `args` 声明字段；
- [ ] 探测仍只发 initialize / notifications/initialized / tools/list 三类只读请求，维持「无副作用」承诺。

**验收**：一个本地 stdio MCP 服务器可完成四层审计，且对 HTTP profile 行为零回归。

#### 9.3 跨 profile 汇总报告

- [ ] 新增 `--all-profiles`：一次扫描输出「profile × 客户端 ×（7 固定 + N 能力组）」矩阵；
- [ ] 报告层产出机读格式（CSV / JSON），作为 CI 断言与阶段三 GUI 的数据源；
- [ ] JSON 结构与阶段一第 12 节数据契约一致，GUI 无需二次解析。

**验收**：两个 profile（如 hermes-home + generic-http）一次命令出全矩阵，且单 profile 结论与 `--profile X` 单独运行完全一致。

#### 9.4 独立打包与分发

- [ ] 包名定案（skill-mcp-studio），打包为可 `pipx install` / `brew install` 单命令安装的分发物；
- [ ] **退出码约定**（承接详细设计第 9 节）：`0`=全绿，`1`=存在不合规项，`2`=配置/运行错误；排查并迁移现有「恒为 0」的脚本依赖；
- [ ] config 与 profile 资源外置（不以包内写死提供），`--config` / `--profile` 可指向任意路径；
- [ ] README 面向陌生用户重写：安装 → 最小配置（一个通用 profile）→ 首次完整审计的三步上手，并附 v1.0 能力边界说明。

**验收（阶段二 DoD，即路线图退出标准）**：**陌生用户凭 README 单命令安装，用通用模板完成一次完整审计**；`pipx` 环境无源码依赖完成扫描；退出码 CI 可消费。

#### 9.5 技术债清理（随阶段二收尾）

- [ ] 清理 ADR-1 遗留的 `profile=None` 旧分支回退（check_agents / fix_mcp_clients 的 `config.get("unified_mcp")` 回退路径）；
- [ ] 定案 hermes_checker 旧文件去留（开放问题 1，建议测试全绿后删除，git 历史即归档）；
- [x] 重跑一次 `--full` 刷新 3.3 节引用的审计快照数据（2026-09-04 完成：248 目录/60 路径口径刷新为 175 目录/10 路径，见路线图 §3.3；见待决问题 4）。

### 10. 阶段三：Tauri 图形层（展开）

**目标**：给「多客户端 + 多状态」的审计结论一个一眼可读的只读看板——客户端矩阵 × 状态字段，红黄绿三态。**写入操作仍仅 CLI**（硬边界）。

| 步子步骤 | 内容 | 验收要点 |
|----------|------|----------|
| 3.1 数据契约落地 | 以阶段一第 12 节 `check_agents` 返回结构为唯一契约，实现 CLI→GUI 的 JSON 序列化通道（子进程调用 scan / 共享引擎） | 契约字段只增不改名不删除 |
| 3.2 只读看板 | 客户端行 × 状态列矩阵；红（不合规/探测失败）黄（未探测/部分缺失）绿（通过）；能力组列按 `capabilities` 键动态生成 | 无任何写入口 |
| 3.3 一致性验证 | GUI 与 CLI 对同一环境分别跑审计并逐项 diff | **结论完全一致**（DoD 硬要求） |
| 3.4 打包分发 | Tauri 构建（macOS 首发，后续评估 Win/Linux）；内置「选择 profile」「刷新审计」只读操作 | 脱离项目源码独立安装运行（运行时依赖已安装的 CLI；缺失时 GUI 横幅给出 `pipx install skill-mcp-studio` 引导，2026-09-05 评审整改 GUI-3 落地） |

**DoD**：GUI 与 CLI 对同一环境产出完全一致的审计结论；写操作仍仅 CLI（阶段三不做任何写入型按钮）。

### 11. 阶段四：按需加载与启停（视需求，展开为立项维度）

此阶段进入 skills-hub 类工具的竞争领域，属**增值功能**，不纳入主线承诺。立项时用以下维度单独评估，而非沿用主线 DoD：

- [ ] 需求确认：统计阶段三上线后「技能冲突/加载过重」类反馈的真实占比，达到阈值再立项；
- [ ] 形态选择：技能级启用/禁用 + 按会话加载，先做只读开关（生成/移除符号链接或快捷方式）再考虑运行时会话注入；
- [ ] 安全对齐：任何启停操作复用现有「备份-原子-回滚」工作流，不引入新的写路径；
- [ ] 边界复核：确认不滑入技能商店/同步分发（那是刻意不做的领域）。

**DoD**：立项时另定；不满足立项门槛则维持 M3 形态，不强行推进。

### 12. 发布与版本策略

| 版本 | 触发点 | 形态 | 发布面 |
|------|--------|------|--------|
| v0.x | M1 完成 | 引擎 + CLI（多 profile） | 内部/私有仓库 |
| **v1.0** | M2 完成 | 通用 CLI（可独立安装） | **首次公开**（GitHub/PyPI/homebrew） |
| v1.x beta | M3 完成 | + Tauri 只读 GUI | 公开 beta |
| v2.0 | M4 立项通过 | + 按需加载 | 视需求 |

发布前硬性检查（每版）：测试全绿 · 主链路无个人拓扑 · README 陌生人三步可跑通 · 审计数据已刷新（非 2026-07-13 旧快照）。

---

## 第三部分 · 执行保障

### 13. 质量门禁（每个阶段共用的收尾闸口）

1. **测试全绿**：无新增跳过项；新增功能配新增测试（阶段一按详细设计第 10 节，之后按各阶段验收清单）；
2. **行为回归**：写操作类改造必须做「改造前后逐行 diff」或「GUI vs CLI 一致性 diff」；
3. **零个人拓扑**：任意阶段收尾均需 `grep` 复核主干无个人域名/网段/设备名（`profiles/` 除外）；
4. **文档同步**：SKILL.md、README、三份规划文档中受影响部分随代码同 PR 更新；
5. **git 收尾**：沿用本工具自证的安全工作流——每步提交独立、保留基线、可回滚。

### 14. 风险登记册

| 风险 | 影响 | 阶段 | 对策（沿用路线图第 6 节） |
|------|------|------|------|
| 竞品跟进验证能力 | 差异化窗口收窄 | 全阶段 | 把四层验证 + 安全修复做深，长期工程门槛 |
| 生态碎片化（配置格式持续演变） | 注册表维护成本 | 二、三 | 注册表机制已把新增客户端降到「加一段声明」 |
| 项目存续风险（领域活跃度低） | 无人维护 | 一、发布前 | 阶段一起公开仓库/文档/测试，把可信度做成竞争力 |
| 核心链路回归 | 改造崩掉主力功能 | 一 | 每步独立验收 + 逐行 diff + 测试护航 |
| stdio 探测复杂度超预期 | 阶段二延期 | 二 | 2.2 独立成步，若受阻可降级为「stdio 只做安装/配置/路径三层、活体仅 HTTP」，不阻塞打包分发 |

### 15. 待决问题（阶段一完成前需定案，转自路线图第 7 节）

> **已定案**（2026-09-01）：以下 5 项已由《决策记录-待决问题定案.md》定案（D1–D6、D9），见该文档。概述：

1. **命名与仓库** →【已决 D1/D2/D3】名称 `skill-mcp-studio`，许可证 **MIT**，Codeup 开发 → M1 后公开 GitHub + PyPI/homebrew；
2. **profile 分发形态** →【已决 D4】独立仓库 + 声明式引用（`profile_sources`），主仓库仅留 generic 示例；
3. **profile schema 版本化** →【已决 D5】引入 `schema_version: 1`；
4. **实测数据刷新** →【行动项 D6】发布前重跑 `--full`；
5. **退出码历史依赖** →【行动项 D9】已并入阶段二 §7.2，阶段二开工第一步 grep 排查。

---

## 附录 A · 文档关系

```
《产品形态与开发计划.md》（本文·总纲）
   ├── 产品形态：定位/用户/场景/形态演进/边界
   └── 开发计划：全阶段里程碑 + 阶段二~四展开
        │
        ├─▶ 《产品定位与路线图规划.md》v1.1
        │     差异化/资产盘点/竞品调研/阶段一六步方案/非目标与风险/开放问题
        │
        └─▶ 《详细设计文档-阶段一-endpoint-profile-抽象.md》v1.0
              阶段一实现细节：profile schema/模块改造/流程/测试/目录/ADR/数据契约
```

## 附录 B · 术语速查

- **profile**：对一个统一 MCP 端点的完整声明（名称/URL/传输/能力组/遗留命名/识别规则）；
- **四问模型**：安装证据 → 路径合规 → 配置正确 → 端点活体，四层独立验证；
- **能力组**：一组必须同时存在的工具名，用于判定端点能力覆盖；
- **legacy 条目**：客户端配置中指向旧端点/旧通道的 MCP 条目；
- **活体探测**：initialize + notifications/initialized + tools/list 三步 JSON-RPC 调用；
- **source of truth**：config.yaml 的客户端注册表（mcp_tools / tools / auto_discover 段）。

---

## 附录 C · 文档体系与里程碑架构图

正式架构图见独立文件 [`skill-mcp-studio-architecture.html`](skill-mcp-studio-architecture.html)（暗色主题、内联 SVG，浏览器打开即可，离线可用；同目录另有 `skill-mcp-studio-architecture.png` 导出位图供贴入 Word/PPT）。

图由两条主线构成，映射关系如下：

| 主线 | 内容 | 配色 |
|------|------|------|
| ① 文档体系 | README（入口）→ 总纲（本文件）→ 定位/路线图 + 阶段一详细设计（两份支撑） | 青=总纲 · 绿=定位 · 紫=阶段一设计 · 灰=README |
| ② 里程碑路线图 | M0 现状基线 → M1 profile 抽象 → M2 CLI 通用化 → M3 Tauri GUI → M4 按需加载 | 琥珀=里程碑 · 橙=交付形态 L0–L3 |

里程碑与「主导文档」的对应关系（图中以彩色圆点标注）：

- **M0** ← README / 现状；
- **M1** ← 《阶段一详细设计》+《产品定位与路线图规划》；
- **M2 / M3 / M4** ← 本文件 §9 / §10 / §11。