# v0.23.0 评审：整改与优化清单

| 项 | 值 |
|---|---|
| 评审基线 | `v0.23.0`（build 109，tag `v0.23.0` → `f5cd028`） |
| 评审分支 | `develop`（与 `origin/develop` 一致） |
| 评审方式 | 以实际执行过的操作与仓库现状为依据，逐条附证据 |
| 用途 | 下一版本（0.24.x）的需求池 |
| 更新 | 第二轮补充：1 号问题定案为方案 B；新增 5 条（P0-6、P1-10、P1-11、P2-16、并强化 2 条） |
| 实施进度 | 第一批（`a349c59`）：P0-1 / P0-2 / P0-3 / P0-6 / P1-9；第二批：P1-7 / P2-14，并新增 P2-17（见文末）。其余待下一批 |

> 分级：`P0` 会造成实际损害或已实际发生；`P1` 明确应改、影响可控；`P2` 体验与效率优化。
> 文末附「v0.23.0 已修」，避免重复立项。

---

## P0 — 需要整改

### P0-1. Release 正文被三个平台重复追加三次 —— ✅ 已完成（方案 B，`a349c59`）

**现象**：`v0.23.0` 与 `v0.22.0` 的 GitHub Release 正文都是三条完全相同的内容。

**证据**
- `v0.23.0` Release 正文实测为三行重复的
  `**Full Changelog**: .../compare/v0.22.0...v0.23.0`（`v0.22.0` 同样三行）。
- `build-macos.yml:152-158`、`build-linux.yml:94-101`、`build-windows.yml:120-126`
  **各自**调用 `softprops/action-gh-release@v3`，且都带 `generate_release_notes: true`。
- workflow 注释自述「action-gh-release 对已存在 release 做增量追加」——
  行为符合注释，但结果是无信息量的重复正文。

**已选定方案：B（独立 job 统管 Release 创建与正文）**

实施要点：
1. 三个平台 workflow 的 `generate_release_notes` 一律改为 `false`，
   它们只负责**上传资产**（`action-gh-release` 会在 release 不存在时自动创建空壳）。
2. 新增 `.github/workflows/release-notes.yml`，触发 `push: tags: ['v*']`，
   在三个 build 完成后（用 `workflow_run` 触发、或轮询等待资产就绪）执行：
   - 从 `CHANGELOG.md` 抽取当前 tag 对应段落（`## [vX.Y.Z]` 到下一个 `## [` 之间）；
   - `gh release edit "$TAG" --notes-file <抽出内容>`；若 release 尚不存在则 `gh release create`。
3. **竞态处理**：`gh release edit` 要求 release 已存在。
   首选 `workflow_run`（在三个 build workflow 全部 completed 后触发）避免轮询；
   若坚持同 tag 并发，则需重试若干次。

**验收标准**：新打一个 tag，Release 正文**只出现一份** changelog（内容来自 CHANGELOG），
且三平台共 8 个资产齐全。

---

### P0-2. 没有 tag 与 `version.json` 的一致性校验 —— ✅ 已完成（`a349c59`）

**现象**：打 tag 时若忘记先 bump 版本，CI 不会拦住，
会产出「tag 名与包内版本号不一致」的发布。

**证据**
- `grep -rn "version.json" .github/workflows/*.yml scripts/*.sh scripts/*.py`
  除 `bump_version.py` 自身外**无命中** → CI 从未读取过 `version.json`。
- 对照：`pyproject.toml:10` 有 `requires-python`，
  说明项目有「声明 + 校验」意识，但发布链路上缺了这一环。

**影响**：错误产物会被签名、上传、分发；tag 一旦推送很难干净撤回。

**建议动作**：在 `test.yml` 增加一步（tag 触发时生效）：

```
- name: tag 与 version.json 一致性
  if: startsWith(github.ref, 'refs/tags/v')
  run: |
    v=$(python -c "import json;print(json.load(open('version.json'))['version'])")
    [ "v$v" = "${GITHUB_REF_NAME}" ] || { echo "tag ${GITHUB_REF_NAME} != v$v"; exit 1; }
```

**验收标准**：故意打一个 `v9.9.9` tag，Test workflow 失败并指出不一致。

---

### P0-3. 缺少运行时 Python 版本守卫 —— ✅ 已完成（`a349c59`，`scan.py`）

**现象**：用 Python 3.9 运行会以**与代码无关**的方式失败，误导排查方向。

**证据**
- 同一份代码实测：
  - `/usr/bin/python3`（3.9.6）→ `Ran 933 tests ... FAILED (failures=1, errors=1, skipped=2)`
  - `python@3.11`（3.11.16）→ `OK (skipped=2)`
- `pyproject.toml:10` 已声明 `requires-python = ">=3.11"`，
  但 `grep -rn "version_info" core/ scan.py` **无命中** → 运行时没有检查。
- 该声明只对 pip 安装生效；源码直跑（`python3 scan.py`）毫无保护。

**影响**：本会话真实发生，并一度导致误判根因。

**建议动作**：在 `scan.py` 与 `scripts/` 入口各加早退检查，
版本不符时打印可操作提示（当前版本、要求版本、建议命令）后 `exit 2`。

**验收标准**：`/usr/bin/python3 scan.py --help` 输出明确版本提示而非回溯。

---

### P0-4. 三处模块级 `Path.home()` 常量 —— 「改了要重启」类问题的共同根源 —— ✅ 已完成（抽出 `core/paths.py`）

**现象**：技能库根路径在 `import` 时被求值成模块常量，运行期无法改变。

**证据**
- `core/skill_market.py:120` `_AGENTS_DIR = Path.home() / ".agents"`
- `core/skill_market.py:122` `_SKILLS_DIR = Path.home() / ".skills-manager" / "skills"`
- `core/skill_merge_advisor.py:21`、`core/skill_market_ops.py:26` 同一模式。

**影响**：这是**同一类缺陷**的根源，且本项目已因此出过 bug：
- 「技能市场源只在首次打开时探测，环境变化后无法刷新」属于这一族；
- `v0.23.0` 修的测试问题（用例读死开发机技能库）是同一根因的另一表现。

**建议动作**：改为访问器函数（`def skills_dir() -> Path`），
或抽出 `core/paths.py` 提供可注入 / 可重载的路径解析；
确需缓存时用显式 `lru_cache` 并提供 `clear()`。

**验收标准**：存在测试能在**不重启进程**的前提下把技能库根切到临时目录。

**落地（已完成）**：新增 `core/paths.py`，提供 `home_root()` / `agents_dir()` / `skills_dir()` / `default_lock_path()` 四个访问器，**每次调用重新解析、不做模块级缓存**（缓存正是本项要治的病）；优先级为「显式注入 > 环境变量 `SKILL_MCP_STUDIO_HOME` > 真实 `Path.home()`」，并提供 `override()` 上下文管理器供测试临时切换、退出即恢复。

- **消费点全部改造**：`skill_market.list_installed`（两处默认值）、`skill_market_ops._skills_dir`、`skill_merge_advisor.load_metas` 均改为跟随访问器；`core/` 与 `scan.py` 已无任何 `Path.home()` 模块级常量。
- **验收用例**（`tests/test_paths.py`，8 个）：核心两条是 `test_override_switches_root_without_restart`（同进程切根并恢复）与 `test_list_installed_default_follows_root`（**不显式传路径**时默认值也落到注入的根 —— 这一条在改造前不可能通过）。
- **判据有效性已反向验证**：把 `paths.skills_dir()` 退回「import 期定死」语义，立刻 4 个用例失败（`PosixPath('/Users/.../.skills-manager/skills') != PosixPath('/var/folders/...')`）；恢复后全绿。
- **验收**：相关用例 88 个通过（`test_paths` + `test_skill_market` + `test_skill_market_ops` + `test_skill_market_cli` + `test_merge_exec`）。

---

### P0-5. `test_stdio_probe` 有一次未定位的偶发失败 —— ✅ 已定位并修复（stderr 管道从未排空）

**现象**：全量跑时该用例偶发报 `stdio handshake timed out`，无法复现。

**证据**
- 一次全量跑中出现：
  `FAIL: test_handshake_and_tool_names ... AssertionError: False is not true : stdio handshake timed out`
  （当时 4 次全量跑中 1 次）。
- 此后复跑 **18 轮**（3 轮全量 + 15 轮专项）**再未复现**。
- 正常握手耗时 0.034–0.057s，预算 5.0s（约 100× 余量）；
  10 路 CPU 满载下该文件仍 0.102s 通过 → 不是纯负载问题。
- **注意**：它不是 CI 失败的原因（真实 CI 日志显示失败在 `test_skill_market_cli`）。

**建议动作**：不急着调超时，先让它**可诊断**：
- 失败时输出子进程实际耗时、stderr 内容、握手在哪一步超时；
- 给 `probe_stdio` 的 stderr 增加「超时时回读已缓冲内容」以便取证。

**验收标准**：下次偶发时，日志足以判定是调度停摆、管道阻塞还是子进程未启动。

**根因（已定位并修复）**：`probe_stdio` 用 `stderr=subprocess.PIPE` 启动子进程，**但整个握手期间没有任何代码读 stderr**（只在 `finally` 里 close）。子进程一旦向 stderr 写入超过管道缓冲（POSIX 通常 64 KiB）就阻塞在 `write` 上，从此不再读 stdin、不回握手 —— 外部只能看到一句 `stdio handshake timed out`。触发与否取决于子进程在握手期间往 stderr 写了多少，这正是它「偶发、复跑不复现」的原因。

- **复现**：子进程先写 200 KiB 无换行 stderr、再正常握手 → 修复前 `elapsed=8.01s`（超时、`initialize_ok=False`）；修复后 `elapsed=0.07s`，握手与 `tools/list` 全部成功。
- **修复**：新增 `_StderrDrain` 守护线程，启动即按块（`read(4096)`）排空 stderr，只保留末尾 2000 字符；`Popen` 增加 `errors="replace"`，避免解码失败把排空线程打退（那会让管道重新被填满）；失败时把 stderr 尾部附到 `result["error"]`，超时不再是一句裸信息。
- **回归用例**（`tests/test_stdio_probe.py`）：`test_noisy_stderr_does_not_block_handshake`（管道阻塞不再发生）+ `test_stderr_tail_reported_when_handshake_fails`（失败可归因）。
- **判据有效性已反向验证**：把排空整个去掉，第一个用例稳定失败（5.0s 超时）；恢复后通过。即该用例确实守住了本缺陷，不是摆设。
- **一处被实测证伪的说法**：先前记录认为「`readline()` 排空会死锁、必须按块读」—— 实测 `TextIOWrapper.readline()` 会持续把数据读进自己的内部缓冲，**同样能排空管道、并不死锁**。选按块读的真实理由是有界内存（`readline()` 对不含换行的 stderr 会无界增长），且 `tail()` 在它返回前一直取不到内容 —— 而这恰恰是最需要取证的输入形态。代码注释已按实测改写。
- **验收**：`scripts/ci_parity.sh` → `Ran 935 tests ... OK (skipped=2)`（原 933 + 新增 2）。

---

### P0-6. 「禁止未经允许推 GitHub」的门禁 hook **不随仓库生效** —— ✅ 已完成（`a349c59`）

**现象**：门禁只存在于本机 `.git/hooks/pre-push`，**不在版本控制里**，换机器或重新 clone 后自动消失。

**证据**
- `git config --get core.hooksPath` → **未设置**（输出为空）。
- `.git/hooks/pre-push` 与 `scripts/git-hooks/pre-push` 内容一致，说明是**手工拷贝**进 `.git/hooks/` 的。
- hook 自身注释即承认：「本 hook 安装在 `.git/hooks/pre-push`，**不随仓库提交**」。
- 全仓 grep 未见任何安装脚本或 `core.hooksPath` 说明（`CONTRIBUTING.md`、`README.md`、`scripts/*.sh` 均无）。
- 该 hook 是**唯一**拦截 GitHub 推送的机制（拦截文案即「按当前约定，只有在你明确要求时才允许提交 GitHub」）。

**影响**：这是一条**安全/流程约定的单点失效**。
任何一次新克隆、换机、或 `.git` 目录重建，门禁都会静默消失，
而使用者不会收到任何提示——直到某次误推发生。

**建议动作**（二选一，前者更好）：
- **A**：把 hook 目录纳入仓库并由 git 自身加载：
  `git config core.hooksPath scripts/git-hooks`
  （该配置可写入一个 `scripts/install-hooks.sh`，并在 `CONTRIBUTING.md` 说明；
  注意 `core.hooksPath` 本身在 `.git/config` 里，仍需一次安装——但只需一条命令，且可脚本化。）
- **B**：在 CI 侧补一道防线：`test.yml` 中检查「非显式放行时不得出现推送到 github remote 的 workflow 步骤」
  （治本程度弱，因为 CI 拦不住本地误推，但可作为兜底）。

**验收标准**：新克隆仓库后，按文档执行一条命令即可让门禁生效；
或存在 CI 兜底检查且有对应测试。

---

## P1 — 建议整改

### P1-7. `ci_parity.sh` 的 skipped 基线硬编码 —— ✅ 已改（基线可用环境变量覆盖）

**证据**
- `scripts/ci_parity.sh:23` `CI_BASELINE_SKIPPED=2`，第 104/109 行据此判定「与 CI 基线一致」。
- `grep -rln "ci_parity" tests/` **无命中** → 该脚本没有回归保护。

**影响**：一旦 CI 侧新增或移除可选依赖导致 skip 数变化，
脚本会对**正确的**结果报「≠ CI 基线」，退化为噪音源；它自身坏了也无人察觉。

**建议动作**：基线允许环境变量覆盖（如 `CI_BASELINE_SKIPPED`）或从 CI 配置派生；
补一个最小 smoke 测试（能解析汇总行即可）。

---

### P1-8. 文档中残留 pytest 指令与过时的解释器路径建议 —— ✅ 已完成（统一为 unittest / `ci_parity.sh`）

**证据**
- `docs/product/PRD-产品需求文档.md:229`：`python3 -m pytest tests/ -q` 全绿。
- `docs/superpowers/plans/2026-09-20-mcp-entry-removal.md` 另有 10+ 处 `python3 -m pytest ...`。
- 其中 `:39` 的建议不适用于本机：「必须用 `python3`（`/usr/local/bin/python3`），
  `/usr/bin/python3` 缺 PyYAML」——本机为 Apple Silicon，Homebrew 前缀是 `/opt/homebrew`。
- 对照 `CONTRIBUTING.md` 明确写「无需额外安装 pytest」——两份文档互相矛盾
  （矛盾的根源见 P1-10）。

**建议动作**：统一为 CI 的命令或 `scripts/ci_parity.sh`；
解释器路径建议改为「交给 `ci_parity.sh` 自动选择」，不写死绝对路径。

---

### P1-9. `CONTRIBUTING.md` 末尾缺少换行 —— ✅ 已完成（`a349c59`）

**证据**：发布流程中
`write_text.py --path CONTRIBUTING.md --verify` 明确报「不合规 … 末尾缺少换行」。

**影响**：轻微。但会让编码校验持续报警，长期会让人忽略该信号。

**建议动作**：补一个末尾换行；并将该 `--verify` 纳入 CI（见 P2-14）。

---

### P1-10. `pyproject.toml` 宣告了测试依赖组与覆盖率配置，但 CI **完全不用**（新增） —— ✅ 已完成（采方案 B：全项目统一 unittest）

**现象**：项目在打包元数据里声明了 pytest / coverage 及其配置，
但 CI 既不安装它们，也从不运行覆盖率——声明与实际行为脱节。

**证据**
- `pyproject.toml:19-22` 有 `[project.optional-dependencies] test = ["pytest>=8.0", "coverage>=7.0"]`。
- 同文件 `:27-44` 有完整的 `[tool.pytest.ini_options]`（`testpaths`/`python_files`/`addopts`）
  与 `[tool.coverage.run]`（`source = ["core", "scan"]`、`branch = true`）、
  `[tool.coverage.report]`（`exclude_lines`）。
- 但 `.github/workflows/test.yml:32` 实际只装 `PyYAML certifi`；
  `grep -c coverage .github/workflows/*.yml` 在**四个** workflow 里计数均为 **0**。
- 这与 P1-8 的文档矛盾同源：配置层设想用 pytest + 覆盖率，执行层用的是 unittest 且无覆盖率。

**影响**：维护者可能以为在跑覆盖率，实际从未产生过覆盖率数据；
也让「该装什么」在不同文档间说法不一。

**建议动作**（二选一，需明确）：
- **A**：让声明落地 —— CI 改用 `pip install -e '.[test]'`，
  并以 `coverage run -m pytest` + `coverage report` 产出报告（可选是否设门禁阈值）。
- **B**：删掉 `[test]` 组与 pytest/coverage 配置，全项目统一 unittest，
  并在 `pyproject.toml` 注释说明「测试用标准库 unittest，不引入 pytest」。

**验收标准**：`README`/`CONTRIBUTING`/`pyproject` 三者对「怎么跑测试」说法一致。

---

### P1-11. 无任何 Python 静态检查 / 格式化配置（新增） —— 🟡 可审计终态（配置 + CI job + F401 已启用；I001 / format 经量化论证不采用，待确认）

**现象**：51 个领域模块 + 74 个测试文件（约 14000 行测试代码），
没有任何 lint、格式化或 import 排序工具的配置。

**证据**
- 以下文件**均不存在**：`.ruff.toml`、`ruff.toml`、`.pre-commit-config.yaml`、
  `.editorconfig`、`setup.cfg`、`tox.ini`。
- `pyproject.toml` 中 grep `ruff|black|isort` 无命中。
- 全仓 grep `pre-commit` 无命中（`pyproject.toml` 与所有 workflow 均未引用）。

**影响**：代码风格与 import 顺序完全依赖人工自觉；
新增贡献者无从得知约定；也无法在 CI 上自动拦住低级问题。

**建议动作**：引入 **ruff**（单工具即可覆盖 lint + format + import 排序），
配置写入 `pyproject.toml`，并新增一个 CI job；
首次启用建议先 `ruff check --fix` 全量清理一次，再设为门禁。

**调查记录（未完成，但已定位具体障碍 —— 下次请勿直接 `--fix`）**

在 `pyproject.toml` 写入 `[tool.ruff]`（`line-length = 100`、`target-version = "py311"`、
显式 `select`、`tests/*` 豁免 E402）后实测：

- **必须显式写 `select`**：不写则跟随 ruff 版本默认集。本机 ruff 0.16 未配 `select` 时报
  **1538** 条，且含 `UP006`(754)、`UP045`(129)、`BLE001`(78)、`PLW1510`(35) 等**非默认族**
  —— 不写死就会「本地绿、CI 红」或随版本漂移。
- 按 `E4/E7/E9/F/I/W` 统计共 **161** 条：`I001` 71、`F401` 34、`E402` 28、`F841` 13、
  `F541` 10、`F811` 2、`E741` 1、`W293` 1、`E401` 1。
- **`ruff check --fix` 直接跑会打断既有架构**：实测两轮**都造成 5 个用例失败**
  （`test_check_model_attach` ×2、`test_management_snapshot` ×2、`test_registry` ×1）：
  - 第 1 轮（含 `F401`）：自动修复 120 处 → 5 失败
  - 第 2 轮（`--ignore F401`）：自动修复 87 处 → **仍 5 失败**
  即破坏源不止 F401，**`I001` 重排 import 也在其中**。根因与本项目架构强相关：
  大量模块依赖「先注入 `sys.path` 再扁平导入」，并存在跨模块**再导出**
  （A `from X import n`，B 再从 A 取 `n`）。自动重排/删除会静默断掉这两条链路，
  而失败点离改动点很远（改 import，挂在 registry/工具发现类用例上）。
- 两轮改动均已 `git checkout` 回退，回退后 `Ran 943 tests, OK (skipped=2)`。

**结论**：本项不能走「先全量 `--fix`、再设为门禁」的常规路径，应作为独立批次按下列顺序做：

1. 给真正的再导出加显式别名（`from X import n as n`）或 `__all__`，让 `F401` 不再误判；
2. 把 `E402` 的 `per-file-ignores` 从 `tests/*` 扩到 `core/`、`scripts/` 里同样做引导的模块；
3. 关闭 `I001` 的自动重排（或对引导点所在文件豁免），**逐文件**确认 import 顺序无害后再启用；
4. 剩余的 `F841`(13)、`E741`(1) 人工过一遍，再用 `--unsafe-fixes` 会改变语义，需逐个看；
5. 最后才加 CI job，并确保首次即绿。

---

**P1-11 进展记录（分三批，均全绿）**

第一批 `ca533e6`：`pyproject.toml` 新增 `[tool.ruff]`，并把 190 处告警清到 0。

```toml
[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
ignore = ["F401", "E402"]        # 会改变代码行为，先关掉并写明原因
[tool.ruff.lint.per-file-ignores]
"tests/*" = ["F841"]
```

**为什么分阶段开而不是一次全开**（本项的核心判断）：`F401` 在本项目大量是**有意的
再导出**（扁平 `core/` 布局 + `patch("mod.name")` 依赖），实测 `--fix` 会删掉它们并
破坏 5 个用例；`I001`/`E402` 则会打乱「先 `sys.path` 引导、再 import」的顺序。
**开一个常年红灯的门禁，不如开一个守得住的绿灯门禁。**

本批修掉 20 处，其中 `F811` 两处**性质相反、必须逐处判断**：

| 位置 | 性质 | 处置 |
|---|---|---|
| `core/config_store.py:88` | 与顶层 L19 重复的 `import re` | 真冗余 → 删除 |
| `core/tool_registry.py:402` | **刻意的惰性导入**（避免顶层互 import 成环 + 保证 `patch("config_store.load_discovered")` 生效） | 不可删 → 加 `# noqa: F811` 并注明原因 |

其余：13 处自动修（`F541` 10 / `E401` 1 / `F841` 2）；6 处死赋值逐个确认右侧无副作用后
删除（含 `core/skill_toggle.py:153-158` —— 整块 `try/except` 只为给一个从不使用的变量赋值）；
`E741` 把测试里的 `l` 改为 `ln`。

第二批 `0acb7d3`：`.github/workflows/test.yml` 新增 `lint-ruff` job，
`pip install "ruff==0.16.9"`（**固定版本**：诊断集合随版本变化，不锁版本会让门禁时红时绿），
执行 `python -m ruff check --output-format=github core tests scan.py scripts`。

**验证方式（不只是看 YAML 表面）**：PyYAML 解析确认 jobs 列表含 `lint-ruff`，
再**把该 step 的 `run` 命令抽出来在本地实跑** → `exit=0`、`All checks passed!`。

**尚未完成（如实记录）**：`F401`（54 处，需先补 `import X as X` / `__all__`）、
`I001`（78 处，需先处理 `sys.path` 引导）、`ruff format`（清单原文要求 lint+format，
目前只落了 lint）。**因此本项记为「部分完成」，不计为已完成。**

**P1-11 第三批：F401 已清理并启用（`55737f3`）**

`[tool.ruff]` 的 ignore 从 `["F401", "E402"]` 收窄为 `["E402"]` —— **F401 已启用**。

53 处清理（25 个文件，+13/-43）：绝大多数是 `from typing import ...` 的多余名字；
5 处单名导入整行删除；`mcp_fixer_render.py` 一处占 14 个（拆分时把整个 import 块复制了过来，
其中 `file_atomic`/`mcp_checker`/`ops_log`/`profile_loader`/`tool_registry` 等**写侧依赖
在纯渲染模块里本就不需要**）。

**风险控制的关键结论**：先反查「被外部引用」的名字 —— `patch("mod.name")` 字符串锚点与
跨模块 `mod.name` 访问是 ruff 看不到的（P2-16 侦察时证实本项目有 9 处这类锚点）。
实测结论：**53 处无一被外部引用**。原因是两件事正好互补 ——
P2-16 拆分时已把再导出写成 `import X as X`，而**带冗余别名的导入本就不会被 F401 标记**。
所以 F401 标记出来的必然不是再导出，"删"这个动作因此是安全的。

**过程中的一处教训**：ruff JSON 的 `message` 用**完全限定名**（`typing.List`），
按基名（`List`）比对会导致带模块前缀的全部漏配 —— 第一版脚本因此只清掉 38 处，
需修正为 `name.split(".")[-1]`。同时把脚本**写入文件再执行**（而非塞进 heredoc），
否则错误信息会被淹没 —— 前两轮正是因此空转。

---

**P1-11 第四批：`I001` 的量化评估（本轮）—— 结论：建议保持豁免**

实测 `I001` 共 **76 处、63 个文件**，按是否含 `sys.path` 引导分类：

| 类别 | 文件数 | 处数 |
|---|---|---|
| **含 `sys.path` 引导**（重排有风险） | **34** | **45** |
| 无引导（安全） | 29 | 31 |

**45/76 落在风险侧，且几乎全是 `tests/*`（33 个测试文件）+ `scan.py`（4 处）。**

原因是结构性的，不是「少数特例」：测试文件的标准骨架就是

```python
import sys, pathlib
sys.path.insert(0, str(ROOT / "core"))   # ← 引导必须在前
import config_store                       # ← 只能在引导之后
```

`I001` 会把 `import config_store` 排到引导**之前** → 在 `sys.path` 未设置时执行 →
`ModuleNotFoundError`。这解释了此前实测 `--fix` 破坏 5 个用例的现象。

**因此建议 `I001` 记为「不采用（附理由）」而非「待办」**：若只为 31 处安全侧开启、
再为 45 处加 per-file 豁免，配置复杂度远超收益；而本项目已有更强的约束在起作用
（`E402` 豁免 + 测试骨架本身就是统一写法）。**判定不做的理由，也是这项工作的产出。**
此项**待确认**（见文末待决清单）。

**顺带产出**：`F821` 在本轮抓出一个真实缺陷 —— 见 `d983427`：
`core/workspace_cleaner.py` 拆分时丢失 `import shutil`，三处 `shutil.rmtree`
（目录型临时目标的删除路径）抛 `NameError` 且被 `except` 吞掉，**943 个用例全绿却无人发现**。
这说明 `F821` 一类「正确性规则」有独立于风格规则的价值。

**P1-11 第五批：`ruff format` 评估 —— 结论：建议不采用**

实测：**114 / 135 个文件需要重排**（21 个已合规）；抽样 `tests/test_paths.py` 14 行、
`core/config_store.py` **243 行**。改动性质：**只动换行/折行/缩进，不改 import 顺序** ——

```diff
-        self.assertEqual(
-            paths.skills_dir(), Path.home() / ".skills-manager" / "skills"
-        )
+        self.assertEqual(paths.skills_dir(), Path.home() / ".skills-manager" / "skills")
```

**因此它与 `I001` 的风险面完全不同**：`I001` 是「技术上不可行」（会打乱 `sys.path` 引导），
`format` 是「收益不匹配成本」：

1. **会淹没 git 历史** —— 本仓库刚完成 P2-16 模块拆分、`F401` 清理等**有语义**的改动，
   紧接着一次全局重排会让 `git blame` 与回溯明显变难；
2. **与清单其余条目的「定点整改」性质冲突** —— 那些都是修一处具体问题，这是全库重排；
3. **已有更强的门禁在守实质正确性** —— 943 个测试 + ruff lint + 编码校验 + 脚本卫生自检。

**故建议 `ruff format` 不采用**（理由与 `I001` 不同，见上）。二者均**待确认**。

---

## 附二：待确认事项（4 项，均为「判定不做 + 写明理由」型）

以下 4 项**不因继续编写代码而消解**，需要明确决策。每项均已给出量化依据 ——
在整改清单中，**「经论证不采用」与「已完成」同样是有价值的结论**。

| # | 事项 | 建议 | 量化依据 |
|---|---|---|---|
| 1 | **P2-16 `config_store` 拆或不拆** | **B：判定不拆** | 3 个猴补丁锚点（`_overlay_target`/`_local_overrides_file`/`load_discovered`，含 9 处测试 `patch("config_store.load_discovered")`）+ 加载期环 + 20+ 处 import；而**可搬部分仅约 61 行**，搬完约 598 行、离 600 只剩 2 行余量。收益薄、约束密 |
| 2 | **P2-12 验收口径** | 静态改动待 CI 验证 | 本机**无 `cargo`/`rustc`**（PATH、`~/.cargo/bin`、Homebrew、`/usr/local` 均无），「改完刷新即可见」无法本地实测 |
| 3 | **`I001` 采用与否** | **不采用** | 76 处 / 63 文件，其中 **45 处（59%）落在含 `sys.path` 引导的文件**（33 个测试文件 + `scan.py`），**结构上不可行** |
| 4 | **`ruff format` 采用与否** | **不采用** | 114/135 文件需重排、单文件可达 243 行；纯排版噪声 vs 定点整改的仓库习惯，**收益不匹配** |

**P1-11 的终态（可审计）**：已完成 = `pyproject [tool.ruff]` 配置 + CI job
（`ruff==0.16.9`）+ `F401` 清理 53 处并启用 + 首批 20 处修复；
经论证不采用 = `I001`、`ruff format`（各附量化理由）。

---

## P2 — 可以优化


---

### P2-12 现状侦察（本轮实测）—— 待决策，不宜先行编码

**痛点成因已定位**：

| 事实 | 含义 |
|---|---|
| `src-tauri/tauri.conf.json` → `build.frontendDist = "../gui"` | `dashboard.html`（363 KB）在**构建时嵌入二进制** |
| `app.windows[0].url = "dashboard.html"` | 窗口加载的是**嵌入资源**，而非磁盘文件 |
| `src-tauri/src/lib.rs:442 pub fn run()` | **无 dev/prod 分支**，配置里也没有 `devUrl` |

这三条共同导致「改一行 HTML → 必须 `cargo build` + 重装」——即本项要消除的痛点。

**Tauri v2 下的两个候选方案**：

- **方案 A：`build.devUrl` + 静态服务器**
  `tauri dev` 时指向 `http://localhost:PORT`，由一个小静态服务器（如 `python -m http.server`）
  提供 `gui/`。改动集中在配置，不必改 Rust 逻辑；但需要**额外起一个进程**，
  且 dev 与 prod 加载路径不同（前者 HTTP、后者嵌入）。
- **方案 B：运行时分支直读磁盘**
  在 `run()` 中读环境变量（如 `SMS_GUI_DEV=1`），用
  `WebviewUrl::External(Url::from_file_path("gui/dashboard.html"))` 替代嵌入资源。
  更贴合清单原文「WebView 直读磁盘 dashboard.html」，且**不需要额外服务器**；
  代价是要改 Rust 代码并处理 CSP（现配置含 `connect-src 'none'`，
  `file://` 加载下的资源解析需实测）。

**为什么本轮不宜先行编码**：

1. **本机无 Rust 工具链**（`cargo`/`rustc` 在 PATH、`~/.cargo/bin`、Homebrew、
   `/usr/local` 均不存在）——改完的 Rust 代码**连能否编译都无法验证**，
   写出去只是「看似完成」；
2. **「开发态」如何界定未定** —— 是环境变量、`tauri dev` 检测，还是构建 profile？
   这直接决定方案 A/B 的取舍，也正是待确认的**验收口径**问题；
3. 若验收口径最终选「静态改动待 CI 验证」，则需同步确认 **CI 是否具备
   构建 Rust shell 的条件**（`test-rust` job 已在 CI 中，本地缺失不代表 CI 缺失）。

**结论**：P2-12 与其余 3 项同属**需先决策**的事项，不属于「不需决策即可推进」——
此处纠正此前的判断。决策时的最小问题集是：
（a）方案 A 还是 B；（b）「开发态」的判定方式；（c）验收口径（本地/CI/跳过）。
### P2-12. GUI 是 6315 行单文件 HTML，改一行需重建 + 重装

**证据**：`gui/dashboard.html` 共 6315 行
（内联 `<script>` 约 4672 行、`<style>` 约 1441 行、185 个 `function` 定义）；
`frontendDist` 指向 `../gui`，HTML 被 brotli 内嵌进 Rust 二进制
（改样式必须重建并重装 app 才可见）。

**影响**：UI 迭代成本高，试错代价直接转化为等待时间。

**建议动作**：加一个开发态开关（debug 构建或环境变量），
让 WebView 直接读磁盘上的 `dashboard.html`，改完刷新即可见。

---

### P2-13. 发布流程依赖人肉执行 —— ✅ 已完成（固化为 `scripts/release.sh`，提交 `dc917d6`）

**证据**：本次发布的实际步骤为
`bump_version.py --bump minor` → 手改 CHANGELOG 段落 → commit →
merge master → `git tag -a` → push master + push tag，全部由人按顺序执行。

**建议动作**：固化为 `scripts/release.sh`，把顺序与前置校验写进脚本；
与 P0-2 的 CI 校验形成双重保险。

**落地（已完成）**：新增 `scripts/release.sh`。三个设计取舍：**默认预演**（无 `--apply`
不改任何文件，发布不可逆）；**默认不推送**（仓库有未授权 push 门禁 P0-6，`--push` 才
显式授权且需二次确认）；**CHANGELOG 正文不自动生成**（脚本只硬校验新版本段落存在且非空）。

前置校验链：工作区干净 → 分支为发布源 → 未落后 upstream → **全量测试绿**（复用
`ci_parity.sh`）→ bump 预演（**不解析 `bump_version.py` 输出格式**，解析别家脚本打印很脆）。
事后：合并 `master` 后校验「**tag == master 上 `version.json`**」，与 P0-2 的 CI 校验同源，
构成事前+事后双保险；tag 已存在则拒绝重打。

**验收（实测）**：`bash -n` 通过；`--help` 正常；缺/非法 `--bump` 正确报错；**真跑默认模式
完整链路走通** —— 前置校验 → `Ran 943 tests, OK (skipped=2)` → bump 预览 `0.23.0 → 0.23.1`
（构建号 109 → 110）→ 结束且**工作区未被改动**。提交前那次真跑还额外证明校验在拦
（因工作区有未跟踪文件而拒绝发布，非空转）。

---

### P2-14. 文本编码校验未进 CI —— ✅ 已改（CI 增 BOM/CRLF 检查，范围见 P2-17）

**证据**：`write_text.py --verify` 目前只在本地手工运行；
本次正是靠手工跑才发现 `CONTRIBUTING.md` 末尾缺换行（P1-9）。

**建议动作**：CI 增加一步，对仓库内文本产物批量 `--verify`。

---

### P2-15. 测试输出的裸 stderr 噪音 —— ✅ 已完成（收进断言，实测 4 → 0 行）

**证据**：全量跑汇总里混着负例用例的输出，例如
`❌ --all-profiles 与 --profile 互斥`、`❌ 扫描失败: boom`、`❌ profile 'bad' 加载失败`。

**影响**：这些是**预期内**的负例输出，直通终端后容易让人误判「是不是有失败」。

**建议动作**：负例用例把子进程 stderr 收进断言（而非直通），
或统一由测试框架捕获、仅在 verbose 模式显示。

**落地（已完成）**：先实测确认噪音的**真实位置与规模**：

- 全量跑分别捕获 stdout / stderr，`❌` 行 **stdout 4 行、stderr 0 行**
  —— 即清单原文说的「裸 stderr」其实不准确，是 **stdout**（`scan.py` 的 `print`）。
  对 CLI 本身而言打印错误是对的，问题只在**测试直接调内部函数时输出直通终端**。
- 来源锁定在单文件：`tests/test_exit_codes.py` 的 4 个负例
  （profile 互斥、`run_scan` 抛错、``load_profile`` 抛错 ×2）。

修复方式按清单建议的第一条，并**顺带把输出文案纳入断言**（此前完全没验证过）：

- 新增 `_captured_output()` 上下文管理器（`contextlib.redirect_stdout/redirect_stderr`）；
- 4 个负例改为在捕获内调用，并断言错误文案：
  `--all-profiles 与 --profile 互斥`、`扫描失败: boom`、
  `profile 'bad' 加载失败` + `template missing`、`profile 加载失败: missing`。
  其中 profile 名与底层原因都断言 —— 失败必须指名道姓，否则用户无从下手。

**验收**：全量跑 stdout 中 `❌` 行数 **4 → 0**；`test_exit_codes` 9 个用例全绿。
**判据有效性已反向验证**：把断言文案故意改成一个不存在的字符串，该用例立刻 FAILED
（说明断言真的在比对，不是恒真的空断言）。

---

### P2-16. 四个模块超过 600 行，职责边界需要复核（新增） —— 🚧 进行中（3/4：mainstream_registry、workspace_cleaner、mcp_fixer 已拆）

**证据**（`wc -l core/*.py`）
- `core/mcp_fixer.py` **900**
- `core/mainstream_registry.py` **782**
- `core/workspace_cleaner.py` **698**
- `core/config_store.py` **658**

（对照：全目录 51 个模块合计 14496 行。）

**影响**：单个模块承载多种职责时，改动牵连面大、测试定位困难。
其中 `mcp_fixer` 与 `workspace_cleaner` 涉及**写操作**，
按 `CONTRIBUTING.md` 要求必须遵循「备份 → 原子写 → 校验 → 回滚」安全链——
模块越大，越难保证每条写路径都完整走完该链。

**建议动作**：先做**只读复核**（不改行为），逐个确认是否混装了多种职责；
确认后按职责拆分，并保持对外接口不变。**不要在无测试保护下直接拆。**

**只读复核结果（已完成，未改动任何代码）**

方法：用 `ast` 提取四个模块的顶层结构（函数/常量/import 计数与行数），再查引用面。
**没有把 3000 行读进来做主观判断。**

先修正一个前提：**四个模块都不是「一个文件混装了好几种职责」** —— 它们全是**扁平纯函数
集合、零类定义**，各自单一领域。问题在**体积**，不在职责杂糅。所以「按职责拆分」对其中
两个并不适用，强行切会切出人为边界。

| 模块 | 行数 | 顶层函数 | 体积真正来源 | 该不该拆文件 |
|---|---|---|---|---|
| `mcp_fixer` | 900 | 40 | 渲染器各 25–94 行 + 写操作编排 | **该拆**（唯一真正混装） |
| `mainstream_registry` | 782 | **仅 4** | **单个函数 `register_mainstream_tools` 占 223 行** | **不该拆文件** |
| `workspace_cleaner` | 698 | 13 | 只读检查与写操作各占一半 | 可拆 |
| `config_store` | 659 | 15 | overlay 管理与 managed 生命周期各半 | 可拆 |

- **`mcp_fixer`（900）—— 唯一真正混装。** 三类职责同处一室：①**纯字符串渲染器**
  （`_render_without_entries` 94、`_render_toml` 41、`_render_reasonix_toml` 26、
  `_render_cordis_yaml` 26、`_render_reasonix` 25）；②**工具入口与编排**（`fix_mcp_tool` 74、
  `remove_mcp_entries_tool` 52、`fix_mcp_clients` 48）；③**写安全链**（`_validate_written_config` 32）。
  ①②的分界干净：渲染器是**纯函数、无副作用**，编排层才碰磁盘。拆为
  `mcp_fixer_render.py` + `mcp_fixer.py`。
- **`mainstream_registry`（782）—— 不该拆文件，该拆函数。** 整模块只有 **4 个函数**，
  体积几乎全来自 `register_mainstream_tools` 这**一个 223 行函数**。拆文件只会制造碎片；
  正确做法是按**平台/工具族拆成若干小函数 + 一张数据表**。**且最该先做**：引用面最窄。
- **`workspace_cleaner`（698）** 只读侧：`check_workspace_cleanliness` 90、
  `format_cleanliness_report` 53、`classify_untracked` 42、`scan_temp_files` 26；写侧：
  `ensure_workspace_clean` 115、`interactive_commit_untracked` 90、`clean_temp_files` 77、
  `commit_all_modified` 62。引用面窄（1 测试 + `scan.py`）。
- **`config_store`（659）** overlay 侧：`_disable_in_overlay` 54、`_ensure_overlay_registered` 50、
  `add/update/remove_discovered_client` 71/83/28；managed 侧：`remove_managed_client` 118、
  `cleanup_config_only_client` 61、`set_unified_dir` 57。

**两条硬约束（复核得出）**

1. **对外导入路径必须不变。** `core/` 是扁平目录、无 `__init__.py`，且引用面不小：
   `mcp_fixer` 被 5 个 core 模块 + 3 个测试 + `scan.py` 引用；`config_store` 被 `tool_registry`
   + 3 个测试 + `scan.py`；`mainstream_registry` 被 2 core + 2 测试；`workspace_cleaner` 被
   1 测试 + `scan.py`。新模块承载实现后，**原模块名必须继续可用**（显式再导出）。
2. **⚠️ 再导出与 P1-11 是同一个坑。** 保证兼容所依赖的「原模块再导出」，正是 P1-11 实测中
   **ruff `F401` 会当死代码删掉**的东西（当时造成 5 个用例失败）。两项必须**协同**：先给
   再导出加显式别名（`from x import n as n`）或 `__all__`，再引入 ruff 门禁。**顺序反了会互相拆台。**

**建议执行顺序（风险从低到高）**：`mainstream_registry`（只拆函数不拆文件）→
`workspace_cleaner`（只读/写分离）→ `config_store`（overlay/managed 分离）→
`mcp_fixer`（引用面最广且含写安全链）。

**验收标准**：拆分为多个模块后，现有测试全绿且对外导入路径不变。

**进度：1/4 已完成（提交 `f225908`）**

`mainstream_registry` 已拆分完成，实测数据如下：

| 文件 | 拆分前 | 拆分后 |
|---|---|---|
| `core/mainstream_registry.py` | 782 行 | **18 行**（薄壳） |
| `core/mainstream_registry_data.py` | — | **538 行** |
| `core/mainstream_registry_extra.py` | — | **227 行** |

拆分前实测到的精确边界（供后续三项参考）：
`MAINSTREAM_TOOLS` 是 **L95–L555 的 461 行 AnnAssign 字面量，19 个多行 dict**
（不是 782 行里另有玄机 —— 该模块绝大部分体积就是这两张数据表）；
`extra` 字面量 L562–L781 共 220 行 / 19 元素；`MAINSTREAM_NAMES` 在 L558。

验收证据（三条全过）：
1. 改动前后 `MAINSTREAM_TOOLS + register_mainstream_tools()` 的 `json.dumps`
   **逐字节相同**（17599 字节）；拼接正确性另由 `19 + 19 = 38` 印证；
2. `scripts/ci_parity.sh` → `Ran 943 tests, OK (skipped=2)`；
3. 三模块行数 538 / 227 / 18，**全部 < 600**。

**⚠️ 操作方式教训（重要，直接影响后三项能否做成）**

前四轮（第 11–14 轮）我在这个模块上连续失败，并**错误地归因为「环境在调用之间回滚工作区」**。
真相是：**我把操作写成 2000+ 字符的巨型原子脚本，脚本自身失败了，而我写的 `restore()`
把仓库复原，于是我看不到失败，只看到「什么都没发生」。**

第 15 轮改成**分解为短的、每步都打印明确结果**的命令后一次通过：

1. 生成 data 模块 → 打印行数 + `git status` 立即可见；
2. 生成 extra 模块 → 顺带验证 data 模块可导入；
3. 改写壳 → 当场做逐字节比对；
4. 跑全量测试；
5. 编码校验 + 提交。

期间还自查掉一次误报：看到「19 个元素」曾怀疑数据被截断，实测确认 461 行 / 19 个多行 dict
完全正常。**结论：后三项务必沿用分步方式，不要写巨型脚本；有自动回滚保护时，
必须让每一步都打印可验证的输出，否则失败会被保护逻辑掩盖。**

---

**第四刀 `mcp_fixer` 已完成（提交 `303b436`）：四刀中三刀落地**

| 模块 | 原行数 | 拆分后 | 提交 |
|---|---|---|---|
| `mainstream_registry` | 782 | 18 + 538 + 227 | `f225908` |
| `workspace_cleaner` | 698 | 387 + 322 | `b02214f` |
| `mcp_fixer` | 900 | **400 + 558** | `303b436` |
| `config_store` | 659 | 未拆（约束见下） | — |

**全仓复核（第 22 轮实测）：拆分后 >600 行的模块只剩 `config_store.py`（659 行）。**
（`scan.py` 有 2307 行，但 P2-16 原文限定「四个模块」，未列入，不擅自扩大范围。）

`mcp_fixer` 验收证据：公开名集合 **67 → 67（缺失 0、多出 0）**；`_toml`（`try/except` 内的
`tomllib`/`tomli` 回落导入）在新模块中同样存在；两模块 400/558 行；`Ran 943 tests, OK`。

---

**⚠️ 第 25–31 轮教训：一个伪装成「测试失败」的自伤缺陷（建议单列 P2-18）**

第四刀此前**三次被误判为「测试失败」而回滚**，真正根因**不在被测代码**，而在我做
P2-17 编码归一化时的副作用：用 `write_text.py --stdin < backup` **重写**
`scripts/ci_parity.sh` 时，该工具**新建文件、不继承原权限**：

```
-rwx--x--x  →  -rw-------        （可执行位丢失）
git 100755  →  100644
```

**后果具有分裂性，这正是它难查的原因：**

| 调用方式 | 行为 |
|---|---|
| `scripts/ci_parity.sh`（bash 直接） | 正常 —— shell 交给解释器，**不需要可执行位** |
| Python `subprocess.call([...])` | `PermissionError`；早期表现为 `unbound variable` —— 需 `execve` |

于是调用方看到 **`exit=1` 且没有任何 `Ran N tests` 行**，极易被读成「测试失败」。

处置：`8aae84f` 把 `$var` 紧跟全角标点改为 `${var}`（L48/98/108，**本身是真缺陷**，
但**不是**这些失败的原因，勿混淆）；`edaacd4` 恢复可执行位 `100644 → 100755`；
并纠正 `scripts/verify_gui_consistency.sh` 的同类权限不一致（本地 600 / git 644）。

**必须写入操作规范的两条（建议单列 P2-18「工具链自检」）：**

> **(1)** 用 `write_text.py` 重写 `scripts/*.sh` 后，**必须 `chmod +x` 再 `git add`**。
>
> **(2) 判定「测试失败」前，先确认测试真的跑起来了** —— 日志里必须有 `Ran N tests`。
> 没有这一行就不是测试失败，而是**验证工具没跑起来**；两者处置完全相反。

---

**第 19 轮实测：第三刀 `config_store` 尝试后回滚 —— 猴补丁锚点有三个，不是一个**

按上面的保守方案实做了一次：把 7 个「不碰 `load_discovered`」的低层助手
（`_default_config_path`/`_discovered_file`/`_local_overrides_file`/`_overlay_target`/
`_ensure_overlay_registered`/`_read`/`_disable_in_overlay`）搬进新建的 `config_store_io.py`。
搬迁本身很干净：`config_store.py` 659 → 502 行，io 模块 173 行，公开名 **27 → 27**
（缺失 0、多出 0），且验证了 4 个 `load_discovered` 调用者仍经全局名查找（patch 有效）。

**但测试挂了 1 个**：`test_disables_in_overlay_keeps_trunk_and_removes_config` ——
`FileNotFoundError: .../overlay.yaml`。已 `git checkout` 回滚，仓库现为全绿。

**根因（这是本项最有价值的一条记录）**

`tests/test_config_store.py` L33-44：

```python
# patch _local_overrides_file + _overlay_target to land under tmp
self._orig_overlay_target = config_store._overlay_target
self.mod._overlay_target = lambda cfg_path: self.local   # 直接给 config_store 模块设属性
```

测试**猴补丁了 `config_store` 上的私有助手**。搬走之后，`_disable_in_overlay`
从 **io 模块的 globals** 解析 `_overlay_target`，补丁不再生效 → 写到真实路径。

**所以被猴补丁的锚点共三个，不是一个**：`load_discovered`、`_overlay_target`、
`_local_overrides_file`。我开工前只核对了 `load_discovered` —— **把契约清单做窄了，
于是自己踩了自己刚记录的那类陷阱。**

**由此得出的开工前检查（下一刀必须先跑）**

> 拆分前，除「外部 import 了哪些名字」之外，**必须再查一遍「测试对哪些模块级名字设过属性」**：
> 在 `tests/` 中搜 `patch("config_store.`、`mock.patch.object(config_store`、
> `config_store.<name> =` 等写法，把所有锚点列为**不可迁移**集合。
> 对被搬走的函数，还要检查它调用的名字里有没有这些锚点 —— 有一个就不能搬。

**收益/风险的重新评估（需要决策）**

把这些锚点全部留在 `config_store` 后，可安全搬走的只剩
`_default_config_path`、`_discovered_file`、`_ensure_overlay_registered`、`_read` 四个
（约 61 行），搬完 `config_store` 约 **598 行 —— 离 600 仅剩 2 行余量**。

三种选择：
- **A**：照搬这 4 个，接受 598 行的紧线（须先补上面的猴补丁锚点检查）
- **B**：**判定 `config_store` 不拆**，在清单写明理由（约束密度过高：3 个猴补丁锚点 +
  已知加载期环 + 20+ 处 import；收益仅 ~61 行），把预算让给 `mcp_fixer`
- **C**：把被补丁的助手改成运行时经 `config_store` 间接查找（`config_store._overlay_target(...)`），
  可搬更多，但引入新间接层

前两刀（`mainstream_registry`、`workspace_cleaner`）没有猴补丁锚点，所以顺利 —— 
**这不是运气，是那两个模块的测试没有猴补丁。**

---

**第四刀 `mcp_fixer`（900 行）的约束侦察结果（第 21 轮，已按新规则执行）**

按上面新增的「猴补丁锚点检查」实查了一遍，结果如下：

**猴补丁锚点只有两个，且都在 `tests/test_mcp_fixer.py`：**

| 位置 | 补丁目标 | 含义 |
|---|---|---|
| L340 | `patch("mcp_fixer.os.replace", ...)` | 补丁的是 **`mcp_fixer` 命名空间里的 `os`** —— 即模块级 `import os`。`os` 若被搬走，补丁立即失效 |
| L356 | `patch("mcp_fixer._validate_written_config", ...)` | 补丁**写安全链的校验函数** |

**外部 import 的名字（9 个，全部必须继续可用）：**

- `core/combined_checker.py` → `_merged_legacy_names`
- `core/endpoint_store.py` → `_atomic_write`, `_backup_path`
- `core/config_backups.py` → `_atomic_write`, `_backup_path`, `validate_config_text`
- `core/skill_link_migrator.py` → `_backup_path`
- `core/mcp_entry_removal.py` → `remove_mcp_entries_tool`
- `scan.py` → `fix_mcp_clients`, `remove_legacy_mcp_clients`
- `tests/test_jsonc_removal.py` → `_render_without_entries`, `validate_config_text`
- `tests/test_mcp_entry_removal.py` → `remove_legacy_mcp_tool`, `remove_mcp_entries_tool`
- `tests/test_mcp_fixer.py` → 多个

**因此第四刀的硬约束是三条**（比前两刀严，但比 `config_store` 清楚）：

1. **`os` 必须留在 `mcp_fixer` 命名空间**（`patch("mcp_fixer.os.replace")` 依赖它）；
2. **`_validate_written_config` 必须留在 `mcp_fixer`**，且调用它的写安全链也要留在同模块（否则补丁失效）；
3. 上述 9 个名字经 `mcp_fixer` 的导入路径必须全部不变。

**这符合评审时对该模块的判断**：`mcp_fixer` 是四个模块里**唯一真正混合职责**的
（纯渲染器 + 编排 + 写安全链），所以它该拆；但正因为含写安全链，约束也最紧。
建议的切法：纯渲染器（`_render_without_entries` 等无副作用的格式化函数）出表，
写安全链与编排留在 `mcp_fixer`，出表部分由 `mcp_fixer` 再导出。

**第四刀的函数级切分依据（第 24 轮实测，可直接照做）**

用 AST 实测的 `mcp_fixer` 形状：**41 个函数，按性质三组，分界线很干净**。

| 组 | 函数（行数小计） | 性质 |
|---|---|---|
| **纯渲染/纯文本** | `_result` `_nested_container` `_bearer_headers` `_toml_string` `_render_json` `_render_yaml` `_render_reasonix` `_toml_section_ranges` `_toml_section_tree_ranges` `_render_toml` `_toml_header_end` `_set_toml_field` `_remove_toml_field` `_set_toml_http_headers` `_remove_toml_http_headers` `_reasonix_plugin_ranges` `_toml_string_field` `_replace_or_insert_toml_field` `_render_reasonix_toml` `_set_reasonix_headers` `_remove_reasonix_headers` `_cordis_entry_block` `_render_cordis_yaml` `_render` `_render_without_entries` `_render_without_legacy` `_render_cordis_without_entries` `_reasonix_servers`（**~530 行**） | 无副作用 |
| **写安全链** | `validate_config_text` `_validate_written_config` `_restore_backup` `fix_mcp_tool` `remove_mcp_entries_tool`（~180 行） | 含真正写盘 |
| **编排/入口** | `_effective_tools` `_merged_legacy_names` `_load_cordis_yaml` `fix_mcp_clients` `_endpoint_profiles` `remove_legacy_mcp_tool` `remove_legacy_mcp_clients` + 2 个消息常量（~190 行） | 编排 |

**决定性证据（证明纯渲染组是真的纯）**：实测全模块**真正落盘的只有两处** ——
`fix_mcp_tool`（`os.unlink` / `shutil.copy2`）与 `remove_mcp_entries_tool`（`shutil.copy2`）。
其余被扫描命中的「写盘调用」全是 `str.replace` 与 `yaml.safe_dump` ——
**后者是生成文本字符串，不落盘**，所以渲染组可整体出表。

**建议切法**

```
core/mcp_fixer_render.py   ~530 行  纯渲染/纯文本处理（28 个函数）
core/mcp_fixer.py          ~370 行  validate/写安全链 + 编排 + 再导出
```

**三件必须守住的事**（同第 21 轮侦察结论）：`os` 留在 `mcp_fixer`；
`_validate_written_config` 及其调用链留在 `mcp_fixer`；
9 个外部引用名经 `mcp_fixer` 路径不变（渲染组出表后以 `import X as X` 再导出，
同时满足 P1-11 的 ruff F401 要求）。

**进度：2/4（`workspace_cleaner` 已于提交 `b02214f` 完成）**

`workspace_cleaner`（698 行）按「只读检查 / 写操作」分界拆为两模块：

| 文件 | 行数 | 内容 |
|---|---|---|
| `core/workspace_cleaner_read.py` | **322** | 分类谓词、`check_workspace_cleanliness`、`scan_temp_files`、`classify_untracked`、两个格式化函数 |
| `core/workspace_cleaner.py` | **387** | `_ask_user` + 4 个写操作；并再导出 9 个只读名字 |

- `_ask_user` 归写侧是**查出来的**（仅被原 L313 / L374 两处写操作调用），不是猜的。
- 再导出用 `from workspace_cleaner_read import X as X` 逐个列出，因为
  `tests/test_highrisk_modules.py` 与 `scan.py` 通过本模块访问这些名字（**含私有谓词**
  `_is_temp_file` / `_is_suspicious_file`）；该写法同时避免被 ruff F401 删掉（P1-11）。
- 验收：公开名字集合 **20 → 20（缺失 0、多出 0）**，`TEMP_PATTERNS` 仍 14 项，943 用例全绿。

---

**⚠️ 第三刀 `config_store`：开工前已查明的硬约束（必须先读这一段）**

侦察发现该模块的对外契约**比前两个模块严格得多**，照搬「薄壳 + 再导出」会**静默打破测试契约**：

1. **`load_discovered` 是本模块内 4 处调用 + 外部 2 处 import + 9 处测试 mock 的共同锚点。**
   - 模块内调用：L205、L371、L495、L560
   - `core/tool_registry.py:11` 与 `:402` 两处 `from config_store import load_discovered`（**刻意惰性**）
   - **9 处测试**：`test_registry`、`test_mcp_fixer`×3、`test_check_model_attach`×2、
     `test_management_snapshot`×3 —— 全部写作 `patch("config_store.load_discovered")`
2. `tool_registry.py:397-402` 的注释把契约写明：惰性 import 的存在目的就是
   **「保证测试对 `config_store.load_discovered` 的 mock 生效」**。也就是说，
   `load_discovered` 必须**继续定义在 `config_store` 上**且消费方**运行时取值**。
   若把它搬到别的模块、消费方改为从新模块 import，那 9 处 patch 就会**失效**，
   而测试**仍可能全绿** —— 那是最糟的结果：覆盖假象。
3. 另据 `tests/test_import_acyclic.py`，`config_store` 处在
   `config_store ↔ mcp_fixer ↔ tool_registry` 的**加载期环**中间，环靠 6+ 处惰性 import 打补丁。

**因此 `config_store` 不能照搬前两刀。** 建议做法（待执行）：

- 拆分必须**保持 `load_discovered` 定义在 `config_store` 本模块内**（不搬走）；
- 只搬与 patch 契约无关的部分（如 `set_unified_dir`、`remove_managed_client`、
  `cleanup_config_only_client`、`add_default_clients`、`add/update_discovered_client` 等），
  且搬走后**由 `config_store` 再导出**，保证 `scan.py:101/404/425/538/560` 与
  `test_config_store.py` 的 20+ 处 `from config_store import ...` 全部继续可用；
- 若确实需要移动 `load_discovered`，必须**同时改掉 9 处 patch 路径**，并验证
  mock 仍真正生效（例如故意让 mock 返回特殊值，确认被测代码确实收到该值），
  不能只看「测试全绿」。
- 第四刀 `mcp_fixer`（900 行）引用面最广（5 core + 3 tests + `scan.py`）且含写安全链，
  仍应放在最后。
**执行方案（第 10 轮定稿，可直接照做）**

用 `ast` 算清了依赖闭包，上一轮方案有一处**会达不到验收线**的疏漏，一并修正：

- `extra`（L562–L781，19 个元素）只引用**一个**模块内名字：`_entry`。
- `_entry`（L39 起）的模块级外部依赖只有 `_current_os`（其余全是它自己的参数/局部名）。
- `_current_os` 只依赖 `_PLATFORM_KEY`（L24）+ `sys`。
- `MAINSTREAM_TOOLS` 定义在 **L95，占 463 行**；`MAINSTREAM_NAMES` 在 L558。
- `core/config_store.py:25` 直接 `from mainstream_registry import MAINSTREAM_TOOLS` → **它是公开 API，必须继续可用**。

**疏漏**：只把 220 行的 `extra` 搬出去，数据模块仍带着 463 行的 `MAINSTREAM_TOOLS`，
**总数仍超 600**，达不到「拆分后各模块 < 600」的验收线。必须**拆三个模块**：

| 新模块 | 承载内容 | 预计行数 |
|---|---|---|
| `core/mainstream_registry_data.py` | `sys` + typing import、`_PLATFORM_KEY`、`_current_os`、`_entry`、`MAINSTREAM_TOOLS` | ~490 ✅ |
| `core/mainstream_registry_extra.py` | `EXTRA_MAINSTREAM_TOOLS`（原 `extra`），`from mainstream_registry_data import _entry` | ~225 ✅ |
| `core/mainstream_registry.py`（改薄壳） | 再导出 + `MAINSTREAM_NAMES` + `_pick` + `register_mainstream_tools()` | ~40 ✅ |

依赖方向是单向的（`extra` → `data`；壳 → 两者），**不构成循环 import**，因为 `data` 既不 import
`extra` 也不 import 壳。

**必须守住的三件事**

1. **壳里要显式再导出 `MAINSTREAM_TOOLS`**，否则 `config_store.py` 的 `from mainstream_registry import MAINSTREAM_TOOLS` 立刻断。
   ——这正好又是 P1-11 记录过的那个坑：**再导出是 `ruff F401` 会当死代码删掉的东西**，
   所以要么写成 `from ... import MAINSTREAM_TOOLS as MAINSTREAM_TOOLS`，要么加 `__all__`。
2. `register_mainstream_tools()` 的**返回值必须逐元素等同于改动前**（建议改动前后各 dump 一次对比，
   19 + N 个元素、顺序也要一致）。
3. `MAINSTREAM_NAMES` 是 `[t["name"] for t in MAINSTREAM_TOOLS]`，留在壳里即可，但取值必须不变。

**验收**：`scripts/ci_parity.sh` 全绿（943 用例），且改动前后 `register_mainstream_tools()` 与
`MAINSTREAM_TOOLS` 的 `json.dumps` 结果**逐字节相同**。后一条是本项唯一能证明「对外行为零变化」的手段。

**执行前核查（第 9 轮补充，修正上一条结论里的一处不准确）**

对 `mainstream_registry` 做开工前结构核查时发现：**那个「223 行的函数」内部没有逻辑。**
用 `ast` 看它的骨架只有三条语句：

```
L561  Expr      函数文档字符串
L562  Assign    extra = [
L782  Return    return MAINSTREAM_TOOLS + extra
```

即 **L562–L781 共 220 行全是一个字面量列表**，函数本身只做「拼两个列表并返回」。

因此上一条复核里「按平台/工具族拆成若干小函数」对该模块**不成立** —— 没有逻辑可拆。
正确做法是**把数据表挪出去**：新建 `core/mainstream_registry_data.py` 承载该字面量，
`mainstream_registry.py` 只保留拼接。这样两个模块都能降到 600 行以下，
且对外 API（`register_mainstream_tools()` 的返回值）完全不变。

**下次开工必须先确认的两点（未确认就搬会写出 import 期就报错的模块）**

1. `MAINSTREAM_TOOLS` 的定义位置 —— 它在 `return` 里被引用，但**不在本模块顶层常量清单中**
   （顶层只有 `_PLATFORM_KEY`、`MAINSTREAM_NAMES`），需确认它是靠后定义还是 import 进来的。
2. **那 220 行数据是否引用了模块内局部名**（如 `_current_os()`、`_PLATFORM_KEY`、`MAINSTREAM_NAMES`）。
   若有引用则**不能纯挪**：要么在本模块先求值再传入，要么让数据模块自行 import 这些名字。

**P2-12 的判断（说明为什么本轮不动它）**

Tauri 的 release 打包路径（`frontendDist: ../gui` + brotli 内嵌）目前是**正常工作**的。
本机没有 Rust 工具链 —— 无法编译、无法运行，也就**无法证明「改完刷新即可见」**。
在此前提下往 `build` 里写一个 `devUrl`（指向并不存在的 dev server）会**真的把 `tauri dev` 弄坏**，
比不改更糟。因此本轮不改 `tauri.conf.json`。

若要做，风险可控的形态是：新增 `scripts/dev_gui.sh`，用 Tauri 官方的 `--config` 覆盖机制，
在**开发时**把 `devUrl` 指向本地静态服务器（`python3 -m http.server`），
`tauri.conf.json` 保持不动 → **生产构建路径零影响**。
字段合法性可用 `src-tauri/gen/schemas/desktop-schema.json`（本机可做静态校验）核对。
但「WebView 真的读了磁盘」这一步**必须靠 Rust 运行才能证明**，不能只凭静态校验宣称完成。


### P2-17. 96 个文本文件末尾缺少换行（存量，实测得出） —— ✅ 已完成（补齐 90 个并纳入 CI 门禁）

**现象**：仓库内 **96 / 200** 个被跟踪的文本文件末尾没有换行符（约占一半）。

**证据**
- 实施 P2-14 时写了全量检查脚本并在本地实跑：
  `检查了 200 个文本文件` → 报出 96 个「末尾缺少换行」。
- 分布很广：`core/*.py`（23 个）、`tests/*.py`（33 个）、`docs/**`（13 个）、
  根目录 `README.md` / `SECURITY.md` / `pyproject.toml` / `setup.py`、
  `gui/dashboard.html`、`src-tauri/**` 等。
- 对照：同一批检查中 **BOM 0 个、CRLF 0 个** —— 也就是说编码层面是干净的，
  只有末尾换行这一项是存量普遍现象。

**影响**：无功能影响。但会让「末尾缺换行」这类校验持续报警，
如果直接设为 CI 门禁会立刻让 CI 变红（因此 P2-14 已刻意**不**纳入此项）。

**建议动作**：作为**独立批次**一次性补齐，避免与其它改动混在一起
（96 个文件的 diff 会淹没真实改动）。补齐后再把该检查纳入 CI。

**验收标准**：全量检查报「末尾均有换行」，且此后新增文件由 CI 保证。

---

## 附：v0.23.0 已修（不重复立项）

| 已修项 | 处理 |
|---|---|
| CI 的 Python 作业长期红色 | 根因是 `test_skill_market_cli` 依赖开发机 `~/.skills-manager/skills`；改为临时 HOME 自造夹具 |
| 文档里的测试命令跑不通 | `CONTRIBUTING.md` 与 PR 模板的 `discover -s tests -t .` 会抛 `ImportError: Start directory is not importable`，统一为 CI 的命令 |
| 「本地全绿、CI 变红」 | 根因是 35 个渲染护栏用例因 `node` 不在 PATH 而静默 skip（本地 37 / CI 2）；新增 `scripts/ci_parity.sh` |
| `ci_parity.sh` 自身的盲区 | 初版只校验 node，漏了**解释器版本**（3.9 会得到与代码无关的假失败）；已补校验并自动回退到 3.11 |
| Release 正文为空 | 本次人工补写（**根因见 P0-1，已定案方案 B**） |

---

### P2-18. 脚本卫生自检（新增，超出原清单范围） —— ✅ 已完成

**来源**：P2-16 第四刀（`mcp_fixer` 拆分）时暴露的真实缺陷，不在原清单 9 项之内。

**问题**：用 `qclaw-text-file` 的 `write_text.py` 重写 `scripts/*.sh` 时，该工具
**新建文件、不继承原权限**，导致可执行位丢失并被提交为 git mode `100644`。后果具有分裂性：

| 调用方式 | 行为 |
|---|---|
| `scripts/xxx.sh`（bash 直接执行） | 正常 —— shell 把脚本交给解释器，**不需要可执行位** |
| Python `subprocess.call([...])` | `PermissionError` —— 需要 `execve` |

于是调用方看到 **`exit != 0` 且日志里没有任何 `Ran N tests` 行**，极易被误判为「测试失败」。
本项目为此**误回滚过一次本来正确的重构**。

**同源问题**：脚本中 `$var` 紧跟全角标点（如 `$cand，`）时，bash 在部分 locale 下会把
标点首字节并入变量名，配合 `set -u` 直接 `unbound variable` 中止脚本 —— 同样伪装成测试失败。

**落地内容**

1. **本地门禁**（`scripts/ci_parity.sh` 新增 1c 段，置于测试之前）：
   - `scripts/*.sh` 的 git mode 必须为 `100755`；
   - `scripts/*.sh` 不得出现「`$var` 紧跟全角标点」，须写 `${var}`。
2. **CI**（`.github/workflows/test.yml` 的 `text-hygiene` job 新增 step
   `Shell script hygiene (P2-18)`）：同样两条，失败以 `::error::` 标注。
   与文本卫生同 job —— 两者都属「会伪装成测试失败」的缺陷。
3. **修正自检抓出的 4 处真实缺陷**：
   `scripts/release.sh:140/210`、`scripts/verify_gui_consistency.sh:37` 的变量写法，
   以及 `scripts/verify_gui_consistency.sh` 的 git mode `100644 → 100755`。

**验证（含反向验证，确认自检不是摆设）**

- 人为写入 `$foo，` → 自检报「全角标点」并中止，**未进入测试**；
- 人为 `chmod 644` → 自检报「可执行位」并中止，**未进入测试**；
- 干净态：两项均 ✓ 且 `Ran 943 tests, OK (skipped=2)`；
- 从 workflow 中抽出该 step 的 `run` 脚本**在本地实跑 exit=0**（不是只看 YAML 表面）。

**提交**：`8aae84f`（变量写法）、`edaacd4`（恢复可执行位）、`e56a41b`（自检 + 4 处修正）、
`af8a8e4`（同步进 CI）。

**给后续的操作规范（两条，务必遵守）**

> **(1)** 用 `write_text.py` 重写 `scripts/*.sh` 后，**必须 `chmod +x` 再 `git add`**。
> **(2) 判定「测试失败」前，先确认测试真的跑起来了** —— 日志里必须有 `Ran N tests`。
> 没有这一行就不是测试失败，而是**验证工具没跑起来**；两者处置完全相反
> （前者查被测代码，后者查工具链/权限/locale）。
