# 产品实现 vs 设计文档 评审整改问题清单

- 评审日期：2026-09-04
- 评审方式：8 视角并行智能体评审（子代理实读代码+文档，锚定专业技能标准：ui-ux-pro-max / web-design-guidelines / python-code-style / TDD / Diátaxis）
- 基准文档：详细设计文档-阶段一~四 / 产品形态与开发计划 / 决策记录 D1-D16 / README / SKILL / BUILD
- 结论汇总：**13 条 major · 26 条 minor · 6 条 info（45 条，含跨视角同源去重后约 8 个顶层问题）**

---

## 一、统计总览

| 视角 | major | minor | info | 核心问题 |
|------|:---:|:---:|:---:|----------|
| UI 视觉设计 | 3 | 5 | 1 | L5 启停列渲染偏离、三态配色语义、footer 对比度 |
| UI 交互可访问性 | 3 | 7 | 0 | 错误走 alert 非横幅、无 aria-live、仅靠颜色传状态 |
| Tauri GUI 功能 | 0 | 4 | 1 | L5 聚合列、run_audit_profile 死代码、innerHTML 未转义 |
| 数据契约 | 1 | 2 | 1 | L2/L5 两套链接判定规则不等价 |
| CLI 通用化 | 3 | 3 | 1 | 退出码分类不严、transport 无白名单、strict 双路径失效 |
| 测试与回归 ※ | 0 | 2 | 1 | 新缺陷路径无回归测试锁定 |
| 文档一致性 ※ | 2 | 2 | 0 | LICENSE 缺失、审计数据未刷新（发布 gate） |
| 阶段四启停 ※ | 1 | 1 | 1 | L2/L5 判定分歧（同 DATA-1）、CLI 契约未全兑现 |
| **合计** | **13** | **26** | **6** | |

※ 测试/文档/阶段四三视角：子代理补跑不稳定，由主智能体基于会话已验证事实（234 测试全绿、gate 修复、D 收口、mcp_fixer 安全模式等）综合成稿，证据见各条「证据」。

---

## 二、8 视角完整 findings

### 1. UI 视觉设计（ui-ux-pro-max / design-taste-frontend 标准）

**UIV-1 · major · 可访问性**：L5 启停列三态配色偏离 §6.3
- 设计：阶段四 §6.3 要求「绿 enabled / 黄 disabled / 灰 not_in_repo」
- 实现：`gui/dashboard.html:152-163` l5Cell 当 `enabled===0` 用 `ok-false`(红)、部分用浅灰 `unk`——**全禁用被判红**（red=不合规语义，而 disabled 是正常态）；色盲用户仅靠色相无法区分
- 整改：按 §6.3 配色（全启=绿/全禁=黄/部分启用=黄加明度区分），dot 加 ✓/!/— 符号或描边变体并补 aria-label

**UIV-2 · major · 功能缺失**：skill×client 启停三态矩阵未按 §6.3 实现
- 设计：§6.3 「skill_states 作为新增动态列（client 行 × skill 列）」
- 实现：`dashboard.html:175` 仅单列「启停」(enabled/total 计数)，`skill_states` 字典从未渲染，**not_in_repo 态无任何呈现**
- 整改：按 capability 列同法（:133-141）用 skill_states 键并集生成逐技能列（enabled=绿✓/disabled=黄✗/缺失=灰·）

**UIV-3 · major · 可访问性**：footer `#475569` on `#0b1020` 对比度约 2:1，远低于 WCAG AA 4.5:1（`dashboard.html:70,111`）
- 整改：提到 --muted 级（#94a3b8 ≈7:1）

**UIV-4 · minor · 交互**：空态/加载态/错误态不完整（无空态文案、加载仅按钮文字、解析/审计失败走 alert()）
**UIV-5 · minor · 功能缺失**：§4.1 要求的行末格 hover 明细（install_evidence/configured_url/hook_events）未实现，数据无 UI 出口
**UIV-6 · minor · 交互**：动态列+nowrap 无横向滚动容器、无 sticky 首列，900px 最小窗口必溢出且客户端名滚出视野
**UIV-7 · minor · 可访问性**：无 :focus-visible、hover 无过渡、⚠️/✅ emoji 作图标（违反 no-emoji-icons）
**UIV-8 · minor · 视觉**：排版层级单薄、灰系语义过载（未安装/空值/部分态/降级行四处灰阶）
**UIV-9 · info**：`dashboard.html:92` 重复 class 属性（非法 HTML）、legend 引用未定义 `.form-tag.root`、state=unknown 时 dot 透明不可见

### 2. UI 交互可访问性（web-design-guidelines / WCAG 标准）

**UIX-1 · major · 契约偏差**：退出码 2 走 `alert()` 而非设计规定的顶部「工具运行失败」横幅
- 设计：阶段三 §3.1/§11 明确 exit-2 → 顶部横幅、不当作审计结论
- 实现：`dashboard.html:284-285` catch 统一 alert；`lib.rs:46-51` exit2→Err 落到 alert；banner 仅用于 probe.error（:234-242）
- 整改：invoke 失败(尤其 exit-2) 写入 `#banner`(:93)+`role="status" aria-live`，alert 仅兜底

**UIX-2 · major · 可访问性**：异步更新无 `aria-live`，读屏收不到审计结果/横幅变化（`dashboard.html:92-93`）
**UIX-3 · major · 可访问性**：状态仅靠颜色圆点区分，无文本/ARIA 替代（WCAG 1.4.1；`dashboard.html:54-58,185`）
**UIX-4 · minor**：table 无 caption、th 无 scope（`dashboard.html:96-99,174-177`）
**UIX-5 · minor**：粘贴 textarea 无 label（`dashboard.html:88`，placeholder 不能替代）
**UIX-6 · minor**：全页无 :focus-visible 焦点样式
**UIX-7 · minor**：footer 对比度约 2.5:1（同 UIV-3）
**UIX-8 · minor · 契约偏差**：run_audit_profile 已注册（`lib.rs:69`）但 UI 从不调用（:282 仅 run_audit），选择 profile 仅本地重渲染（:251）
**UIX-9 · minor**：meta div 重复 class 属性（:92，同 UIV-9）
**UIX-10 · minor**：暗色主题未声明 color-scheme/theme-color，滚动条/系统控件明暗不一致

### 3. Tauri GUI 功能

**GUI-1 · minor · 契约偏差**：L5 聚合列 vs §6.3 逐技能矩阵（同 UIV-2）
**GUI-2 · minor · 功能缺失**：run_audit_profile 前端未接线=死代码，且其「同构单元素 results」形状兼容性未验证（`lib.rs:69-74`、`dashboard.html:251,275-297`）
**GUI-3 · minor · 文档不一致**：总纲 §10 "可脱离 CLI 独立运行" 与详细设计 §7 "宿主需已装 CLI、缺则引导 pipx install" 矛盾；实现仅报错，无 §7 引导（`lib.rs:21-59`、`dashboard.html:285`）
**GUI-4 · minor · 安全边界**：innerHTML 直拼快照字段未转义（`dashboard.html:185,208,218-222`），粘贴 JSON/受污染 config 可注入 webview DOM
**GUI-5 · info**：meta 重复 class 属性（:92）
- ✅ 正向确认：只读硬边界（无写入口、lib.rs 仅两条只读命令）、一致性 gate 真复算（verify_gui_consistency.sh:29-39 已 set +e、:41-61 verify_snapshot 逐字段）、Tauri 2 桥接（withGlobalTauri + window.__TAURI__.core.invoke）均达标

### 4. 数据契约

**DATA-1 · major · 行为回归**：同一定义对象两套链接判定规则不等价 → L2 与 L5 可能相反
- checker.py:86-88 `_per_skill_link_states` 用 `commonpath` 前缀匹配；skill_state.py:84-90 `classify_skill_state` 用精确 `target == skill_real`
- 一致时 L2 判 correct 而 L5 判 disabled（链接指向仓库内非本技能目录时）
- 整改：统一判定语义（或双改 single rule），补跨模块一致性测试

**DATA-2 · minor · 文档不一致**：record 新字段 `native_disabled_skills` 未写入阶段四 §4.2 契约（combined_checker.py:132,146）
**DATA-3 · minor · 文档不一致**：无 skills 路径客户端 skills_total=0、form='mixed' 取值分支设计未定义（combined_checker.py:126-133）
**DATA-4 · info · 测试缺口**：多 skills_paths 合并取 enabled 语义、result_ok strict 回退重算路径无测试锁定
- ✅ 正向确认：7 固定字段+capability 动态、L5 五字段、result_ok 默认 L1-L4/strict 可选（combined_checker.py:234-274）、JSON 顶层结构均符合契约，无改名/删除

### 5. CLI 通用化

**CLI-1 · major · 契约偏差**：`--all-profiles` 把 profile/模板加载错误吞为「不合规」退出码 1，违反「2=配置错误」（scan.py:96-118 统一 all_ok→0/1）
**CLI-2 · major · 契约偏差**：transport 无白名单校验，非法值（'sse' 等）静默落入 HTTP 探测（mcp_probe.py:99-112），未 fail fast 退出码 2，且可能对真实端点发起非预期请求
**CLI-3 · major · 功能缺失**：`--strict-skill-state` 在 `--all-profiles`(scan.py:108) 与单 profile 快照(scan.py:145) 两路径不生效（未透传 args），跨客户端漂移仍可能退出 0
**CLI-4 · minor**：用户模板目录畸形文件静默跳过（capability_templates.py:97-105），同名覆盖内置时静默失败
**CLI-5 · minor**：stdio timeout 按单条消息独立计时非「启动+握手总超时」，可拖慢至 2×8s；Transport(Protocol) 抽象未按设计落地（transport.py:84-92）
**CLI-6 · minor**：主流程各 Phase 内部异常被吞并不污染退出码（scan.py:703,992,1031...），工具自身错误未归入 2
**CLI-7 · info · 正向**：--all-profiles 矩阵/JSON 契约、CSV 空值语义、--config/--profile 资源外置、模板 extends/并集/覆盖均符合设计

### 6. 测试与回归 ※（主智能体综合）

**TST-1 · minor · 测试缺口**：CLI-1/CLI-2 暴露的「配置错误→2」「非法 transport→2」路径无测试锁定（test_exit_codes.py 覆盖 0/1/2 基础场景，未覆盖 --all-profiles 配置损坏/非法 transport）——缺陷先于测试出现
**TST-2 · minor · 测试缺口**：--strict-skill-state 双路径失效（CLI-3）无回归测试
**TST-3 · info · 正向**：python3.11 实测 Ran 234 tests OK 全绿无跳过；阶段四 8 个新测试文件齐备（test_skill_state 332 行/toggle 243 行/auditor 164 行/contract 207 行等）；契约测试锁定 L5 字段；verify_gui_consistency.sh 已修复 set -e 缺陷（9c0639e）并真复算
- 证据：会话多次实测 `python3.11 -m unittest discover` = Ran 234 OK；`scripts/verify_gui_consistency.sh` 现行逻辑 set +e + SCAN_RC 捕获 + 仅 exit 2 阻断

### 7. 文档一致性 ※（主智能体综合）

**DOC-1 · major · 文档不一致**：根目录**无 LICENSE 文件**——D2 决策「根目录添加标准 MIT LICENSE」未落地（pyproject.toml 已声明 license=MIT 但空转）；**公开发布 gate 阻塞项**
**DOC-2 · major · 文档不一致**：路线图 §3.3 仍引用 **2026-07-13 旧审计快照**（248 目录/60 路径），D6 行动项「发布前重跑 scan.py --full 刷新」未执行；发布 gate 阻塞项
**DOC-3 · minor**：阶段四 §4.2 未收录 native_disabled_skills（同 DATA-2）；README CSV 空值语义需与设计 §4.3 对齐（同 CLI-7）
**DOC-4 · minor**：阶段三 §7「首次启动 pipx 引导」未实现且总纲 §10 措辞矛盾（同 GUI-3）
- 证据：`ls LICENSE` 确认不存在（本会话实测）；grep 各 md 确认 2026-07-13 引用仍在（路线图:34,74-78 等）

### 8. 阶段四启停 ※（主智能体综合）

**P4-1 · major · 安全边界**：L2/L5 两套链接判定规则不等价（同 DATA-1）——同一定义对象跨层结论可冲突，破坏「四层独立验证 + L5」的判定可信度
**P4-2 · minor · 契约偏差**：run_audit_profile 前端死代码（同 GUI-2）+ --strict-skill-state 双路径失效（同 CLI-3）——阶段四 CLI/GUI 契约未全兑现
**P4-3 · info · 正向**：ADR-17~21 主体落地——skill_state 三态归一、skill_toggle 复用 mcp_fixer 备份-原子-校验-回滚（mcp_fixer.py:355-479）、symlink os.replace 原子替换、审计只读硬边界、不造开关同步器；D12 收口表 4 项与实现一致
- 证据：本会话已读 core/mcp_fixer.py:355-479 安全工作流；决策记录 D11-D16 全文；阶段四详细设计 §13 ADR 表

---

## 三、顶层聚合问题（跨视角同源去重，按优先级）

| # | 问题 | 严重度 | 涉及视角 | 建议整改归属 |
|---|------|:---:|----------|--------------|
| T1 | **L5 启停矩阵未按 §6.3 渲染**（聚合列替代 skill×client 三态矩阵，not_in_repo 无呈现，全禁用误用红） | major | UIV-1/2 + GUI-1 | 阶段四 P1 收尾 |
| T2 | **L2/L5 链接判定规则不等价**（commonpath vs 精确匹配），跨层结论可冲突 | major | DATA-1 + P4-1 | 阶段四 P1 收尾 |
| T3 | **退出码 0/1/2 分类不严**（配置错误落 1、非法 transport 不 fail fast、内部异常吞没） | major | CLI-1/2/6 + TST-1 | 阶段二收口 |
| T4 | **--strict-skill-state 双路径失效**（--all-profiles / 单 profile 快照不生效） | major | CLI-3 + P4-2 | 阶段四 P1 收尾 |
| T5 | **可访问性系统性缺失**（alert 替代横幅、无 aria-live、仅靠颜色传状态、无焦点态、对比度不达标） | major | UIX-1/2/3/6/7 + UIV-1/3 | 阶段三 GUI 收尾 |
| T6 | **发布 gate 未达**：LICENSE 缺失（D2）+ 审计数据未刷新（D6）+ git 历史零拓扑未复核 | major | DOC-1/2 | v1.0 发布前 |
| T7 | **GUI 渲染安全**：innerHTML 未转义注入面 | minor | GUI-4 | 阶段三 GUI 收尾 |
| T8 | **契约/代码死代码与文档漂移**：run_audit_profile 死代码、native_disabled_skills 未入契约、无路径客户端取值未定义、独立运行措辞矛盾 | minor | GUI-2/3 + DATA-2/3 + DOC-3/4 | 文档同步+代码清理 |

---

## 四、建议整改顺序

1. **第一批（阻塞发布，安全/契约类）**：T2（判定规则统一）→ T4（strict 透传）→ T3（退出码分类）→ T6（LICENSE + 数据刷新）
2. **第二批（GUI 达标）**：T1（L5 三态矩阵渲染）→ T5（可访问性：横幅/aria/非颜色状态/焦点/对比度）→ T7（innerHTML 转义）
3. **第三批（收尾清理）**：T8 各项（死代码删除或接线、契约文档补字段、README 对齐）
4. 每批配对应回归测试（TST-1/TST-2 先补测试锁定新缺陷路径），延续 §13 质量门禁（测试全绿+零个人拓扑+文档同步）

---

## 五、整改进度

### 第一批（2026-09-04 完成）

| 项 | 落地内容 | 验证 |
|----|----------|------|
| T2 判定规则统一 | `checker._per_skill_link_states` 由 commonpath 前缀匹配改为精确匹配（`realpath(entry) == realpath(unified/<name>)`），与 `classify_skill_state` 一致；docstring 同步消除矛盾表述 | 新增 `test_skill_state.py::test_link_to_other_skill_or_repo_root_is_not_enabled`（链接指向仓库内其他技能/仓库根 → 无效） |
| T4 strict 透传 | `scan.py` `_run_all_profiles` 与 `_run_single_profile_snapshot` 两处 `result_ok` 补传 `strict_skill_state`（getattr 防御） | `tests/test_review_regressions.py::StrictSkillStatePassthroughTest`（漂移结果不带 --strict → 0，带 → 1，两路径） |
| T3a 配置错误→2 | `_run_all_profiles` 增 `config_error` 标志：profile/模板加载失败、审计执行期异常 → 退出码 2；端点探测失败保持 1 | `test_exit_codes` 更新 + `AllProfilesInternalErrorTest` |
| T3b transport 白名单 | `profile_loader._resolve_profile` 加载期拒绝非法 transport（ProfileError→2）；`probe_mcp` 入口防御性兜底 | `TransportWhitelistTest` |
| T3c 内部异常→2 | `main()` 收集各 Phase/报告生成异常入 `internal_errors`，收尾非空 → 退出码 2（优先于 0/1 判定） | 手工验证 --full 正常路径不受影响（exit 1 分层保持） |
| T6a LICENSE | 根目录补标准 MIT LICENSE（D2 落地） | `ls LICENSE` |
| T6b 数据刷新 | 重跑 `scan.py --full`（175 目录/175 SKILL.md/0 坏链/10 路径）；路线图 §3.3、成熟度行、附录 A、开发计划 §9、决策记录 D6 全部同步刷新 | grep 无 2026-07-13 数据引用残留 |

测试：241 全绿（234 基线 + 1 条 T2 精确匹配用例 + 6 条评审回归用例）。

### 第二批（2026-09-05 完成，GUI 达标）

| 项 | 落地内容 | 验证 |
|----|----------|------|
| T1/UIV-2 L5 三态矩阵 | `gui/dashboard.html` 新增逐技能动态列（所有 record 的 `skill_states` 键并集有序），绿✓=启用 / 黄✗=禁用 / 灰·=不在仓库；图例同步；聚合列保留为次要维度 | node 冒烟：skillColumns 并集/三态映射全过 |
| UIV-1 聚合列配色 | 全禁用=黄（`l5-off`，disabled 属正常态）、部分=黄系降透明（`l5-partial`），不再误用红色；未安装=灰 | 冒烟断言 l5-on/l5-off/l5-partial/l5-na |
| T7/GUI-4 转义 | 新增 `escapeHtml`，全部快照注入点（客户端名/能力组/旧通道/漂移/未纳管/profile/明细 title/aria-label）转义 | 冒烟断言 + grep 18 处调用 |
| UIX-1 exit-2 横幅 | `runTauriAudit` catch 与文件/粘贴解析失败统一写入顶部 `#banner`（"工具运行失败/解析失败（非审计结论）"），alert() 全部移除 | grep 无 alert 残留；与 §3.1/§11 及 gate 注释语义一致 |
| UIX-2/3/4 aria/语义 | `#banner`/`#meta`/`#drift` 加 role=status + aria-live=polite；行首改 `<th scope="row">`，表头 scope=col，caption sr-only；✓/✗/· 全部带 role=img + aria-label；行首 dot 附 sr-only 状态文本（合规/部分缺失/不合规/未安装/未审计） | grep aria-live/sr-only/scope 18 处 |
| UIX-5/6/7/9/10 | textarea aria-label；全交互元素 :focus-visible 外环；footer 提亮至 --muted（≈6.6:1）；meta 重复 class 修复；html color-scheme: dark + theme-color meta | 目检 |
| UIV-4/6/7 空态/滚动/微交互 | 新增 `#empty` 空态引导；`.table-scroll` 横向滚动容器 + 首列 sticky（thead 交叉位 z-index 2）；按钮过渡 150ms；⚠️/✅ emoji 移除（改 ✓/✗/「警告：」文字） | 目检 |
| UIV-5 末格明细 | 新增「明细」列：ⓘ 标记（tabindex=0 可聚焦），hover/焦点展示 install_evidence / configured_url / hook_events（title + aria-label）；SAMPLE 补三字段 | 冒烟覆盖 detailCell 数据源 |
| UIV-9 细节 | `.form-tag.root` 补样式；`.dot.unknown` 描边圆点可见 | 目检 |

验证：241 测试全绿 · `scripts/verify_gui_consistency.sh` 通过（1 profiles, 1 results 完全一致）· node JS 语法检查通过 · 渲染逻辑冒烟 12 项断言全过。

### 第三批（2026-09-05 完成，收尾清理）

| 项 | 落地内容 | 验证 |
|----|----------|------|
| T8/GUI-2/UIX-8 死代码 | 删除 `run_audit_profile`（lib.rs）及其注册；profile 切换确认为前端聚合快照切片（§14.C）；CLI 单 profile 出口保留供 CI；模块注释 + BUILD.md + 阶段三 §5/§7/§8/§11.4 全部同步说明 | grep 无残留引用；241 测试全绿（test_exit_codes 继续锁定 CLI 单 profile 契约） |
| GUI-3 引导与措辞 | lib.rs 缺 CLI 错误信息补 `pipx install skill-mcp-studio` 安装引导（走 UIX-1 横幅通道）；总纲 §10 3.4 验收措辞改为「脱离项目源码独立安装运行（运行时依赖已安装 CLI…）」 | 目检 |
| DATA-2 契约补录 | `native_disabled_skills` 写入阶段四 §4.2（语义：原生禁用挂载点覆盖后仍禁用的技能名）；`test_skill_state_contract.py` 锁定存在性+类型 | 契约测试全绿 |
| DATA-3 取值定案 | 阶段四 §4.2 新增「无 skills 扫描路径客户端取值约定」：空 states/0/0/mixed，不参与漂移；未来扩展走新值不改现有语义 | 文档同步 |

验证：241 测试全绿；`verify_gui_consistency.sh` 通过。

> ~~已知缺口：本机 Rust 工具链缺失（~/.rustup 不存在），lib.rs 改动（纯删减+文案）未经 `cargo check` 编译验证~~
> **已清零（2026-09-05）**：Rust 工具链恢复安装至仓库本地目录（`.rustup-home/` + `.cargo-home/`，均已 .gitignore，不碰系统路径），`cargo check` 全绿（Rust 1.98.1 / tauri 2.11.5），lib.rs 删减编译验证通过；`.dmg` 出包同批修复（原「系统磁盘映像权限限制」实为会话沙箱限制，非环境问题，详见 BUILD.md §3）。

### 遗留项修复（2026-09-05 第二批遗留清零）

| 项 | 落地内容 | 验证 |
|----|----------|------|
| CLI-4 模板畸形文件 | `_load_dir_templates` 对任一 `*.yaml`/`*.yml` 畸形/不可读/缺 `capabilities` **fail fast**（`TemplateError` 退出码 2），逐一点名问题文件，不再静默跳过；同时覆盖用户级与项目级目录 | `DirTemplatesTest`（8 用例） |
| 模板分层目录（B 组） | 三层来源落地：内置 `templates:` < 项目级 `<config 所在目录>/.skill-mcp-studio/templates/` < 用户级 `~/.config/.../templates/`（后装载覆盖同名）；`load_profile(config_path=...)` 穿线，`scan.py` 7 处调用点全部传入 | `DirTemplatesTest` + `ProjectTemplatesWiringTest`（3 用例） |
| CLI-5a 单一超时预算 | `probe_stdio` 的 `timeout` 改为覆盖「启动 + initialize + tools/list」的**单一 deadline 预算**（原每步独立计时最多 2×） | `DeadlineBudgetTest`（实测 1.5s 预算 < 2.4s 上限） |
| CLI-5b Transport 抽象 | 按设计 §5.2 落地完整接口形态：`ProbeSpec`（frozen）/ `Transport` 协议（`runtime_checkable`）/ `StdioTransport`+`HttpTransport` / `TRANSPORTS` 注册表 / `get_transport`+`dispatch_probe`；`probe_mcp` stdio 分支改经注册表分派 | `test_transport_dispatch.py`（14 用例） |
| stdio Windows 兼容（B 组） | 消除三处 POSIX 专属依赖：`select.select`→后台读线程+队列（Windows `select` 不支持管道）；`SIGTERM/SIGKILL`→`proc.terminate()/kill()`；Windows 子进程加 `CREATE_NO_WINDOW` | POSIX 回归 260+ 全绿；真机验证待 Windows 环境（阶段二 §14.5 已注明） |
| UIV-8 排版/灰阶 | 看板字号统一走 `--fs-2xs…--fs-lg` 5 档 token；灰阶语义收敛为 `--gray`（状态标记灰）/ `--absent`（缺失占位 `·`、`—` 更深一级） | node 语法检查通过 |

验证：263 测试全绿；`verify_gui_consistency.sh` 通过。

### 遗留项修复（2026-09-05 第三批：.dmg + 旧账 + D13 复核）

| 项 | 修复 | 验证 |
|----|------|------|
| 旧账：lib.rs 未经编译验证 | Rust 工具链恢复至仓库本地目录（`.rustup-home/`+`.cargo-home/`，已 .gitignore，不碰系统路径）；`cargo check` 全绿（Rust 1.98.1 / tauri 2.11.5，`run_audit_profile` 删减后的 lib.rs 编译通过）；`cargo tauri build` 重建 `.app` 成功 | `cargo check` exit 0；`.app` 产物在 `target/release/bundle/macos/` |
| .dmg 出包 | 根因实锤（三次复现）：tauri 内置 `--bundles dmg` 调用使 `rw.<pid>.<name>.dmg` 临时映像落在源目录 `bundle/macos/` 内，`hdiutil create -srcfolder` 递归自包含报「设备上无剩余空间」（历史误诊为「系统磁盘映像权限限制」；沙箱权限是另一层独立原因，完全访问会话下 `hdiutil` 可用）。修复：新增 `scripts/build_dmg.sh`（绝对路径输出 + 残留/陈旧挂载清理 + 裸 hdiutil 兜底 + 挂载自检） | `skill-mcp-studio_0.1.0_arm64.dmg` 产出（2.6MB，UDZO 92.5%），挂载→核对 .app→卸载自检通过 |
| D13 运行时会话注入（用户要求复核） | 按决策记录重开触发条件逐客户端核查（2026-09-05）：Claude Code 2.1.261 新增 `/skill-doctor`（会话内交互式，非可编程跨会话 API，不合格）；其余 7 客户端无运行时查询面。触发条件均未满足 → **维持延后定案**，评估记录 + 触发条件语义澄清已写入决策记录 §D13 | 决策记录 §D13「重开评估（2026-09-05）」小节 |

验证：263 测试全绿；`verify_gui_consistency.sh` 通过。

### 遗留（不阻塞发布，后续评估）

- B 组延期项（原规划）：~~用户模板目录~~、~~项目级模板分发~~、~~stdio Windows 兼容~~、~~.dmg~~（已落地，见上）、Win/Linux Tauri、运行时会话注入（D13）
- ~~UIV-8/info 级视觉细节~~（已落地）
- ~~CLI-4/CLI-5（minor）~~（已落地）
- 剩余：**Win/Linux Tauri 打包**（用户明示本轮排除；跨平台验证需对应宿主或 CI）；**运行时会话注入（D13，维持延后，2026-09-05 复核未触发重开条件，见决策记录 §D13）**
