# 评审 v0.24.0 待完善清单

> 本轮（第十三轮）为**新起一轮评审**，范围覆盖 CI/CD、工具脚本、版本一致性、
> 依赖治理与仓库卫生。产出是**后续可继续完善的需求清单**，与本仓库既有的
> `评审-v0.23.0-整改与优化清单.md`（P0-1 … P2-18，均已 ✅）互为补充、不重复。
>
> **方法**：先由 3 个只读子代理分头深挖（`core/` 运行时健壮性、GUI+Tauri 安全面、
> CI/文档一致性），再由主评审逐条**复核证据**（对照真实文件与行号），
> 复核不通过的条目不写入本清单。
>
> **编号规则**：`Q<严重度>-<序号>`。Q0 = 会直接导致错误结论或安全风险；
> Q1 = 一致性 / 可维护性；Q2 = 卫生 / 体验。

## 修复进度

| 编号 | 状态 | 提交 | 验收证据 |
|---|---|---|---|
| S1 `--progress-file` 任意路径写入 | ✅ 已修 | `94bccaf` | `DENIED_FLAG_PREFIXES` 含该条目；追加改无条件；反向验证（删除条目 → 2 个测试 FAILED, exit=101） |
| S3 argparse 前缀缩写绕过 | ✅ 已修 | `94bccaf` | 判据改前缀匹配；`scan.py:1324` 加 `allow_abbrev=False`（grep 命中 1） |
| S15 安全边界不变量不可测 | ✅ 已修 | `94bccaf` | 抽为模块级 `is_denied_flag` + 3 个 cargo 测试（6 passed） |
| Q0-4 `release.sh --push` 推错远端 | ✅ 已修 | 本轮 | 改 `RELEASE_REMOTE`（默认 github）+ `--remote` 覆盖；非 github 远端会告警并说明流水线不启动 |
| Q2-4 发布编码校验依赖仓库外个人路径 | ✅ 已修 | 本轮 | 判据内联进脚本（grep `write_text.py` = 0） |
| Q2-5 release.sh 注释与实现不符 + 死代码 | ✅ 已修 | 本轮 | 补检 AHEAD（未推送且不打 tag 时 die）；删除未使用的 `AFTER_VERSION`（grep = 0） |
| **Q0-5** 门禁缺 `cargo test` | ✅ 已修 | 本轮 | 门禁新增 Rust 段跑 `cargo test`；**实测首次真正执行**（此前本地"全绿"从未覆盖 lib.rs） |
| **Q0-6** 门禁缺依赖时只告警不失败 | ✅ 已修 | 本轮 | 缺 PyYAML / ruff 改 `exit 1`；`skipped≠基线` 改 `exit 1`（可用 `CI_ALLOW_SKIP_DRIFT=1` 显式豁免）。**实测**：缺 ruff 时门禁 `exit=1` |
| Q0-3 门禁 ruff 回退指向个人临时目录 | ✅ 已修 | 本轮 | 删除 `/tmp/p05/...`（grep = 0），改为 `.venv/bin/ruff` → `/opt/homebrew/bin/ruff` → `$PY -m ruff`；并给 cargo 补 PATH 发现 |
| **R3** 非 dict 配置段被静默覆盖（数据丢失） | ✅ 已修 | 本轮 | `_find_json_mcp_section` 改为「键不存在才创建，类型不对返回 None」；调用方「结构异常即跳过」**恢复可达**并说明原因；新增 `tests/test_ai_memory_section_guard.py`（5 用例）。**反向验证**：恢复旧写法 → 3 个用例 `FAILED (failures=3)`、`AssertionError: {} is not None`（即"被覆盖成 {}"的直接证据）、exit=1 |
| R6 `_backup_file` 异常无人接住 | ✅ 已修 | 本轮 | 两处调用点包 `try/except RuntimeError` → 跳过并置 `written=False`（无备份的改写不可逆，不能继续） |
| **R1** `git_status` 把「查不了」报成「干净」 | ✅ 已修 | 本轮 | 修了**两层**：① 三个 git 探测命令的 `returncode` 此前完全未检查（`subprocess.run` 不带 `check=True` 时非 0 退出不抛异常）；② 外层 `except Exception: pass`。现在两者都置 `is_clean=None` + 记录 `error`（`None` = 不可知，绝非「干净」）。**实测复现**：`\.git` 为空目录 → `git diff` 退出码 129、`is_clean=None`（修复前为 `True`）。新增 `tests/test_git_status_honesty.py`（3 用例） |
| R2 `git commit` 失败仍打印「✅ 已提交」 | ✅ 已修 | 本轮 | 检查 `returncode`，失败时记入 `result["errors"]` 并打印 `git exit=N` + stderr 首行；成功才打印 ✅。与同文件 `commit_all_modified` 口径一致 |
| R4 `git add` 失败仍计入「已添加」 | ✅ 已修 | 本轮 | 两处 `git add`（自动 + 交互确认）均检查 `returncode`，非 0 计入 `result["skipped"]` 而非 `added` |
| R7 `parse_frontmatter` 读 SKILL.md 无异常保护 | ✅ 已修 | 本轮 | `except (OSError, UnicodeDecodeError) → {}`；此前一个 GBK 编码的 SKILL.md 会让 `check_all_versions` 整轮中断 |
| R8 CLI 版本探测把报错文本当版本号 | ✅ 已修 | 本轮 | 先判 `returncode != 0 → continue`；stdout 优先、stderr 仅兜底 |
| R9 `fetch` 用「stderr 非空」判「有更新」 | ✅ 已修 | 本轮 | 失败→记 `fetch_failed` 并返回；判据改为**只认同形如 ref 更新的行**（` -> ` / `[new branch]` / `[new tag]` / `[new ref]`） |
| R10 备份命名与备份列表约定不一致 | ✅ 已修 | 本轮 | 改用 `file_atomic.backup_path()`（匹配 `config_backups.BACKUP_MARKER = ".bak-"`）；此前 `.bak.<时间戳>` 列不出、也还原不了 |
| **R11** `fix_unified_dir` 整文件改写版本受控 `config.yaml` | ✅ 已修 | 本轮 | 不再 `yaml.dump` 回写 trunk，委托 `config_store.set_unified_dir` 写 gitignored overlay。**实测**：trunk 注释与键序原样保留、值落 overlay |
| R14 截断提示串混入路径列表 → 计数偏大 1 | ✅ 已修 | 本轮 | 只截断展示、不塞提示串。**实测**：60 个 `.log` → 返回 50 个纯路径 |
| R15 技能面板四步共用宽 `except` | ✅ 已修 | 本轮 | 收窄为 `(ImportError, OSError, ValueError, TypeError)` + 记 `skills_error` 透出（「读取失败」不再伪装成「空库」） |


| Q0-1 Rust 工具链浮动 `stable` | ✅ 已修 | 本轮 | 4 个 workflow 的 `dtolnay/rust-toolchain@stable` → `@1.98.1`（与本地实测 rustc 一致），上游发新版不再无声切换编译器 |
| Q1-2 门禁脚本死代码 + 变量遮蔽 `exit` | ✅ 已修 | 本轮 | 删除 `exit=$(printf '%s' 1)`（grep 残留 0） |
| Q1-5 无 `.editorconfig` | ✅ 已修 | 本轮 | 新增 `.editorconfig`：utf-8 / lf / 末尾换行 / 4 空格（yml/json/html/mjs 为 2），Markdown 不裁行尾双空格 |
| Q1-6 无 dependabot | ✅ 已修 | 本轮 | 新增 `.github/dependabot.yml`：pip / github-actions / cargo（src-tauri）/ npm，weekly |
| Q2-1 工作树内 633MB Rust 缓存 | ✅ 已修 | 本轮 | 删除 `.rustup-home`(436M) 与 `.cargo-home`(197M)；从 `.gitignore` 移除条目并注明「不应把 RUSTUP_HOME/CARGO_HOME 指向仓库内」 |
| R5 `.git` 排除未剪枝 | ✅ 已修 | 本轮 | `dirs[:] = [d for d in dirs if d != ".git"]`（与只读侧写法统一，省去无谓遍历整个 `.git`） |
| R12 回滚分支 `open()` 未关闭 | ✅ 已修 | 本轮 | 改 `with open(...) as fh` —— 此前每次回滚泄漏一个 fd |
| R13 `repo_skill_names` 缓存永不失效 | ✅ 已修 | 本轮 | 加 `invalidate_repo_skill_names()` 出口 + 注释说明它同时是 skill_toggle/矩阵/迁移校验的输入（同进程内「先枚举再增删」会拿到陈旧清单） |
| Q0-2 构建 workflow 授予 `contents: write` 过宽 | ✅ 已修 | `8bfcab0`+`144bae3` | 顶层收窄为 `contents: read`；发布拆为独立 job（`contents: write` + `needs` + 仅 tag 触发）；artifact 改为总是上传供 release job 下载。**逐 job 校验 8 项不变量**（build 内发布 0 次 / release 内恰好 1 次 / 上传与下载各 1 次 / artifact 名一致）|
| Q1-1 CI node 20 与本地 node 26 跨大版本 | ✅ 已修 | `8bfcab0` | 门禁跑 JS 用例前校验本地 node 主版本与 CI（20），不一致显式告警。**实测触发**：`⚠ node 主版本不一致：本地 v26 vs CI v20` |
| Q1-3 每次 push 都触发三平台全量构建 | ✅ 已修 | `8bfcab0` | 三个 build workflow 的 `push` 加 `paths-ignore`（docs/**、**.md、LICENSE、dependabot.yml）；tags 触发不受影响 |
| Q1-4 仅测 Python 3.11 | ✅ 已修 | `8bfcab0` | `test.yml` 三个 job 改用 matrix `['3.11','3.13']`（声明范围的上下界），`fail-fast: false`；校验「引用矩阵的 job 均已定义」 |
| Q2-2 用例 skip 基线是魔数 | ✅ 已修 | `8bfcab0` | 基线 2 已注明对应的两条环境耦合用例，后人看到 skipped=3 可判断是噪音还是环境差异 |
| Q2-3 三个构建 workflow 高度重复 | ✅ 已修 | `8bfcab0` | 核验三者 `on/concurrency/permissions` 完全同构；本次把三处改成相同形状，并加**结构不变量校验**以替代无法本地验证的 `workflow_call` 重构 |
| **S4** `open_url` 漏过滤 CR/LF 与 `%`/`!` | ✅ 已修 | `3910f6f` | 判据加 `char::is_control()`（覆盖 `\n \r \t` 与 DEL）并显式拒绝 `%`/`!`。**新增 Rust 测试** `open_url_rejects_control_chars_and_percent`（5 个恶意 URL）；`cargo test` 7 passed |
| **S5** `openExternal` 回退不校验 scheme | ✅ 已修 | `3910f6f` | scheme 白名单 `^https?:\/\/` 提到**回退之前**；`open_url` 被拒时不再落到 `window.open`，改为提示后 return（url 来自 SKILL.md frontmatter，属第三方内容）|
| **S6** `<tr role="button">` 覆盖表格语义 | ✅ 已修 | `3910f6f` | 去掉 `role`，保留 `tabindex` 与键盘补丁，改加 `aria-label`（读屏恢复 5 列的行列关联）|
| **S7** 弹层声明 `aria-haspopup="menu"` 但无菜单语义 | ✅ 已修 | `3910f6f` | 新增 `setupMenuA11y()`：`.pop-menu` 补 `role="menu"`、每个 `.pm-item` 补 `role="menuitem"`、焦点送入第一项；技能菜单与 agent 菜单两处都调用（定义 1 处 / 调用 2 处）|
| **S8** 状态仅以纯色点表达 | ✅ 已修 | `3910f6f` | 状态点加 `aria-hidden="true"`，紧邻补 `.sr-only` 纯文本（样式本就存在，181 行），复用同表已有的 `installed/install_state` 判定，与 MCP 列文字标签口径统一 |
| **S9** stat-card / kv 两份实现 | ✅ 已修 | 本轮 | 抽出**顶层** `statCard()` 作为唯一实现，`renderOverview` 内改为别名、`renderMergeChanges` 改调它。**过程记录**：首次改动直接让顶层函数调用 `renderOverview` 内的局部 `card()` —— 那是函数作用域，会**运行时 `ReferenceError`**，而 `node --check` 只查语法、查不出作用域错误；发现后改为顶层实现。校验：顶层定义 1 / 别名 1 / 调用 1 / 局部残留 0 |
| **S11** 技能搜索无 debounce | ✅ 已修 | 本轮 | 输入处理加 120ms debounce（`setTimeout` 而非 rAF —— 要的是「停下来之后再渲染」）。此前每击键都重建整张技能表 |
| **S12** 无全局错误处理 | ✅ 已修 | 本轮 | 加 `window` 的 `error` 与 `unhandledrejection` 兜底：提示 + 保留控制台堆栈（不吞异常）；try/catch 包住 toast 自身，避免递归。此前异常只落 devtools，页面表现为「某区块一直空着」 |
| **S13** `!important` 覆盖自身样式 | ✅ 已修（宽度类） | 本轮 | `.modal` 宽度改为 `var(--modal-w, min(560px,92vw))`，`.modal-xl`/`.cfm-card`/`.task-card` 只赋变量（grep：变量 4 处、宽度类 `!important` 残留 0）。**保留** `.hidden{display:none}` 与 reduced-motion 的 `animation:none !important` —— 这两处语义上**必须**压倒一切，移除才是错的；`.wrap-cell`/`.empty-cell` 的 padding/white-space 属特异性重构，需浏览器肉眼复验，未动 |
| **S14** `renderMergeChanges` 部分值未转义 | ✅ 已修 | 本轮 | `v.from`/`v.to`/`v.diff` 加 `escapeHtml(String(...))`（此前仅 `k` 被转义）。函数实际名为 `renderMergeChanges`（清单原写 `mergeChangesHtml`，已更正） |
| **S10** 单文件 6316 行拆分 | ⏸ **已论证，暂不做** | — | 见下方专节。已用**真实 WKWebView 实测**证明外置脚本可行（`file://` 下 `'self'` 匹配同目录 .js），但实测同时暴露代价：**15 个测试文件的 114 条断言会失败**（它们从 `dashboard.html` 正则抽 JS 函数体）。纯组织性收益 vs 重写 15 个测试文件的风险，前者不值当前代价。**已试做并回退**（回退后门禁 961 用例全绿） |
| **S2** CSP 收紧 / 关 `withGlobalTauri` | ⛔ **不改（建议被推翻）** | — | 见下方专节。**清单原本建议的「移除 `dangerousDisableAssetCspModification`」会回退一个已验证的修复**：该开关设为 `true` 正是为修「Tauri 注入 nonce → 按 CSP 规范 `'unsafe-inline'` 被忽略 → 75 处 `style=` 属性被静默拦截」的 bug，且 `tests/test_gui_inline_style_csp.py::test_asset_csp_modification_is_disabled` 锁死它。**反向验证**：临时移除该开关 → 该用例失败（`AssertionError: None is not True`），已恢复 |
| S10（Round 13 旧记） | ⚠️ **理由已被推翻** | — | 原写「开发态 `file://` 下 CSP `'self'` 是否匹配同目录子资源**无法在本会话验证**」—— 该理由已被 Round 15/16 的 **WKWebView 实测推翻**（`'self'` 确实匹配，见下方 S10 专节）。**实际结论以上面那条 S10 行为准**，此行仅作修订留痕 |

**本批新增回归测试**：`tests/test_config_overlay_writes.py`（R11 overlay 隔离写入 + R14 截断语义）。
**测试隔离教训（已写入测试文件注释）**：`config_store._overlay_target` 取的是传入 config 的
`profile_sources` 里「第一个已存在的 overlay」。我第一次验证时只把 config 放进临时目录、
没让 `profile_sources` 指向临时 overlay，结果写入落到了**仓库真实的**
`data/local_overrides.yaml`（已回滚：`unified_skills_dir` 这一行是我加进去的，
备份文件证明改动前没有它）。

**门禁强度提升的实测证据**（Q0-5/Q0-6 修完后）：
```
=== Rust 单元测试（Q0-5，CI test-rust 同命令）==
✓ cargo test 通过（src-tauri）        ← 新增；此前本地从未运行，lib.rs 回归不可见
✓ ruff check 通过（.venv/bin/ruff）   ← Q0-3：不再依赖 /tmp/p05 个人目录
Ran 947 tests → OK (skipped=2) → 与 CI 基线一致
exit=0
```
以及 Q0-6 的反向证据：移除可发现的 ruff 后门禁 `==> 失败：ruff 不可用（Q0-6）`、`exit=1`。

**执行方法说明（重要）**：本轮发现 `edit` 工具会报告 `has been updated successfully`
而文件内容实际未变（S1 的第一次修复因此白做）。此后所有改动一律走
**Python 文件 I/O 写入 → `grep` 回读核对 → 跑门禁/测试** 三步，缺一不算完成。

## 总览

| 编号 | 标题 | 位置 | 严重度 |
|---|---|---|---|
| Q0-1 | Rust 工具链用浮动 `stable`，actions 未钉 SHA | `.github/workflows/build-*.yml` | 高 |
| Q0-2 | 构建 workflow 授予 `contents: write` 过宽 | `.github/workflows/build-*.yml:16-17` | 中高 |
| Q0-3 | 本地门禁的 ruff 回退路径指向个人临时目录 | `scripts/ci_parity.sh:223-224` | 中 |
| Q1-1 | CI node 20 与本地 node 26 跨大版本运行 JS 用例 | `.github/workflows/test.yml:239` | 中 |
| Q1-2 | 门禁脚本内死代码，且变量名遮蔽 `exit` | `scripts/ci_parity.sh:206` | 低 |
| Q1-3 | 每次 push 到 develop 都触发 3 平台全量构建 | `build-*.yml:4-8` | 中 |
| Q1-4 | 仅测 Python 3.11，未覆盖声明支持的更高版本 | `.github/workflows/test.yml:56,152,181` | 中 |
| Q1-5 | 无 `.editorconfig` / 无 pre-commit | 仓库根 | 低 |
| Q1-6 | 无 dependabot / renovate | `.github/` | 低 |
| Q2-1 | 工作树内遗留 633MB Rust 缓存 | `.rustup-home/`、`.cargo-home/` | 中 |
| Q2-2 | 用例 skip 依赖开发者机器状态，基线是魔数 | `core/`、`scripts/ci_parity.sh:26` | 中 |
| Q2-3 | 三个构建 workflow 高度重复 | `build-*.yml` | 低 |

---

## Q0-1. Rust 工具链用浮动 `stable`，actions 未钉 SHA

**证据**（`.github/workflows/*.yml` 中全部 `uses:`）
```
uses: actions/checkout@v5
uses: actions/setup-node@v5
uses: actions/setup-python@v6
uses: actions/upload-artifact@v6
uses: dtolnay/rust-toolchain@stable      ← 连大版本都没钉，永远取最新 stable
uses: softprops/action-gh-release@v3
uses: swatinem/rust-cache@v2
```
**影响**：`dtolnay/rust-toolchain@stable` 会在上游发布新 stable 时**无声切换编译器**。
本项目 `Cargo.toml` 用的是 `tauri = "2"` 这类宽松约束，配合浮动的 rustc，
构建失败会以「与本次改动无关」的形式出现 —— 正是本仓库最忌讳的**不可归因失败**。
其余 actions 钉到 major tag（`@v5`）仍可被上游在同一 major 内改动。

**建议**：① `dtolnay/rust-toolchain` 钉到具体版本（如 `@1.98.1`）或 commit SHA；
② 其余 actions 钉 commit SHA；③ 引入 dependabot 统一管理升级（见 Q1-6）。

## Q0-2. 构建 workflow 授予 `contents: write` 过宽

**证据**：`.github/workflows/build-macos.yml:16-17`（三个平台一致）
```yaml
permissions:
  contents: write
```
**影响**：`contents: write` 只有「上传 release 资产」时才需要，而该 workflow 同时被
`pull_request` 触发（`:7-8`）。PR 场景下这一步授权超出所需，属最小权限原则的偏离。

**建议**：把「构建」与「发布」拆成两个 job，构建 job 用 `contents: read`，
仅发布 job 提权到 `contents: write`；或按 job 级 `permissions:` 限定。

## Q0-3. 本地门禁的 ruff 回退路径指向个人临时目录

**证据**：`scripts/ci_parity.sh:221-224`
```bash
elif [ -x /tmp/p05/venv/bin/ruff ]; then
  RUFF_BIN="/tmp/p05/venv/bin/ruff"
```
**影响**：`/tmp/p05/venv` 是某次会话临时创建的虚拟环境。它存在与否**决定了本地门禁
是否真的执行 ruff** —— 换一台机器就静默落到 `python -m ruff` 分支（若也没装则整步跳过并打
「⚠ 未找到 ruff」）。也就是说：**同一份代码在不同机器上的门禁强度不同**，而输出只差一行 ⚠。

**建议**：删除该回退分支；把 ruff 固定为显式前置依赖（例如写入 `CONTRIBUTING` 的
准备步骤并给出 `pip install "ruff==0.16.9"`），找不到就**以非零退出并明确报错**，
而不是降级为警告。

## Q1-1. CI node 20 与本地 node 26 跨大版本运行 JS 用例

**证据**：`.github/workflows/test.yml:239` → `node-version: '20'`；
本地实测 `node --version` → **v26.7.0**。
**影响**：`node --test`（T-3 用例）在 node 20 与 26 上并非完全等价 ——
测试运行器的报告格式与部分 API 行为跨大版本有变化。当前状态是**本地绿不保证 CI 绿**，
反之亦然；而 T-3 恰是 GUI 纯函数（`escapeHtml`/`stripAnsi` 等）的主要防线。

**建议**：`ci_parity.sh` 在跑 JS 用例前**校验 node 主版本并与 CI 对齐**（不一致就显式告警），
或把 CI 与本地统一到同一 LTS 主版本。

## Q1-2. 门禁脚本内死代码，且变量名遮蔽 `exit`

**证据**：`scripts/ci_parity.sh:204-208`
```bash
  note "==> 失败：文本卫生自检未通过（P2-17 / P2-14）。"
  exit=$(printf '%s' 1)
  note "    修法：补齐/去掉末尾换行，或去掉 BOM、把 CRLF 转为 LF。"
  exit 1
```
**影响**：`exit=$(printf '%s' 1)` 赋值的变量从未被使用，属死代码；且用一个名为 `exit`
的变量遮蔽了内建命令，读者容易误以为这两行都在控制退出码。
**建议**：删除该行。（不影响行为，纯可读性。）

## Q1-3. 每次 push 到 develop 都触发 3 平台全量构建

**证据**：三个 `build-*.yml` 均为 `on.push.branches: [master, develop]` +
`pull_request` + `workflow_dispatch`；`build-macos.yml:26` `timeout-minutes: 45`。
**影响**：文档类改动（如本轮只改 `docs/`）也会拉起三个平台的全量打包，
45 分钟超时 × 3 平台的排队成本与并发占用都不小；而这类改动**不可能影响构建产物**。
**建议**：为 `push` 触发加 `paths-ignore`（`docs/**`、`**.md`、`LICENSE` 等），
或把全量构建收敛到 `tags: ['v*']` 与 PR 场景。

## Q1-4. 仅测 Python 3.11，未覆盖声明支持的更高版本

**证据**：`pyproject.toml:10` `requires-python = ">=3.11"`；
`.github/workflows/test.yml` 三处 `python-version: '3.11'`（`:56`、`:152`、`:181`）。
**影响**：声明支持 3.11+ 却只在 3.11 上验证。本仓库大量使用
`pathlib`、`subprocess`、`unittest` 与字符串处理，3.12/3.13 的行为差异
（如 `unittest` 输出细节、deprecation 转 error）可能只在用户机器上暴露。
**建议**：加一个 `matrix: python-version: ['3.11', '3.13']`（不必全测，取上下界即可），
或把 `requires-python` 收窄到 `>=3.11,<3.12` 以如实声明。

## Q1-5. 无 `.editorconfig` / 无 pre-commit

**证据**：仓库根 `.editorconfig`、`.pre-commit-config.yaml` 均**不存在**。
**影响**：缩进、行尾、末尾换行目前**只能靠门禁事后拦截**（1d 文本卫生自检）。
新贡献者用不同编辑器时，会先产出不合规文件再被门禁挡下 —— 成本高且体验差。
**建议**：加 `.editorconfig`（`indent_style=space`、`indent_size=4`、`end_of_line=lf`、
`insert_final_newline=true`、`trim_trailing_whitespace=true`），可选加 pre-commit 钩子
复用现有门禁。

## Q1-6. 无 dependabot / renovate

**证据**：`.github/dependabot.yml`、`renovate.json` 均不存在。
**影响**：`pyproject.toml` 的 `PyYAML>=6.0`、`certifi>=2024.2.2` 是宽松下界，
7 个 actions 也都是浮动引用（见 Q0-1）。当前**没有任何机制**提示依赖/工具链更新，
升级只能靠人想起来。
**建议**：加 `.github/dependabot.yml`，覆盖 `pip`（根目录）与 `github-actions`（weekly）。

## Q2-1. 工作树内遗留 633MB Rust 缓存

**证据**
```
.rustup-home   436M   mtime 2026-09-05   已跟踪 0  .gitignore 命中
.cargo-home    197M   mtime 2026-09-25   已跟踪 0  .gitignore 命中
```
两者**均早于本会话**（本会话的 rustup 装在 `$HOME/.rustup`、`$HOME/.cargo`，
mtime 为 2026-09-26），因此是**更早会话把 `RUSTUP_HOME`/`CARGO_HOME` 指向了仓库内**留下的。
`.gitignore:24` 已覆盖 `.rustup-home/`，故不污染 git，但白占 633MB 工作树。
**影响**：磁盘占用；且 `.gitignore` 里长期保留这两个条目会让人以为「这是预期布局」。
**建议**：删除两个目录；若确认不再需要，从 `.gitignore` 移除对应条目；
并在 `CONTRIBUTING` 加一句「不要将 `RUSTUP_HOME`/`CARGO_HOME` 指向仓库内」。

## Q2-2. 用例 skip 依赖开发者机器状态，基线是魔数

**证据**：`ci_parity.sh:26` `CI_BASELINE_SKIPPED="${CI_BASELINE_SKIPPED:-2}"`；
实测 skip 之一为
`test_combined_checker.CombinedCheckerTest.test_dsh_installed_via_config_evidence`
→ `skipped '本机无 ~/.dsh 配置证据，跳过 DSH 安装证据断言（T-6 环境耦合）'`。
**影响**：① 该用例**是否执行取决于开发者机器上有没有 `~/.dsh`** —— 同样是「本地绿」，
不同人覆盖的应用代码路径不同；② 门禁把跳过数硬编码为 2 却未注明是哪两个，
后人看到「skipped=3」时无从判断是新增噪音还是环境差异。
**建议**：把环境耦合断言改为**显式标记的可选用例**（如 `@unittest.skipUnless` + 命名前缀
`test_optional_`），并在 `ci_parity.sh` 用变量注释写明基线对应的用例名。

## Q2-3. 三个构建 workflow 高度重复

**证据**：`build-linux.yml`、`build-macos.yml`、`build-windows.yml` 的
`on:` / `concurrency:` / `permissions:` 段完全同构，仅 `runs-on` 与打包步骤不同。
**影响**：任何策略调整（如 Q0-1 钉版本、Q0-2 收权限、Q1-3 改触发）都要**改三遍**，
漏改一处就造成平台间策略漂移 —— 本仓库已多次栽在「改了一处没改另一处」。
**建议**：抽成可复用 workflow（`workflow_call`）或至少把公共段提到一个共享文件，
由三个入口引用。

---

---

## 第二轮：子代理分头评审结果（已逐条复核）

由 3 个只读子代理分头深挖，主评审对每条**回到真实文件与行号复核**。
**复核不通过的条目不收录**（见本节末「误报记录」）。

| 编号 | 标题 | 位置 | 严重度 |
|---|---|---|---|
| Q0-4 | `release.sh --push` 推的是 Codeup，而发布流水线在 GitHub | `scripts/release.sh:230,237-238,241` | **高** |
| Q0-5 | 本地门禁完全没有 `cargo test`（CI 独占） | `test.yml:194-225` vs `ci_parity.sh` 全文 | 中高 |
| Q0-6 | 缺 ruff/PyYAML 时只告警不失败；skipped 不匹配仍 `exit 0` | `ci_parity.sh:79-83,228-230,291-294` | 中高 |
| Q1-7 | `SKILL.md` 仍教 `pytest`，与 P1-10 定案直接冲突 | `SKILL.md:151` | 高 |
| Q1-8 | CI 依赖未钉版（pip 无版本、tauri-cli 只锁 major） | `test.yml:158`、`build-*.yml:50/51/57` | 中高 |
| Q1-9 | `test.yml` 缺 `concurrency`（其余 4 个 workflow 都有） | `.github/workflows/test.yml:3-14` | 中 |
| Q1-10 | README 的 CI 章节只描述 5 个 workflow 中的 1 个 | `README.md:409-417` | 中 |
| Q2-4 | `release.sh` 的编码校验依赖仓库外的个人 skill 路径 | `scripts/release.sh:168,176-178` | 中 |
| Q2-5 | `release.sh` 注释与实现不符 + 未使用变量 | `scripts/release.sh:87-94,113-119` | 低 |
| Q2-6 | `release-notes.yml` 的 `head_branch` 判据会误触发等待循环 | `.github/workflows/release-notes.yml:31` | 中 |
| Q2-7 | `pre-push` 注释与实现矛盾 | `scripts/git-hooks/pre-push:10` | 低 |

### Q0-4. `release.sh --push` 推的是 Codeup，而发布流水线在 GitHub —— 最危险的一处「静默不做」

**证据**
```
scripts/release.sh:237  git push origin "$TARGET_BRANCH" || die ...
scripts/release.sh:238  git push origin "$NEW_TAG"     || die ...
scripts/release.sh:241  打印「release workflow 会被 tag 触发，自动补 Release 正文（release-notes.yml）」

git remote -v
  origin   git@codeup-oswaldhill:oswaldhill/skill-mcp-studio.git   ← 脚本推这里
  github   https://github.com/oswaldhill/skill-mcp-studio.git      ← 流水线只在这里跑
```
**影响**：`test.yml` 的 `tag-version-consistency`、三个 build workflow、`release-notes.yml`
**全部只在 GitHub 上运行**。因此 `release.sh --push` 执行成功后——
没有 tag↔version.json 校验、没有三平台构建、没有 Release 正文，
而脚本第 241 行却告诉使用者这些都会发生。**使用者会认为发布已完成。**
这与本仓库反复出现的「动作发生了但门禁/流程没跑」是同一类，但后果最重：它发生在**发布**上。

**建议**：加 `--remote <name>` 参数（默认 `github`），或在 `--push` 时明确提示
必须推 `github` 并联动门禁（`GITHUB_PUSH_ALLOW=1`），同时修正第 241 行的措辞。

### Q0-5. 本地门禁完全没有 `cargo test`（CI 独占）

**证据**：`.github/workflows/test.yml:194-225` 有 `test-rust` job；
而 `scripts/ci_parity.sh` 全文**不含任何 `cargo`**（grep 确认），
但脚本头 `:1-10` 自称「以与 CI 等价的条件运行」。
**影响**：Rust 侧回归（`lib.rs` 的 `cli_candidates`、`open_url` 的注入校验）
在本地永远不可见 —— 本地全绿、CI 变红。这正是 P2-17 / ruff / JS 用例
三次盲区的第四次同型问题。
**建议**：在 `ci_parity.sh` 增加 `cargo test`（先 `command -v cargo` 探测，
缺失时给出**明确的跳过理由**），或改写脚本头的自我声明，
如实写明「本脚本不覆盖 `test-rust`」。

### Q0-6. 缺 ruff/PyYAML 时只告警不失败；skipped 不匹配仍 `exit 0`

**证据**：`scripts/ci_parity.sh`
```
:79-83    PyYAML 缺失 → 仅 note「✗ 缺少 PyYAML」，不 fail
:228-230  ruff 缺失 → note「⚠ 未找到 ruff，跳过静态检查（本地缺此步不会让结论与 CI 相反）」
:291-294  skipped ≠ 基线 → 打印「本地结论不足以代表 CI」，但仍 exit 0
```
**影响**：第 228 行的自我判断是**错的** —— lint 有错时本地绿、CI 红，
正是 P1-11 要根治的盲区。也就是说「ruff 已补进本地门禁」这句话，
在 ruff 二进制缺失时等同于没生效，而输出只差一行 ⚠。
同理，skipped 不匹配意味着有用例没真正跑，却仍以 0 退出。
**建议**：ruff/PyYAML 缺失改为**硬失败**并给出安装命令；
skipped 与基线不符改为非零退出（可用环境变量显式豁免）。

### Q1-7. `SKILL.md` 仍教 `pytest`，与 P1-10 定案直接冲突

**证据**：`SKILL.md:151` → `python3 -m pytest tests/ -q`
（对照：`pyproject.toml` 已按 P1-10 删除 pytest 依赖与配置，
README/CONTRIBUTING/PRD 均已统一为 `python3 -m unittest discover`）。
**影响**：P1-8 把文档统一到 unittest 时**漏了 `SKILL.md`**。
而 `SKILL.md` 是本仓库对外声明的 Skill 入口 —— 别人按它第一条命令执行，
在干净环境会直接失败（pytest 未声明、未安装）。
**建议**：改为 `python3 -m unittest discover -s tests -p 'test_*.py'` 或 `scripts/ci_parity.sh`；
并顺手 grep 全仓是否还有其他 pytest 残留（排除历史归档目录）。

### Q1-8. CI 依赖未钉版

**证据**
```
test.yml:158              python -m pip install PyYAML certifi          ← 无版本
test.yml:187              pip install "ruff==0.16.9"                    ← 已钉（对照组）
build-linux.yml:57        npm install -g @tauri-apps/cli@2              ← 只锁 major
build-macos.yml:51        同上
build-windows.yml:50      同上
```
**影响**：同文件里 ruff 已因「诊断集合随版本变化」而刻意钉死，
但**打包链最上游的 `@tauri-apps/cli` 反而只锁 major** ——
而打包行为恰是 `src-tauri/BUILD.md` 记录过事故的环节。
`PyYAML`/`certifi` 不钉则同一提交在不同时间跑 CI 可能得到不同结果。
**建议**：pip 依赖钉到具体版本；`@tauri-apps/cli` 钉到具体版本；
配合 Q1-6 的 dependabot 统一升级。

### Q1-9. `test.yml` 缺 `concurrency`

**证据**：`concurrency` 在 `.github/workflows/test.yml` 中出现 **0 次**；
而 `build-macos.yml:12-14`、`build-linux.yml:12-14`、`build-windows.yml:12-14`、
`release-notes.yml:23-25` 均已设置。
**影响**：连续 push 到 develop 会并发跑多份 7 个 job 的测试，
不取消在途旧跑 —— 既浪费额度，也让「哪一次的结果有效」变得含糊。
**建议**：补同构的 `concurrency: group: ${{ github.workflow }}-${{ github.ref }}` +
`cancel-in-progress: true`。

### Q1-10. README 的 CI 章节只描述 5 个 workflow 中的 1 个

**证据**：`README.md:409-417` 只提 `build-macos.yml`，且触发表写作
「push / PR 到 `master`」；实际**所有** workflow 的 `branches` 都含 `develop`。
`build-linux.yml` / `build-windows.yml` / `release-notes.yml` / `test.yml`
在 README 中一次都没出现。
**建议**：补一张五个 workflow 的总表（触发条件 + 作用），至少补上 `develop` 分支。

### Q2-4. `release.sh` 的编码校验依赖仓库外的个人 skill 路径

**证据**：`scripts/release.sh:168`
`SK="$HOME/.dsh/skills/qclaw-text-file/scripts/write_text.py"`；
`:176-178` 该文件不存在时仅打印「未找到…跳过」。
**影响**：发布链上的文本编码门禁实际由**某台机器的个人 skill** 承担；
换机器/换用户/未装该 skill 时这一步等于不存在，而脚本 `:166` 却宣称
「与 CI 的 text-hygiene 同源，避免推上去才红」。
**建议**：把判据内联为脚本内的 python（CI 的 `text-hygiene` 本就是一段内联 python，
可直接复用），或写入 `CONTRIBUTING` 作为硬前置要求。

### Q2-5. `release.sh` 注释与实现不符 + 未使用变量

**证据**
- `:87-94` 注释称「落后**或有本地未推提交**都说明状态可疑」，代码只对 `BEHIND` 执行 `die`，
  `AHEAD`（本地未推提交）被完全忽略；
- `:113-119` `AFTER_VERSION="$(...)"` 中嵌入的 python 既不计算也不打印，
  该变量此后**从未被使用**（grep 仅 1 处命中）。
**建议**：删除死代码；注释与检查二选一对齐（要么检查 ahead 并提示，要么改注释）。

### Q2-6. `release-notes.yml` 的 `head_branch` 判据会误触发等待循环

**证据**：`.github/workflows/release-notes.yml:31`
`if: startsWith(head_branch, 'v')`。该 workflow 由 `workflow_run` 触发，
`head_branch` 取**触发运行**的分支名 —— 任何 `v` 开头的普通分支
（例如 `v2-rewrite`）都会让 job 进入 `:87-96` 的 20×15s 等待循环，
5 分钟后以失败收场。
**影响**：制造与发布无关的红色 X，长期会训练人忽略该 workflow 的失败。
**建议**：改为「tag 且非分支」的等价判据（如同时要求事件为 push 且 ref 匹配
`^refs/tags/v`），或直接判 `head_branch` 是否匹配版本号正则 `^v\d+\.\d+\.\d+$`。

### Q2-7. `pre-push` 注释与实现矛盾

**证据**：`scripts/git-hooks/pre-push:10` 注释写
「本 hook 安装在 `.git/hooks/pre-push`，**不随仓库提交**」——
与实际不符：`core.hooksPath=scripts/git-hooks`（随仓库提交），
且该文件 git mode 已是 `100755`。
**影响**：注释误导维护者以为改了源文件无效，从而去改 `.git/hooks/` 下的副本（改了也会被覆盖）。
**建议**：更正注释，说明「经 `scripts/install-hooks.sh` 设置 `core.hooksPath` 生效」。

### 误报记录（复核后**否掉**，不收录）

| 子代理条目 | 子代理结论 | 复核结果 |
|---|---|---|
| 版本 SSOT「四副本无任何校验」 | 只有 `test.yml` 的 tag↔version.json 校验，Cargo/tauri.conf/pyproject 仅被写入、无断言 | **否掉**：`tests/test_version_consistency.py` 已含 5 个用例 —— `test_pyproject_version_matches`、`test_cargo_version_matches`、`test_tauri_conf_version_matches`、`test_readme_badges_match`、`test_changelog_header_matches`，覆盖子代理声称缺失的全部对象。子代理只看了 workflow，漏了这 66 行测试文件。**本项目前如实为「已覆盖」** |

> 记这次误报的意义：子代理能给广度，但**结论必须接受与代码的直接对质**。
> 本轮 14 条里 1 条被否（约 7% 误报率）——这个比例足以说明「不复核就直接采信」是不可接受的。
---

## 第三轮：`core/` 运行时健壮性（子代理产出，主评审逐条复核）

15 条中 **14 条成立、1 条被推翻**（见 R5）。以下为复核后的收录结果。

| 编号 | 标题 | 位置 | 严重度 |
|---|---|---|---|
| R1 | 检查失败被 `except: pass` 吞掉，对外报「工作区干净」 | `core/git_sync.py:305-308` | 高 |
| R2 | `git commit` 不检查 returncode，失败也打印「✅ 已提交」 | `core/workspace_cleaner.py:272-276` | 高 |
| **R3** | **非 dict 配置段被静默覆盖为 `{}` → 用户 MCP 条目丢失** | `core/ai_memory_checker.py:382-383` vs `:460-463` | **高** |
| R4 | `git add` 失败仍计入「已添加」 | `core/workspace_cleaner.py:68-70,95-98` | 中高 |
| R5 | `.git` 排除未做剪枝（**结论部分推翻**） | `core/workspace_cleaner.py:344-346` | 低 |
| R6 | `_backup_file` 抛的 RuntimeError 无人接住，批量修复整体崩溃 | `core/ai_memory_checker.py:342-349,449,484` | 中高 |
| R7 | `parse_frontmatter` 读 SKILL.md 无异常保护，非 UTF-8 中断全阶段 | `core/version_checker.py:33` | 中 |
| R8 | CLI 版本探测不看 returncode，把报错文本当版本号展示 | `core/tool_registry.py:478-484` | 中 |
| R9 | 用 `git fetch --dry-run` 的 stderr 非空判定「有远程更新」 | `core/git_sync.py:200-206` | 中 |
| R10 | `fix_unified_dir` 备份命名与备份列表约定不一致 → 备份不可见不可还原 | `core/git_sync.py:110` vs `core/config_backups.py:21` | 中高 |
| R11 | `fix_unified_dir` 仍整文件改写版本受控的 `config.yaml`，且丢注释 | `core/git_sync.py:113-114` | 中高 |
| R12 | 回滚分支 `open()` 未关闭（fd 泄漏） | `core/endpoint_store.py:83` | 低 |
| R13 | `repo_skill_names` 的 `lru_cache` 永不失效，且是全仓门禁的输入 | `core/skill_state.py:32` | 中 |
| R14 | 截断提示串混入路径列表 → 临时文件计数恒偏大 1 | `core/workspace_cleaner_read.py:181-182` | 中 |
| R15 | 技能面板四步共用宽 `except` → 「读取失败」显示为「库为空」 | `core/management_snapshot.py:260-271` | 中 |

### R3. 非 dict 配置段被静默覆盖 —— 本轮唯一会造成**用户数据丢失**的一条

**复核证据**（`core/ai_memory_checker.py`）
```python
378	    current = data
379	    for key in mcp_key_path:
380	        if not isinstance(current, dict):
381	            return None                    ← 只防「当前层不是 dict」
382	        if key not in current or not isinstance(current[key], dict):
383	            current[key] = {}              ← 但把「值不是 dict」直接覆盖掉
384	        current = current[key]
385	    return current
```
调用方（`:460-463`）本有保护：
```python
460	    section = _find_json_mcp_section(data, mcp_key_path)
461	    if section is None:
462	        result["message"] = "MCP 配置段结构异常，跳过"
463	        return result
```
**但 382-383 行使 `current` 恒为 dict、函数永不返回 `None` —— 461 行的保护是死代码。**
当客户端配置里 `mcpServers`（或 `mcp_key_path` 任一层）是列表/字符串/标量时，
其内容被**无提示清空**后整文件回写，用户原有 MCP 条目消失，只能靠 `.bak-` 恢复。

**建议**：拆开两个判断 —— `key not in current` 时创建；`not isinstance(current[key], dict)` 时
`return None`，让 461 行的「跳过」真正生效。**这是本轮最该先修的一条。**

### R1. `except Exception: pass` 把「检查失败」当成「干净」

```python
305	        result["is_clean"] = not result["has_changes"]   ← 在 try 内
306	
307	    except Exception:
308	        pass                                             ← 无错误字段、无返回值变化
```
`git` 不存在（`FileNotFoundError`）、超时或仓库损坏时异常被吞掉，函数按初始默认值返回
`has_changes=False, is_clean=True` —— **安全结论反向**：对上层报告「工作区干净、无未提交变更」，
任何以此为准的清理/提交前置判断都会被跳过。
**建议**：`except Exception as exc: result["error"] = str(exc)`，并把 `is_clean` 置为 `None`。

### R2. `git commit` 失败也打印「✅ 已提交」

```python
272	                subprocess.run(
273	                    ["git", "commit", "-m", msg],
274	                    cwd=expanded, capture_output=True, text=True, timeout=30
275	                )                                  ← 返回对象丢弃，未读 returncode
276	                print("  ✅ 已提交新添加的未跟踪文件")
```
pre-commit hook 拒绝、`user.email` 未配、索引锁等情况下仍输出成功，也不写入 `result["errors"]`；
第 289 行的「最终验证」只看 working tree，用户会以为改动已入库。
对照 `commit_all_modified`（`:170-185`）是检查了 returncode 的 —— **同一文件内两处口径不一致**。

### R5（**结论部分推翻**，如实记录）

子代理称：`continue` 不剪枝 → `os.walk` 会下钻 `.git/objects` 等并**删除其中的文件**。

**复核结果：该结论不成立。** 判据是 `os.sep + ".git" + os.sep in root or root.endswith(os.sep + ".git")`；
当 `os.walk` 下钻到 `.git` 的任意子目录时，`root` 形如 `<base>/.git/objects`，
**包含 `/.git/` 子串 → 条件为真 → 同样 `continue`**，循环体不会执行删除。
因此 `.git` 子树内的文件**不会被删**。

**仍保留为低严重度改进项**：`dirs[:] = [d for d in dirs if d != ".git"]` 可避免无谓地遍历
整个 `.git`（大型仓库有明显开销），且与只读侧 `workspace_cleaner_read.py:167` 的写法统一。
---

## 第四轮：GUI + Tauri 安全面（子代理产出，主评审逐条复核）

15 条全部复核通过（未发现误报）。第 1 条是本轮**最高严重度**。

| 编号 | 标题 | 位置 | 严重度 |
|---|---|---|---|
| **S1** | **`--progress-file` 未被拒绝 → webview 可指定任意路径写入** | `src-tauri/src/lib.rs:353,368-373,381-385` | **高** |
| S2 | CSP 保留 `script-src 'unsafe-inline'` + `withGlobalTauri: true` + 关闭 CSP 资源改写 | `src-tauri/tauri.conf.json:10,22,23` | 中高 |
| S3 | DENIED_FLAGS 用精确比较 → argparse 前缀缩写可绕过（`--prof` 等价 `--profile`） | `src-tauri/src/lib.rs:353,368-372` | 中 |
| S4 | `open_url` 黑名单漏 `\r`/`\n`/`%`/`!`，Windows `cmd /C start` 有断行面 | `src-tauri/src/lib.rs:417-433` | 中 |
| S5 | `openExternal` 的 `window.open` 回退不校验 scheme，URL 来自 skill 元数据 | `gui/dashboard.html:2632-2640` | 中 |
| S6 | 技能行用 `<tr role="button">`，覆盖表格语义 | `gui/dashboard.html:2795` | 中 |
| S7 | 声明 `aria-haspopup="menu"` 但弹层无 `role="menu"`，焦点不进入菜单 | `gui/dashboard.html:2385,6011,6050` | 中 |
| S8 | 状态仅以纯色点表达，无文本/aria 替代 | `gui/dashboard.html:2485` | 中 |
| S9 | 同一 UI 构件两份实现（stat-card / kv），改一处易漏 | `gui/dashboard.html:2139,5386,2053,4888` | 中 |
| S10 | 单文件 6316 行，CSS/JS 全内联，难以模块化与可测试化 | `gui/dashboard.html:8-1448,1643-6314` | 低中 |
| S11 | 技能搜索每次按键全量重过滤并重建 chips，无 debounce | `gui/dashboard.html:5830,2707-2721` | 低 |
| S12 | 无全局错误处理；后台自动刷新失败静默 | `gui/dashboard.html:3771,3842-3849` | 低 |
| S13 | 5 处 `!important` 用于覆盖自身样式表（宽度类） | `gui/dashboard.html:394,403,404,497,503,536` | 低 |
| S14 | `mergeChangesHtml` 中 `v.from`/`v.to`/`v.diff` 未转义（同函数 `k` 已转义） | `gui/dashboard.html:5524` | 低 |
| S15 | `--active-profile` 在 CLI 中不存在（空转 DENIED 项），与注释互相矛盾 | `src-tauri/src/lib.rs:136-144,349-353` | 低 |

### S1. `--progress-file` 未被拒绝 → 从 webview 取得「写任意路径」的原语

**复核证据**（`src-tauri/src/lib.rs`）

```
353  const DENIED_FLAGS: &[&str] = &["--config", "--profile", "--active-profile"];   ← 没有 --progress-file
355  let first_flag = args.iter().find(|a| a.starts_with("--")).cloned();            ← 只取「第一个」标志
356  if let Some(flag) = first_flag {
357      if DENIED_FLAGS.contains(&flag.as_str()) { return Err(...) }
362      let bare = sub.split('=').next().unwrap_or(sub);
363      if !ALLOWED.contains(&bare) { return Err(...) }                             ← 白名单只作用于第一个标志
366  }
367  // Also scan the whole argv for denied flags appearing later
368  for a in &args {
369      let bare = a.split('=').next().unwrap_or(a);
370      if DENIED_FLAGS.contains(&bare) { return Err(...) }                         ← 后续标志只比对这三项
372  }
381  if is_scan && !argv.iter().any(|a| a == "--progress-file") {                    ← 调用方已给就不覆盖
382      let _ = std::fs::remove_file(progress_file_path());
383      argv.push("--progress-file".to_string());
384      argv.push(progress_file_path().to_string_lossy().into_owned());
385  }
```

而同文件 `:136-144` 的注释明确宣称：
> **路径由 shell 独占决定，webview 不能指定** —— 否则 `--progress-file` 就变成
> 一个「往任意路径写 JSON」的写原语，越过了 ARCH-2 的写边界。

**该不变式被自身守卫破坏。** 触发路径：

```
invoke("run_cli", { args: ["--skill-usage", "--progress-file", "/Users/x/.zshrc"] })
```

- 第一个标志 `--skill-usage` 在白名单内 → 通过；
- 后续标志只与 `DENIED_FLAGS` 三项比对 → `--progress-file` 不在其中 → 通过；
- 381 行发现该 token 已存在 → **不再替换**，于是 CLI 按调用方给的路径原子写 JSON
  （`open(tmp,"w")` + `os.replace`）→ 覆盖任意可写文件。

性质是**任意路径写入（数据破坏 / DoS）**，不是代码执行。另外 `--progress-file=/x` 形式因
精确比较 `a == "--progress-file"` 而漏检、反由 shell 补上自己的路径（结果安全）——
即**「分开写成两个参数」才中招**，这也解释了它为何一直没被测试发现。

**建议**（任一即可，建议第一条）：
1. 把 `--progress-file`（以及所有「值型路径参数」）加入 `DENIED_FLAGS`；
2. 不再依赖 token 检测，改为**先剥掉调用方所有 `--progress-file*`**，再由 shell 追加；
3. 收紧为「argv 只允许白名单标志 + 位置参数」，即让 363 行的白名单对**每个**标志生效。

### S2. CSP 与全局 IPC 暴露的组合

```
 10  "withGlobalTauri": true,
 22  "csp": "default-src 'self'; script-src 'self' 'unsafe-inline'; ... connect-src 'none'; object-src 'none'; ...",
 23  "dangerousDisableAssetCspModification": true
```
**复核**：CSP 未含 `unsafe-eval`、无 `*` 通配、无 `dangerousRemoteDomainIpcAccess`（这些方向没问题），
但 `'unsafe-inline'` 使内联事件处理器（如注入的 `onerror=`）可执行，而 `withGlobalTauri` 把
`invoke` 暴露到 `window.__TAURI__` → **一次 DOM 注入即拿到 `run_cli` 的全部白名单写命令**
（`--delete-skill` / `--remove-mcp-entry` / `--restore-config-backup` / `--merge-skills`）。
第 23 行又关掉了 Tauri 对内置资源的 CSP nonce/hash 改写，「收紧 unsafe-inline」这条路被自断。
子代理未找到当前可达的注入点（S14 之类属边角），故定级中高而非高。

**建议**：`script-src` 去掉 `'unsafe-inline'`，把 4672 行内联 `<script>` 外置为同目录 `.js`
（CSP `'self'` 即够）；关掉 `withGlobalTauri`，改用 `@tauri-apps/api/core` 的具名导入；
移除 `dangerousDisableAssetCspModification`。**这三件事与 S10（单文件拆分）是同一动作，可一并做。**

### S3. 白名单被 argparse 前缀缩写绕过

`DENIED_FLAGS.contains(&bare)` 是**精确比较**，而 `scan.py` 的 `ArgumentParser` 未设
`allow_abbrev=False`（默认开启前缀缩写）。在 `["--add-endpoint", "--prof", "other"]` 中
`--prof` 既不等于 `--profile` 也不在 `ALLOWED` —— 但它不是第一个标志，故 363 行的白名单不适用于它；
后续只与 DENIED 三项比对 → 放行，而 argparse 会把它解析成 `--profile`。
**建议**：`DENIED_FLAGS` 改前缀判断，并给 CLI 加 `allow_abbrev=False`。

### S15. 安全边界的「文档描述」与「实际代码」不一致

`DENIED_FLAGS` 里的 `--active-profile` 在 `scan.py` 中**根本不存在**（grep 0 命中），
注释描述的是「未来」的标志；而真正存在的 `--progress-file` 写原语既没进 DENIED、也没被守卫拦住。
**建议**：把注释里的不变式写成**可执行断言**，删掉指向不存在标志的条目。
**评审时最容易误判为「已覆盖」的正是这种地方。**

### 子代理明确「查过且没问题」的方向（记录以免重复怀疑）

`eval`/`new Function`/`document.write`/`insertAdjacentHTML`/`outerHTML` 全为 0 命中；
技能市场远程渲染全部走 `textContent`/`createElement`（不拼 HTML）；
`capabilities/default.json` 仅 `core:default` + `dialog:default` + `dialog:allow-open`
（无 fs / shell 插件）；CSP 无 `unsafe-eval`、无通配、无远程 IPC；
模态框用原生 `<dialog>` + `showModal()`；大列表均有分页或折叠（20/页、12/30/24 条折叠）；
`escapeHtml` 全文 204 处使用，按「模板插值是否含 escapeHtml」全量枚举 311 处后仅剩 S14 的低危边角。

`core/` 侧：无 `shell=True` / `os.system` / `eval` / `exec`；所有 `subprocess.run` 均带 `timeout=`；
`open()` 除 R12 外均显式 `encoding="utf-8"`；无硬编码个人绝对路径；
`Popen` 仅 `transport.probe_stdio` 一处且 `finally` 已 terminate/wait 并关闭三条管道。

---

## 复核统计（三轮合计）

| 来源 | 条数 | 复核通过 | 复核推翻 |
|---|---|---|---|
| 主评审自查 | 12 | 12 | 0 |
| 子代理·CI/文档 | 14 | 13 | 1（版本 SSOT「四副本无校验」） |
| 子代理·`core/` 运行时 | 15 | 14 | 1（R5「会删除 .git 内文件」） |
| 子代理·GUI/Tauri | 15 | 15 | 0 |
| **合计** | **56** | **54** | **2** |

**约 3.6% 的发现经不起与代码的直接对质。** 这个比例不高，但足以说明：
**子代理能提供广度，结论必须由主评审回到代码复核** —— 两次推翻都发生在
「只看了配置/工作流、没看具体实现」的地方。

---

## 已核实「无问题」项（记录以免后人重复怀疑）

| 项 | 结论 | 依据 |
|---|---|---|
| 版本单一真源是否漂移 | **无问题** | `tests/test_version_consistency.py` 断言 `version.json` ↔ `pyproject.toml` / `src-tauri/Cargo.toml` / `src-tauri/tauri.conf.json` / README 徽章 / CHANGELOG 顶部；`scripts/bump_version.py` 同步这些位置。实测四处均为 `0.23.0` |
| ruff 版本是否 CI/本地一致 | **无问题** | CI `.github/workflows/test.yml:187` 用 `ruff==0.16.9`；`ci_parity.sh:217` `RUFF_VERSION="0.16.9"`，并在版本不符时告警 |
| `test.yml` 权限是否过宽 | **无问题** | 已是 `permissions: contents: read` |
| 并发控制 | **无问题** | 5 个 workflow 全部设置 `concurrency` |
| 生成物是否被跟踪 | **无问题** | `.rustup-home/`（436M）、`.cargo-home/`（197M）跟踪数均为 0；`git status` 干净 |
| 是否存在 `TODO`/`FIXME` 技术债 | **无问题** | 仅命中 vendored 的 rustup/cargo 缓存内第三方源码，项目自身代码无遗留标记 |

---

## 建议的推进顺序

1. **先做 Q0 三项**：它们是「会让人得出错误结论或引入风险」的类别，与本仓库
   已确立的原则（门禁必须能被实际执行的路径强制到）直接相关。
2. **再做 Q1-1 / Q1-3 / Q1-4**：属覆盖面对齐与成本控制，改动小、收益明显。
3. **Q1-5 / Q1-6 / Q2-3** 可与上述并行（互不冲突）。
4. **Q2-1 / Q2-2** 属清理与文档化，随任意一轮顺手完成即可。

每项完成后按既有约定：跑 `scripts/ci_parity.sh` 全量 unittest + `qclaw-text-file`
编码校验，并在本清单标注完成提交号。



---

## S2 / S10 专节：为什么这两项不做，以及一个被推翻的清单建议

### 被推翻的建议（原清单 S2）

原清单写：

> `script-src` 去掉 `'unsafe-inline'`，把 4672 行内联 `<script>` 外置为同目录 `.js` …
> 移除 `dangerousDisableAssetCspModification`。

**后半句是错的。** `CHANGELOG.md:342-355` 记录了同一个坑的完整根因与修复：

- Tauri 构建期调用 `inject_nonce_token` 给每个 `<style>` 注入 nonce
  （`dangerousDisableAssetCspModification` 默认 `false` 时启用）；
- 运行时 CSP 因此变成 `style-src 'self' 'unsafe-inline' 'nonce-…'`；
- 按 CSP 规范，**指令里一旦出现 nonce/hash，`'unsafe-inline'` 即被忽略**；
- 症状：`<style>` 块靠 nonce 活着（整站样式看着正常），而 75 处 `style="…"` 属性被静默拦截
  —— 强度条全部等长（实测逐像素 236px、极差 0）；
- 修复第 ① 层就是**显式设 `dangerousDisableAssetCspModification: true`**，让声明的
  `'unsafe-inline'` 真正生效。

并且 `tests/test_gui_inline_style_csp.py` 有专门用例锁死这个开关。**反向验证**（临时移除该开关）：

```
AssertionError: None is not True : 必须显式关闭 Tauri 的资产 CSP 改写：否则 <style> 会被注入
nonce，CSP 里的 'unsafe-inline' 随之失效，全站 style="…" 属性被静默拦截（症状：强度条全部等长）
Ran 16 tests — FAILED (failures=1)
```

已恢复为 `true`。**所以照清单改会精确回退一个已修好并有用例保护的 bug。**

### S2 剩下两半各自为什么也不做

1. **去掉 `script-src 'unsafe-inline'`**：前置是外置脚本，而开发态页面以 **`file://`** 加载
   （`lib.rs:478` 用 `Url::from_file_path(dashboard.html)`；仅当设了 `SMS_GUI_DEV`）。
   `file://` 文档来源不透明，CSP `'self'` 是否匹配同目录 `.js` **在 WebKit/TAURI 下需实机确认**。
   判错的后果是开发态整页白屏 —— 属「改一行、坏一片」，不做。
2. **`withGlobalTauri: false`**：`tauriInvoke` 建立在 `window.__TAURI__` 上
   （`gui/dashboard.html:3768`），而它是**唯一**的原生桥。替代方案 `@tauri-apps/api`
   必须经打包器引入，**本仓库没有任何前端打包器**（无 `package.json`/vite/rollup，
   `frontendDist` 是静态目录 `../gui`）。因此该项在当前架构下**不可行**，需要先引入
   构建工具链 —— 那是独立议题，不属于本轮整改。

### 若要推进，建议的顺序（都需实机验证）

1. 先让开发态也走 Tauri 自定义协议（而非 `file://`），使 `'self'` 语义与生产一致；
2. 再把内联 `<script>` 外置为 `gui/dashboard.js`（顺带完成 S10 的脚本部分）；
3. 然后去掉 `script-src` 的 `'unsafe-inline'`（**保留** `style-src` 的，页面有 77 处内联 `style=`）；
4. 最后才考虑引入打包器以关闭 `withGlobalTauri`。



---

## S10 专节：实测证据、暴露的代价、以及为什么回退

### 一、已用真实引擎证明「外置脚本可行」

S2/S10 此前的阻塞点是猜测：「开发态以 `file://` 加载页面时，CSP 的 `'self'`
是否匹配同目录 `.js`」。本轮写了 `WKWebView` 探针（Swift，**Tauri 在 macOS 上用的正是
这个引擎**，不是 Chrome）实测，得到确定结论：

```
[现状 script-src 'self' 'unsafe-inline']
  外部脚本 ext.js : yes          ← 加载并执行成功
```

关键推理：**`'unsafe-inline'` 根本不覆盖外部脚本**（它只覆盖内联块），
所以外部脚本 `ext.js` 能执行，只能是因为走了 `'self'` ——
即 **`file://` 下 `'self'` 确实匹配同目录资源**。

结论：**外置 `<script>` 本身是安全的，不需要改动 CSP。**

**探针自身的两次修正（值得记下）**：

1. v1 两档都「未回报」—— 命令行 Swift 程序没有 `NSApplication` 事件循环，
   WKWebView 根本不会开始加载。**那不是结论，是探针没跑起来。**
2. v2 收紧档报「被拦」—— 但报告是**内联脚本**发的，去掉 `'unsafe-inline'` 后内联被拦，
   于是收不到回报，我却解读成「外部脚本被拦」。**两者无法区分，判定逻辑有 bug。**
   v3 改用 Swift 侧 `evaluateJavaScript` 取值（不受页面 CSP 约束）才排除歧义 ——
   但 v3 因工具落盘问题没跑成，**故收紧档的结论本轮未取得**，不做任何声称。

### 二、代价：15 个测试文件的 114 条断言

实做外置后门禁立刻变红，`unittest` 报 **114 个失败/错误，横跨约 15 个测试文件**：

```
AssertionError: 未找到函数 runSkillInsight
ERROR: test_... (test_advice_layout / test_usage_auto_refresh / test_merge_ui /
       test_design_spec / test_dashboard_*_ui / test_gui_inline_style_csp /
       test_probe_verdict_ui / test_rerun_progress / test_modal_top_layer / ...)
```

根因统一：这些用例都靠 `Path("gui/dashboard.html").read_text()` + 正则抽取
**JS 函数体**来做断言（例如 `_fn("runSkillInsight")`）。脚本外置后函数不在 HTML 里了。

`tests/gui_helpers.test.mjs` 我已改成「优先读 `dashboard.js`、回退 HTML 内联块」并通过（5/5），
但剩余 15 个文件的改造量远大于 S10 本身的价值。

### 三、判断：回退

S10 的收益是**纯组织性**的（单文件拆成 HTML + JS），不改变任何用户可见行为或安全性；
而代价是重写 15 个测试文件、且**极可能在改造过程中削弱断言**（例如把精确断言退化为存在性检查）。
收益/风险不成比例，故**回退**：

```
git checkout -- gui/dashboard.html tests/gui_helpers.test.mjs
rm -f gui/dashboard.js
→ 门禁恢复：961 用例 OK(skipped=2)，工作区干净
```

### 四、若将来要做，正确顺序

1. **先**在 `tests/` 加一个共享取源辅助（如 `tests/_gui_source.py`：
   「优先 `dashboard.js`，回退 HTML 内联块」），把 15 个文件的取源方式统一改过去；
2. **确认门禁全绿**（此时脚本仍在 HTML 里，取源辅助走回退分支）；
3. **再**外置脚本 —— 此时测试已与「脚本放在哪个文件」解耦，外置不会引起任何红；
4. 顺带完成 S2 的前置（外置脚本后，`script-src` 才有可能去 `'unsafe-inline'`，
   但仍受 `withGlobalTauri` 与 77 处内联 `style=` 属性双重约束，见 S2 专节）。
