# skill-mcp-studio 详细设计文档

**——阶段四：按需加载与启停**

## 版本

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-09-01 | 基于《立项评估-阶段四-按需加载与启停》v1.0（四维度通过、立项）编写；聚焦启停状态一致性审计 + 禁用生效验证 + 回滚幂等 gate，不造开关同步器 |

## 1. 引言

### 1.1 目的与范围

阶段四把现有四层验证模型（安装证据 → 路径合规 → 配置正确 → 端点活体）**延伸到启停维度**——第 5 层验证：启停状态是否跨客户端一致、禁用是否真正生效、启停操作是否可幂等回滚。

文档回答「怎么做」：启停状态归一模型、数据契约扩展、启停操作与审计两通道、安全工作流复用、CLI、测试、ADR。

- **做（首版）**：启停状态归一三态模型；跨客户端启停一致性审计（`--audit-skill-states`）；最小启停操作（`--enable-skill`/`--disable-skill`）作为审计与回滚的对象，复用 `mcp_fixer.py` 备份-原子-校验-回滚范式；回滚幂等 gate；禁用生效验证（配置层）。
- **不做（远期/刻意）**：运行时按会话注入（生态无人做到，首版不做）；技能商店/同步分发（§6 刻意不做）；开关同步器形态（不复刻 skills-hub / claude-code-tool-manager）。

### 1.2 承继与现状

- 阶段一 §12 数据契约：`check_agents` 返回 record 结构（`combined_checker.py:133-146`），`records` 字段**只增不改名不删除**；
- 阶段一 §5.2 能力组泛化：状态模型「7 固定字段 + N 能力组字段」，启停态作为新增固定字段族延续此数据驱动范式；
- 阶段二 §9.4 退出码约定：`0` 全绿 / `1` 存在不合规 / `2` 配置错误——阶段四沿用；
- 阶段三 §2 数据契约 + `state` 字段注入（`multi_profile_reporter`）+ 只读看板：阶段四的 `skill_states` 矩阵可被阶段三看板直接消费（只增字段）；
- `core/mcp_fixer.py:355-479` 的 `_validate_written_config` / `_backup_path` / `_atomic_write` / `_restore_backup` / `fix_mcp_tool` 是启停写操作可直接复用的安全骨架；
- `core/checker.py:52-76` `_all_entries_point_to_unified` 已认可「客户端 skills 目录内逐技能 symlink」为合规形态——阶段四启停的「单技能 symlink」与其同构（只读检测已有，缺创建器）；
- 立项评估 §5.3 三处生态空白（运行时注入无人做到 / 跨客户端启停一致性审计缺失 / 回滚幂等审计薄弱）均落在「验证与修复」定位内——阶段四差异化聚焦后两者。

### 1.3 DoD（阶段四退出标准）

> 启停状态一致性审计 + 禁用生效验证（配置层）+ 回滚幂等 gate 三能力交付并测试全绿；启停写操作复用 `mcp_fixer` 安全范式、不引入新写路径；审计通道只读、无写副作用；写操作仍仅 CLI 显式触发（延续 §8 硬边界）。

## 2. 设计目标与约束

### 2.1 目标

1. **启停状态归一**：把各客户端异构启停语义（Claude Code `disabledMcpServers`、Codex profile disables、symlink 存在与否）归一为统一三态，供跨客户端一致校验；
2. **第 5 层验证**：在四层验证之上新增启停一致性维度，纳入 record / summary / `result_ok`（可选）；
3. **跨客户端漂移检测**：`--audit-skill-states` 检测「同技能在 A 客户端启用、B 客户端禁用」的漂移；
4. **禁用生效验证（配置层）**：校验「禁用」在客户端配置中是否正确体现，针对竞品缺陷（如 Claude Code issue #36868 禁用泄漏类）做一致性护栏；
5. **回滚幂等 gate**：验证 `enable → disable → enable` 状态可逆且操作幂等；
6. **最小启停操作**：`--enable-skill` / `--disable-skill` 作为审计与回滚的对象，复用安全工作流。

### 2.2 约束（硬性，不可破）

1. **审计与修复分离**：`--audit-skill-states` 只读、无写副作用；启停写操作显式触发（`--enable-skill`/`--disable-skill`），GUI 维持只读（延续阶段三 ADR-15）；
2. **复用安全范式，不引入新写路径**：启停写操作套用 `mcp_fixer.py:355-479` 的备份-原子-校验-回滚，不另造写工作流；
3. **形态向上兼容**：`records` 只增不改名不删除；不引启停的客户端/profile 行为零回归；
4. **不滑入技能商店/同步分发**（§6）：启停只开关**本地已存在**技能，不做分发/上架/检索/云端/多用户；
5. **客户端形态前提**：单技能启停要求客户端 skills 目录为「逐技能 symlink」形态（DSH 式，`_all_entries_point_to_unified` 已认可）；「根 symlink」形态客户端首版只支持整体审计、不支持单技能启停（见 §14 开放项 1）。

## 3. 总体架构

### 3.1 第 5 层验证在四层之上

```
L5  启停一致   同技能跨客户端 enabled/disabled 是否一致；禁用是否生效；回滚是否幂等
    ▲
L4  端点活体   initialize + tools/list + 能力组覆盖（阶段一/二）
    ▲
L3  配置正确   MCP 条目按客户端格式配置正确
    ▲
L2  路径合规   Skills 路径指向统一仓库
    ▲
L1  安装证据   客户端是否真实安装
```

L5 与 L1–L4 **各自独立判结论、不做短路合并**（延续产品 §3 四层独立验证模型）：路径合规但启停漂移、配置正确但禁用泄漏，都是合法且需分别看到的状态。

### 3.2 双通道：启停操作（写）与启停审计（读）

```
┌─ 启停操作通道（写，显式触发）─────────────────────────────┐
│  --enable-skill S [--client C]  →  skill_toggle.enable_skill  │
│  --disable-skill S [--client C] →  skill_toggle.disable_skill │
│        └─ 备份(_backup) → 原子建/删 symlink → 校验 → 回滚     │
│        └─ 复用 mcp_fixer 范式（§8）                            │
│        └─ 复检：check_agents 重算 L1–L4 + skill_states（§6.1）│
└──────────────────────────────────────────────────────────────┘
┌─ 启停审计通道（读，硬边界）────────────────────────────────┐
│  --audit-skill-states  →  skill_state_auditor.audit           │
│        └─ 扫描各 installed 客户端 skill_states → 归一矩阵      │
│        └─ 漂移检测（同技能跨客户端不一致）                      │
│        └─ 禁用生效验证（配置层一致性）                          │
│        └─ 回滚幂等 gate（enable→disable→enable 可逆）          │
│        └─ 产出 skill_state_drift + summary.skill_state_consistent│
└──────────────────────────────────────────────────────────────┘
```

两通道共享同一份 `skill_state` 归一模型与 `check_agents` 产出，故「启停操作后审计结论 == 操作前规划」是结构性保证。

## 4. 核心数据模型

### 4.1 启停状态归一三态（ADR-17）

对每个「已安装客户端 × 仓库内技能」组合，归一为三态：

| 态 | 含义 | 判定（以「逐技能 symlink」形态客户端为准） |
|----|------|---------------------------------------------|
| `enabled` | 该技能在该客户端可用 | 客户端 skills 目录内存在指向仓库对应技能的有效 symlink |
| `disabled` | 该技能在仓库存在，但该客户端未启用 | 无 symlink，或被客户端原生 disabled 机制标记（如 Claude Code `disabledMcpServers`） |
| `not_in_repo` | 技能不在统一仓库 | 仓库内无该技能目录，无法启停（不计入漂移） |

> 借鉴 MCP-Server-Selector 的 RED/GREEN/ORANGE 三态建模，但归一到 skill-mcp-studio 的「仓库存在性 × 客户端启用性」二元语义，适配多客户端异构。

### 4.2 record 扩展（`combined_checker.py:133-146` 增字段，只增不改名）

每 installed 客户端 record 增：

```json
{
  "name": "Claude Code",
  ... 现有 12 字段不变 ...,
  "skill_states": {"memory-basic": "enabled", "tdai": "disabled"},
  "skills_enabled_count": 1,
  "skills_total": 2,
  "skill_link_form": "per_skill" | "root" | "mixed",
  "native_disabled_skills": ["tdai"]
}
```

- `skill_states`：dict[skill_name, `"enabled"`|`"disabled"`]，仅含仓库内技能；`not_in_repo` 不入此 dict；
- `skills_enabled_count` / `skills_total`：摘要，`skills_total` = 仓库内技能数；
- `skill_link_form`：该客户端 skills 目录形态——`per_skill`（逐技能 symlink，支持单技能启停）/ `root`（根 symlink，仅整体审计）/ `mixed`（混合，告警）；
- `native_disabled_skills`：list[str]，native_disable 挂载点（如 Claude Code `disabledMcpServers`）覆盖后仍标记为禁用的仓库内技能名，有序；供审计豁免 symlink 残留检查与看板 tooltip 呈现（2026-09-05 评审 DATA-2 补录入契约）。

**无 skills 扫描路径客户端的取值约定**（2026-09-05 评审 DATA-3 定案）：客户端无统一目录或其 `skills_paths` 全部不存在时，注入 `skill_states: {}`、`skills_enabled_count: 0`、`skills_total: 0`、`skill_link_form: "mixed"`——即按「无有效形态」保守处理，不参与漂移审计（`skill_state_auditor` 跳过空 `skill_states`）；未来若需区分「未纳管」与「混合」，以新增 `n/a` 取值扩展，不改现有语义。

### 4.3 results 扩展（`check_agents` 返回 + 汇总报告，只增）

`check_agents` 返回增 `skill_state_drift`；`--all-profiles --format json` 的 `results[i]` 同步注入：

```json
{
  ... 现有 endpoint/probe/records/unmanaged/summary/capability_groups ...,
  "skill_state_drift": [
    {
      "skill": "memory-basic",
      "states": {"Claude Code": "enabled", "Cursor": "disabled", "Codex": "enabled"},
      "consistent": false,
      "drift_clients": ["Cursor"]
    }
  ]
}
```

`summary` 增 `skill_state_consistent: bool`（无漂移为 true）。

### 4.4 `result_ok` 扩展（向后兼容）

`combined_checker.py:160-189` `result_ok` **默认不纳入**启停一致性（保 L1–L4 语义与阶段一/二完全一致，零回归）。`--audit-skill-states` 单独产出漂移结论；仅当显式 `--strict-skill-state` 时，`result_ok` 额外要求 `skill_state_consistent`。

### 4.5 契约不变量

延续阶段三 §2.4：`records` 字段只增不改名不删除；`skill_states` / `skill_state_drift` 作为新增字段，阶段三看板与 CI 断言按「字段缺失视为未审计」处理，不破坏现有消费者。

## 5. 模块详细设计

| 模块 | 职责 | 新增/改 | 关键函数 |
|------|------|---------|----------|
| `core/skill_state.py` | 启停状态归一三态 + 读写 | 新增 | `classify_skill_state(client, skill, scan_result)` → 三态；`read_skill_states(tool, scan_result)`；`write_skill_state(tool, skill, state, dry_run)` |
| `core/skill_toggle.py` | 启停操作 + 安全工作流 | 新增 | `enable_skill(tool, skill, dry_run)`；`disable_skill(tool, skill, dry_run)`；复用 `mcp_fixer._backup_path`/`_atomic_write`/`_restore_backup`（symlink 路径见 §8.2） |
| `core/skill_state_auditor.py` | 一致性审计 + 禁用生效 + 回滚幂等 gate | 新增 | `audit_skill_states(result)` → drift/生效/幂等；`verify_disable_effective(tool, skill)`；`verify_rollback_idempotent(tool, skill)` |
| `core/skills_analyser.py` | 技能清单（仓库内技能） | 改（扩展） | `scan_skills_distribution` 已有（`skills_analyser.py:16-98`），作为启停候选技能来源 |
| `core/checker.py` | per-skill link 检测 | 改（扩展） | `_all_entries_point_to_unified` 已有（`checker.py:52-76`）；新增 `_per_skill_link_states(path, unified)` 返回每技能 symlink 态 |
| `core/combined_checker.py` | record 注入 skill_states | 改（阶段四增量） | `check_agents` 末尾增 `skill_states`/`skills_enabled_count`/`skills_total`/`skill_link_form`（`combined_checker.py:133-146` 之后） |
| `core/multi_profile_reporter.py` | 汇总注入 skill_state_drift | 改（阶段四增量） | `report_all_profiles` 为每 result 注入 `skill_state_drift` + `summary.skill_state_consistent` |
| `core/mcp_fixer.py` | 安全范式（被复用，不改语义） | 不变 | `_atomic_write`/`_backup_path`/`_restore_backup`/`_validate_written_config` 被 `skill_toggle` 复用 |

## 6. 关键流程

### 6.1 启停操作流（`--enable-skill S [--client C]`）

1. **前置门**：`tool.fix_supported` false → 拒绝；`skill_link_form != "per_skill"` → 报告「该客户端为根 symlink 形态，不支持单技能启停，见 §14.1」，skip 不计失败；
2. **技能存在性**：`skill` 不在仓库（`skills_analyser` 清单无）→ exit 2 配置错误；
3. **备份**：若客户端 skills 目录内已有该技能 symlink，记录原 target（`os.readlink`）作为回滚凭据；`--dry-run` 则只报告将建/删；
4. **原子建/删 symlink**（§8.2）：`enable` → `os.symlink(相对路径, 目标)` 经临时名 + `os.replace` 原子替换；`disable` → `os.replace` 移除（POSIX 对 symlink 原子）；
5. **写后校验**：`_per_skill_link_states` 重读，确认 `enabled`/`disabled` 与目标一致；
6. **失败回滚**：校验失败 → 用备份 target 重建原 symlink（`_restore_backup` 范式）；
7. **复检闭环**：复用 `scan.py:523-536`（`--setup-all` 复检）路径——重跑 `check_agents` + `skill_states`，`result_ok` 产出退出码。

### 6.2 审计流（`--audit-skill-states`，只读）

1. 扫描所有 installed 客户端，`read_skill_states` 构建归一矩阵（client × skill → 三态）；
2. **漂移检测**：对每个仓库内技能，比较跨客户端 `enabled`/`disabled` 是否一致；不一致 → 入 `skill_state_drift`，标 `drift_clients`；
3. **禁用生效验证（配置层）**：对 `disabled` 态，校验客户端配置是否一致体现（如 Claude Code `disabledMcpServers` 是否含该技能，或 symlink 是否真缺失）；不一致 → 标「禁用未生效」（针对泄漏类缺陷）；
4. **回滚幂等 gate**：对启停操作过的技能，验证 `enable → disable → enable` 后状态恢复且操作可重复（fixture 驱动，非破坏性）；
5. 产出 `skill_state_drift` + `summary.skill_state_consistent` + 审计报告（table/md/json）。

### 6.3 与阶段三看板集成

`skill_states` 矩阵作为阶段三 `gui/dashboard.html` 的新增动态列（client 行 × skill 列，三态着色：绿 enabled / 黄 disabled / 灰 not_in_repo），纯渲染、只读，延续 ADR-16 自包含 Web。

## 7. CLI 设计

```text
--enable-skill SKILL [--client CLIENT]   为指定客户端（缺省全部 per_skill 形态客户端）启用某技能
--disable-skill SKILL [--client CLIENT]  禁用某技能（复用备份-原子-校验-回滚）
--audit-skill-states                     只读：跨客户端启停一致性审计 + 禁用生效 + 回滚幂等 gate
--list-skill-states [--client CLIENT]    列出技能启停状态矩阵
--strict-skill-state                     与 --audit-skill-states 或 --full 同用时，把启停一致性纳入 result_ok
```

- 启停操作自带 `--dry-run` 预览（延续 `--fix-mcp --dry-run` 语义）；
- 退出码沿用 `0`/`1`/`2`：审计无漂移且启停全绿 `0`；存在漂移 `1`；技能不存在/客户端形态不支持启停的配置错误 `2`；
- `--audit-skill-states` 与 `--all-profiles` 可组合，产出跨 profile 的启停漂移矩阵。

## 8. 安全设计

### 8.1 启停写操作复用 `mcp_fixer` 范式（ADR-18）

| 环节 | 复用接口（`mcp_fixer.py`） | 启停落地 |
|------|----------------------------|----------|
| 备份 | `_backup_path:389` + `shutil.copy2` | 备份受影响 skills 目录/记录原 symlink target |
| 原子写 | `_atomic_write:394`（tempfile+fsync+os.replace） | 文本状态文件直接套用；symlink 见 §8.2 |
| 写后校验 | `_validate_written_config:355` 范式 | `_per_skill_link_states` 重读校验链接态 |
| 失败回滚 | `_restore_backup:411` | 用备份 target 重建原 symlink |
| 先决门 | `fix_mcp_tool:427` `fix_supported` 拒绝 | 启停对 `fix_supported: false` 拒绝 |
| 复检 | `scan.py:523-536` + `result_ok:160` | 启停后复检产出退出码 |

### 8.2 symlink 原子替换（ADR-21）

`fixer.py:88-91` 的 symlink 创建**非原子**（`unlink`/`move` 与 `symlink` 之间有窗口）。阶段四启停**必须**原子化：

- `enable`：先 `os.symlink(rel_path, temp_name)` 建临时名，再 `os.replace(temp_name, target)` 原子替换路径项（POSIX 对 symlink 的 `rename` 原子）；`finally` 清理残留 temp；
- `disable`：`os.replace(target, backup_name)` 原子移除（POSIX）；回滚则反向 `os.replace`；
- Windows 兼容：symlink 创建需开发者模式/管理员，首版 macOS 首发，Windows 沿用阶段二 §14.5 延期口径。

### 8.3 审计只读硬边界（ADR-19）

`--audit-skill-states` / `--list-skill-states` 走 `run_scan` 只读路径（阶段三 §6.B 已核实 `--all-profiles --format json` early return 不经阶段 9 写盘），**无任何写副作用**；不缓存凭据；不触发 `--fix-*`。延续 ADR-15：GUI 维持只读，启停写操作仍仅 CLI 显式触发。

## 9. 错误处理与退出行为

| 场景 | 退出码 | 行为 |
|------|--------|------|
| 审计无漂移、启停全绿 | `0` | — |
| 存在启停漂移或禁用未生效 | `1` | 列出 drift_clients + 未生效项 |
| 技能不在仓库 / 客户端形态不支持单技能启停（`skill_link_form != per_skill`） | `2` | 配置错误，skip 不计为审计失败 |
| 启停写操作校验失败 | `1`（已回滚） | 报告「已回滚」，原状态不变 |
| 启停写操作回滚失败 | `2` | 报告「回滚失败 + 备份路径」，需人工介入 |

## 10. 测试设计

- **三态归一单测**：给定 client×skill 组合，断言 `classify_skill_state` 输出 `enabled`/`disabled`/`not_in_repo`，含 symlink 缺失、原生 disabled 标志、仓库无技能边界；
- **漂移检测单测**：构造同技能跨客户端不一致 fixture，断言 `audit_skill_states` 产出 `drift_clients` 正确；
- **禁用生效验证单测**：构造「配置层 disabled 但 symlink 仍存在」的泄漏 fixture，断言标「禁用未生效」；
- **回滚幂等单测**：`enable → disable → enable` 后 `skill_states` 恢复原状且操作可重复；
- **安全工作流单测**：启停的备份/原子/校验/回滚四环节，含「写后校验失败触发回滚」「回滚失败保留备份」；
- **契约 schema 测试**：`check_agents` / `report_all_profiles` 返回 JSON 含 `skill_states`/`skill_state_drift`/`skill_link_form` 字段且类型匹配 §4（防字段漂移回归）；
- **形态门测试**：`root` 形态客户端调 `--enable-skill` → skip 不失败、不写；
- **全量回归**：阶段一/二/三测试全绿；不引启停的 profile 行为零变化（`--all-profiles --format json` 现有字段不变）。

## 11. 目标目录结构

```
skill-mcp-studio/
├── core/
│   ├── skill_state.py            # 三态归一 + 读写
│   ├── skill_toggle.py           # enable/disable + 安全工作流
│   ├── skill_state_auditor.py    # 一致性审计 + 禁用生效 + 回滚幂等 gate
│   ├── skills_analyser.py        # 扩展：技能清单作启停候选
│   ├── checker.py                # 扩展：_per_skill_link_states
│   ├── combined_checker.py       # 扩展：record 注入 skill_states
│   └── multi_profile_reporter.py# 扩展：results 注入 skill_state_drift
├── tests/
│   ├── test_skill_state.py
│   ├── test_skill_toggle.py
│   ├── test_skill_state_auditor.py
│   └── test_skill_state_contract.py   # 契约 schema
```

## 12. 阶段N预留

- **运行时按会话注入**（远期）：立项评估 §5.1 已排期为远期子项；生态无人做到，阶段四首版不做。若未来客户端开放运行时注入 API，`skill_toggle` 的写接口可扩展为运行时注入，契约 `skill_states` 不变；
- **GUI 启停看板**（阶段三看板扩展）：`gui/dashboard.html` 新增 skill 列动态渲染，纯只读，延续 ADR-16；
- **多用户/云端启停状态同步**：刻意不做（§6 刻意不做领域）。

## 13. 设计决策记录（ADR，续阶段三 ADR-13~16）

**ADR-17：启停状态归一三态模型（enabled / disabled / not_in_repo）。**
理由：各客户端启停语义异构（disabledMcpServers / profile / symlink 存在与否），跨客户端一致校验必须先归一；三态借鉴 MCP-Server-Selector 但适配 skill-mcp-studio 的「仓库存在性 × 客户端启用性」二元语义。

**ADR-18：启停写操作复用 `mcp_fixer.py` 备份-原子-校验-回滚范式，不引入新写路径。**
理由：`mcp_fixer:355-479` 已是验证过的安全骨架；启停作为新写动作复用此范式而非另造，保证「修这个动作本身不制造新事故」（延续产品 §3 安全修复承诺）。

**ADR-19：启停审计通道只读硬边界，写操作仍仅 CLI 显式触发。**
理由：审计/修复分离是产品级安全承诺；`--audit-skill-states` 走只读 `run_scan` 路径，无写副作用，与阶段三 ADR-15 一致。

**ADR-20：差异化聚焦一致性审计，不造开关同步器。**
理由：立项评估 §5.3 三处空白中，开关同步已被 skills-hub / claude-code-tool-manager 密集覆盖（落入 §6 刻意不做）；本工具定位「验证与修复」，启停一致性审计 + 禁用生效 + 回滚幂等是无竞品空白，把四层验证延伸到第 5 层正契合定位。

**ADR-21：单技能启停要求客户端 skills 目录为「逐技能 symlink」形态，symlink 原子替换。**
理由：现有 `fix_path` 建根 symlink（整体启用），单技能启停需逐技能 symlink（`_all_entries_point_to_unified` 已认可此形态）；symlink 非原子创建有窗口（`fixer.py:88-91`），启停必须用 `os.symlink(temp)+os.replace` 原子化；根 symlink 形态客户端首版只整体审计、不单技能启停（§14.1）。

## 14. 开放问题

| # | 问题 | 建议定案时点 | 收口（2026-09-04 阶段四收尾） |
|---|------|--------------|------------------------------|
| 1 | **客户端形态迁移策略**：根 symlink → 逐技能 symlink 的迁移（`--migrate-skill-links`？）首版是否实现，或首版限定 per_skill 形态客户端才支持单技能启停 | 阶段四 P1 实现前定案（建议首版限定 per_skill，迁移作 P2） | ✅ 已实现：首版按建议限定 per_skill，迁移作为 P2 落地（`core/skill_link_migrator.py` + `--migrate-skill-links`），Codex 真实迁移 + 启停闭环验证通过 |
| 2 | **各客户端原生 disabled 机制归一覆盖度**：Claude Code `disabledMcpServers`、Codex profile disables 等的归一读取首版覆盖哪些 | 阶段四 P2，按客户端逐个接入 | ✅ 已定案：实测 8 客户端均无原生按技能禁用标记（符号链接即机制）；`native_disable` 配置驱动只读挂载点落地，未来客户端零代码接入（见决策记录 D11/D12） |
| 3 | **skill-mcp-manager 人工核实**：立项评估 §4 表中 repo 不可达，启停/回滚语义未验证，立项前需人工核实是否与本工具重叠 | 阶段四开工前 | ✅ 已核实（决策记录 D14）：repo 已可达，系 DSH 宿主级插件，运行时层按需加载，与本工具配置层跨客户端审计互补不重叠；本机未安装，无影响 |
| 4 | **运行时禁用生效验证**：首版做配置层一致性（§6.2 步骤3），运行时「禁用是否真从会话消失」属远期（生态无人做到，含 Claude Code issue #36868 泄漏） | 阶段四后评估 | ✅ 已评估（决策记录 D13）：配置层一致性即 DoD；#36868 证实连原生禁用亦会泄漏且以 not_planned 关闭；重开触发条件已列 |

> 立项评估已收口：四维度通过、立项；运行时按会话注入（远期）、不造开关同步器（刻意不做）两项不在本阶段开放问题内，已定案。
